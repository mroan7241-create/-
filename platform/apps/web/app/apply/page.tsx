'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { ApiClientError, createApplicationDraft, getApplicationGeography, getReferenceData, loadApplicationDraft, saveApplicationDraft, submitApplicationDraft, uploadApplicationAttachment, type GeographicUnit, type ReferenceData } from '../lib/api';
import styles from './application-v2.module.css';

const DRAFT_KEY = 'alzad.apply.v2.draft';
const STEP_LABELS = ['الجمعية والموقع', 'القيادة والجاهزية', 'المستفيدون والبيانات', 'الخبرة السابقة', 'الحوكمة والمالية', 'التخطيط والاستدامة', 'المرفقات والإقرار'];
const EDUCATION_OPTIONS = ['ثانوي فأقل', 'دبلوم', 'بكالوريوس', 'دراسات عليا'];
const SATISFACTION_OPTIONS = ['استبانة', 'اتصال', 'زيارة', 'نظام إلكتروني', 'أخرى'];
const ATTACHMENT_LABELS: Record<string, string> = { licenseFile: 'الترخيص', previousProjectEvidence: 'شاهد تنفيذ المشروع السابق', spendingPolicyFile: 'لائحة الصرف المعتمدة', strategicPlanFile: 'الخطة الاستراتيجية', operationalPlanFile: 'الخطة التشغيلية', initialBeneficiaryFile: 'القائمة الأولية للمستفيدين (اختياري)', financialStatementsFile: 'القوائم المالية المعتمدة/المراجعة (اختياري)' };
type Payload = Record<string, unknown>;
type Credentials = { draftCode: string; resumeToken: string };

export default function ApplyPage() {
  const router = useRouter();
  const [mode, setMode] = useState<'landing' | 'resume' | 'form'>('landing');
  const [step, setStep] = useState(1); const [payload, setPayload] = useState<Payload>({});
  const [credentials, setCredentials] = useState<Credentials | null>(null); const [resumeCode, setResumeCode] = useState(''); const [resumeToken, setResumeToken] = useState('');
  const [revision, setRevision] = useState(0); const revisionRef = useRef(0); const saveQueue = useRef<Promise<number>>(Promise.resolve(0));
  const [saveState, setSaveState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle'); const [attachments, setAttachments] = useState<string[]>([]); const [uploading, setUploading] = useState('');
  const [regions, setRegions] = useState<GeographicUnit[]>([]); const [governorates, setGovernorates] = useState<GeographicUnit[]>([]); const [centers, setCenters] = useState<GeographicUnit[]>([]); const [reference, setReference] = useState<ReferenceData | null>(null);
  const [error, setError] = useState(''); const [success, setSuccess] = useState<{ id: string; message: string } | null>(null); const [busy, setBusy] = useState(false); const [storedDraft, setStoredDraft] = useState<Credentials | null>(null);

  useEffect(() => { try { const raw = localStorage.getItem(DRAFT_KEY); if (raw) setStoredDraft(JSON.parse(raw) as Credentials); } catch { /* ignore invalid browser metadata */ } void Promise.all([getApplicationGeography(), getReferenceData()]).then(([geo, refs]) => { setRegions(geo.items); setReference(refs); }).catch(() => setError('تعذّر تحميل القوائم المرجعية. أعد تحميل الصفحة.')); }, []);
  const regionCode = textAt(payload, 'location.regionCode'); const governorateCode = textAt(payload, 'location.governorateCode');
  useEffect(() => { if (!regionCode) return setGovernorates([]); void getApplicationGeography(regionCode).then((r) => setGovernorates(r.items)).catch(() => setError('تعذّر تحميل المحافظات التابعة للمنطقة.')); }, [regionCode]);
  useEffect(() => { if (!governorateCode) return setCenters([]); void getApplicationGeography(governorateCode).then((r) => setCenters(r.items)).catch(() => setError('تعذّر تحميل المراكز التابعة.')); }, [governorateCode]);
  // enqueueSave intentionally follows the current draft credentials and serialized revision queue.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { if (mode !== 'form' || !credentials || success) return; const timer = window.setTimeout(() => { void enqueueSave(payload); }, 900); return () => window.clearTimeout(timer); }, [payload, credentials, mode, success]);

  function enqueueSave(snapshot: Payload): Promise<number> { if (!credentials) return Promise.resolve(revisionRef.current); saveQueue.current = saveQueue.current.then(async () => { setSaveState('saving'); try { const result = await saveApplicationDraft(credentials.draftCode, credentials.resumeToken, revisionRef.current, snapshot); revisionRef.current = result.revision; setRevision(result.revision); setSaveState('saved'); return result.revision; } catch (reason) { setSaveState('error'); if (reason instanceof ApiClientError && reason.code === 'APPLICATION_DRAFT_REVISION_CONFLICT') setError(reason.message); throw reason; } }); return saveQueue.current; }
  async function startNew() { setBusy(true); setError(''); try { const result = await createApplicationDraft(crypto.randomUUID()); const next = { draftCode: result.draftCode, resumeToken: result.resumeToken }; localStorage.setItem(DRAFT_KEY, JSON.stringify(next)); setStoredDraft(next); setCredentials(next); setRevision(result.revision); revisionRef.current = result.revision; setPayload({}); setAttachments([]); setStep(1); setMode('form'); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } }
  async function resume(existing?: Credentials) { const next = existing ?? { draftCode: resumeCode.trim(), resumeToken: resumeToken.trim() }; if (!next.draftCode || !next.resumeToken) return setError('أدخل رقم المسودة ورمز المتابعة الآمن.'); setBusy(true); setError(''); try { const result = await loadApplicationDraft(next.draftCode, next.resumeToken); localStorage.setItem(DRAFT_KEY, JSON.stringify(next)); setStoredDraft(next); setCredentials(next); setPayload(result.payload); setAttachments(result.attachments); setRevision(result.revision); revisionRef.current = result.revision; if (result.submitted) router.push('/apply/status'); else setMode('form'); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } }
  function setValue(path: string, value: unknown, clearPaths: string[] = []) { setPayload((current) => { let next = setAt(current, path, value); for (const clearPath of clearPaths) next = removeAt(next, clearPath); return next; }); }
  async function upload(fieldKey: string, file: File | null) { if (!file || !credentials) return; setUploading(fieldKey); setError(''); try { await uploadApplicationAttachment(credentials.draftCode, credentials.resumeToken, fieldKey, file); setAttachments((current) => [...new Set([...current, fieldKey])]); } catch (reason) { setError(readError(reason)); } finally { setUploading(''); } }
  function goNext() { const message = validateStep(step, payload, attachments); if (message) return setError(message); setError(''); setStep((current) => Math.min(7, current + 1)); window.scrollTo({ top: 0, behavior: 'smooth' }); }
  async function submit() { const message = validateStep(7, payload, attachments); if (message || !credentials) return setError(message || 'تعذّر التحقق من بيانات المسودة.'); setBusy(true); setError(''); try { const finalRevision = await enqueueSave(payload); const result = await submitApplicationDraft(credentials.draftCode, credentials.resumeToken, finalRevision); setSuccess(result); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } }

  if (mode === 'landing') return <main className={styles.page}><div className={styles.wrap}><header className={styles.hero}><h1>التقديم على مشروع الأجهزة الكهربائية</h1><p>قدّم طلب جمعية جديدًا أو عُد إلى طلب محفوظ لمتابعته واستكمال ما تطلبه الإدارة.</p></header>{error && <p role="alert" className={styles.error}>{error}</p>}{storedDraft && <section className={styles.credentials}><strong>توجد مسودة محفوظة على هذا الجهاز</strong><p>رقمها: <b dir="ltr">{storedDraft.draftCode}</b></p><button className={styles.buttonSecondary} onClick={() => void resume(storedDraft)} disabled={busy}>استكمال المسودة المحفوظة</button></section>}<div className={styles.landing}><button className={styles.choice} onClick={() => void startNew()} disabled={busy}><strong>تقديم طلب جديد</strong><span>ابدأ نموذجًا ذكيًا من سبع خطوات مع حفظ تلقائي.</span></button><button className={styles.choice} onClick={() => { setError(''); setMode('resume'); }}><strong>متابعة / استكمال طلب سابق</strong><span>تابع الحالة أو استكمل مسودة وبيانات ناقصة باستخدام رمز المتابعة.</span></button></div></div></main>;
  if (mode === 'resume') return <main className={styles.page}><div className={styles.wrap}><header className={styles.hero}><h1>متابعة / استكمال طلب سابق</h1><p>استخدم رقم المسودة ورمز المتابعة الآمن اللذين ظهرا عند بدء الطلب.</p></header><section className={styles.resumeBox}><div className={styles.grid}><Field label="رقم المسودة"><input className={styles.input} dir="ltr" value={resumeCode} onChange={(e) => setResumeCode(e.target.value)} /></Field><Field label="رمز المتابعة الآمن"><input className={styles.input} dir="ltr" type="password" value={resumeToken} onChange={(e) => setResumeToken(e.target.value)} /></Field></div>{error && <p className={styles.error}>{error}</p>}<div className={styles.toolbar}><button className={styles.buttonSecondary} onClick={() => setMode('landing')}>رجوع</button><button className={styles.button} disabled={busy} onClick={() => void resume()}>{busy ? 'جارٍ التحقق…' : 'فتح الطلب'}</button><Link href="/apply/status">متابعة الحالة فقط</Link></div></section></div></main>;
  if (success) return <main className={styles.page}><div className={styles.wrap}><header className={styles.hero}><h1>تم استلام الطلب</h1><p>{success.message}</p></header><section className={styles.panel}><p className={styles.success}>رقم الطلب: <b dir="ltr">{success.id}</b></p><p>احتفظ برقم المسودة ورمز المتابعة لمتابعة الحالة أو إرسال أي استكمال مطلوب.</p><Link className={styles.button} href="/apply/status">متابعة حالة الطلب</Link></section></div></main>;

  return <main className={styles.page}><div className={styles.wrap}><header className={styles.hero}><h1>طلب مشاركة جمعية</h1><p>الخطوة {step} من 7 — تُحفظ بياناتك تلقائيًا ويمكنك العودة إليها لاحقًا.</p></header>{credentials && <section className={styles.credentials}><strong>احفظ بيانات الاستكمال في مكان آمن</strong><span>رقم المسودة</span><code>{credentials.draftCode}</code><span>رمز المتابعة</span><code>{credentials.resumeToken}</code></section>}<div className={styles.toolbar}><button className={styles.buttonSecondary} onClick={() => setMode('landing')}>العودة للبداية</button><span className={styles.save}>{saveState === 'saving' ? 'جارٍ الحفظ…' : saveState === 'saved' ? `تم الحفظ — النسخة ${revision}` : saveState === 'error' ? 'تعذّر الحفظ — أعد المحاولة' : 'جاهز للحفظ'}</span></div><ol className={styles.stepper} aria-label="مراحل الطلب">{STEP_LABELS.map((label, index) => <li key={label} className={`${styles.step} ${step === index + 1 ? styles.stepActive : step > index + 1 ? styles.stepDone : ''}`} aria-current={step === index + 1 ? 'step' : undefined}><b>{index + 1}</b>{label}</li>)}</ol>{error && <p role="alert" className={styles.error}>{error}</p>}
    {step === 1 && <StageOne payload={payload} setValue={setValue} reference={reference} regions={regions} governorates={governorates} centers={centers} />}{step === 2 && <StageTwo payload={payload} setValue={setValue} />}{step === 3 && <StageThree payload={payload} setValue={setValue} upload={upload} uploading={uploading} attachments={attachments} />}{step === 4 && <StageFour payload={payload} setValue={setValue} upload={upload} uploading={uploading} attachments={attachments} />}{step === 5 && <StageFive payload={payload} setValue={setValue} upload={upload} uploading={uploading} attachments={attachments} />}{step === 6 && <StageSix payload={payload} setValue={setValue} upload={upload} uploading={uploading} attachments={attachments} />}{step === 7 && <StageSeven payload={payload} setValue={setValue} upload={upload} uploading={uploading} attachments={attachments} onEdit={setStep} />}
    <nav className={styles.actions}>{step > 1 ? <button className={styles.buttonSecondary} onClick={() => { setError(''); setStep((current) => current - 1); }}>السابق</button> : <span />}{step < 7 ? <button className={styles.button} onClick={goNext}>التالي</button> : <button className={styles.button} disabled={busy || saveState === 'saving'} onClick={() => void submit()}>{busy ? 'جارٍ الإرسال…' : 'إرسال الطلب'}</button>}</nav></div></main>;
}

type StageProps = { payload: Payload; setValue: (path: string, value: unknown, clear?: string[]) => void };
type UploadProps = { upload:(fieldKey:string,file:File|null)=>Promise<void>; uploading:string; attachments:string[] };

function StageOne({ payload, setValue, reference, regions, governorates, centers }: StageProps & { reference: ReferenceData | null; regions: GeographicUnit[]; governorates: GeographicUnit[]; centers: GeographicUnit[] }) { const otherLocation=boolAt(payload,'location.useOther')===true; return <><Section title="بيانات الجمعية" description="اكتب البيانات كما تظهر في السجلات الرسمية."><div className={styles.grid}><TextField label="اسم الجمعية الرسمي" value={textAt(payload,'organization.name')} onChange={(v)=>setValue('organization.name',v)} /><TextField label="رقم الترخيص" value={textAt(payload,'organization.licenseNumber')} onChange={(v)=>setValue('organization.licenseNumber',v)} /><TextField label="تاريخ انتهاء الترخيص" type="date" value={textAt(payload,'organization.licenseExpiryDate')} onChange={(v)=>setValue('organization.licenseExpiryDate',v)} /><SelectField label="تصنيف الجمعية" value={textAt(payload,'organization.category')} options={reference?.associationCategories ?? []} onChange={(v)=>setValue('organization.category',v)} /><SelectField label="مجال/قطاع عمل الجمعية" value={textAt(payload,'organization.sector')} options={reference?.associationSectors ?? []} onChange={(v)=>setValue('organization.sector',v)} /><SearchableSelect label="المنطقة الإدارية" value={textAt(payload,'location.regionCode')} items={regions} onChange={(v)=>setValue('location.regionCode',v,['location.governorateCode','location.centerCode','location.otherText','location.useOther'])} /><SearchableSelect label="المحافظة / مقر الإمارة" value={textAt(payload,'location.governorateCode')} items={governorates} disabled={!textAt(payload,'location.regionCode')} onChange={(v)=>setValue('location.governorateCode',v,['location.centerCode','location.otherText','location.useOther'])} />{centers.length > 0 && !otherLocation && <SearchableSelect label="المركز — اختياري" value={textAt(payload,'location.centerCode')} items={centers} allowEmpty emptyLabel="داخل المدينة/المحافظة — لا ينطبق مركز" onChange={(v)=>setValue('location.centerCode',v)} />}<div className={styles.wide}><YesNo label="هل الموقع التفصيلي غير موجود في القائمة الرسمية؟" value={boolAt(payload,'location.useOther')} onChange={(v)=>setValue('location.useOther',v,v?['location.centerCode']:['location.otherText'])} /></div>{otherLocation&&<div className={styles.wide}><TextField label="اكتب الموقع غير الموجود — سيُعلّم للتحقق الإداري" value={textAt(payload,'location.otherText')} onChange={(v)=>setValue('location.otherText',v)} /></div>}<SelectField label="نطاق خدمة الجمعية" value={textAt(payload,'location.serviceScope')} options={['المدينة أو المحافظة المختارة','عدة محافظات داخل المنطقة','المنطقة الإدارية كاملة']} onChange={(v)=>setValue('location.serviceScope',v)} /><TextField label="البريد الرسمي" type="email" dir="ltr" value={textAt(payload,'organization.officialEmail')} onChange={(v)=>setValue('organization.officialEmail',v)} /><PhoneField label="رقم التواصل الرسمي" value={textAt(payload,'organization.officialPhone')} onChange={(v)=>setValue('organization.officialPhone',v)} /></div></Section><Section title="منسق المشروع"><div className={styles.grid}><TextField label="الاسم" value={textAt(payload,'coordinator.name')} onChange={(v)=>setValue('coordinator.name',v)} /><TextField label="المسمى الوظيفي" value={textAt(payload,'coordinator.title')} onChange={(v)=>setValue('coordinator.title',v)} /><PhoneField label="الجوال" value={textAt(payload,'coordinator.phone')} onChange={(v)=>setValue('coordinator.phone',v)} /><TextField label="البريد" type="email" dir="ltr" value={textAt(payload,'coordinator.email')} onChange={(v)=>setValue('coordinator.email',v)} /></div></Section><Section title="ممثل الجمعية في الميثاق"><div className={styles.grid}><TextField label="الاسم" value={textAt(payload,'covenantRepresentative.name')} onChange={(v)=>setValue('covenantRepresentative.name',v)} /><TextField label="الصفة" value={textAt(payload,'covenantRepresentative.title')} onChange={(v)=>setValue('covenantRepresentative.title',v)} /></div></Section></>; }

function StageTwo({ payload, setValue }: StageProps) { return <><Section title="المدير التنفيذي"><div className={styles.grid}><TextField label="الاسم" value={textAt(payload,'executive.name')} onChange={(v)=>setValue('executive.name',v)} /><PhoneField label="الجوال" value={textAt(payload,'executive.phone')} onChange={(v)=>setValue('executive.phone',v)} /><SelectField label="المؤهل العلمي" value={textAt(payload,'executive.education')} options={EDUCATION_OPTIONS} onChange={(v)=>setValue('executive.education',v)} /><NumberField label="سنوات الخبرة" value={numberAt(payload,'executive.experienceYears')} onChange={(v)=>setValue('executive.experienceYears',v)} /></div></Section><Section title="فريق الجمعية" description="يمكن استخدام الزرين أو كتابة العدد مباشرة."><div className={styles.grid}>{[['fullTime','دوام كامل'],['partTime','دوام جزئي'],['activeVolunteers','متطوعون نشطون'],['nonSaudis','غير سعوديين'],['universityOrHigher','جامعي فأعلى']].map(([key,label])=><CounterField key={key} label={label} value={numberAt(payload,`team.${key}`)} onChange={(v)=>setValue(`team.${key}`,v)} />)}</div></Section><Section title="الباحث الاجتماعي"><YesNo label="هل يوجد باحث اجتماعي؟" value={boolAt(payload,'socialResearcher.exists')} onChange={(v)=>setValue('socialResearcher.exists',v,v?[]:['socialResearcher.name','socialResearcher.phone'])} />{boolAt(payload,'socialResearcher.exists')===true&&<div className={styles.grid}><TextField label="الاسم" value={textAt(payload,'socialResearcher.name')} onChange={(v)=>setValue('socialResearcher.name',v)} /><PhoneField label="الجوال" value={textAt(payload,'socialResearcher.phone')} onChange={(v)=>setValue('socialResearcher.phone',v)} /></div>}</Section><Section title="الجاهزية الميدانية"><div className={styles.grid}><CounterField label="عدد أفراد الفريق الميداني" value={numberAt(payload,'readiness.fieldTeamCount')} onChange={(v)=>setValue('readiness.fieldTeamCount',v)} /><NumberField label="القدرة التقريبية للتسليم — أسرة/أسبوع" value={numberAt(payload,'readiness.weeklyDeliveryCapacity')} onChange={(v)=>setValue('readiness.weeklyDeliveryCapacity',v)} /><div className={styles.wide}><YesNo label="هل يوجد موقع/آلية مناسبة لاستلام الأجهزة وحفظها؟" value={boolAt(payload,'readiness.hasReceiptStorage')} onChange={(v)=>setValue('readiness.hasReceiptStorage',v,v?[]:['readiness.receiptStorageDescription'])} /></div>{boolAt(payload,'readiness.hasReceiptStorage')===true&&<div className={styles.wide}><TextField label="وصف مختصر" value={textAt(payload,'readiness.receiptStorageDescription')} onChange={(v)=>setValue('readiness.receiptStorageDescription',v)} /></div>}<div className={styles.wide}><YesNo label="هل تستطيع الجمعية توثيق الاستلام والتسليم إلكترونيًا؟" value={boolAt(payload,'readiness.canDocumentDigitally')} onChange={(v)=>setValue('readiness.canDocumentDigitally',v)} /></div></div></Section></>; }

function StageThree({ payload, setValue, upload, uploading, attachments }: StageProps & UploadProps) { const hasSystem=boolAt(payload,'beneficiaries.hasSystem'); return <><Section title="المستفيدون والبيانات"><div className={styles.grid}><NumberField label="عدد الأسر المسجلة" value={numberAt(payload,'beneficiaries.registeredFamilies')} onChange={(v)=>setValue('beneficiaries.registeredFamilies',v)} /><TextField label="تاريخ آخر تحديث شامل لقاعدة المستفيدين" type="date" value={textAt(payload,'beneficiaries.databaseUpdatedAt')} onChange={(v)=>setValue('beneficiaries.databaseUpdatedAt',v)} /><div className={styles.wide}><YesNo label="هل تستخدم الجمعية نظامًا إلكترونيًا منظمًا للمستفيدين؟" value={hasSystem} onChange={(v)=>setValue('beneficiaries.hasSystem',v,v?[]:['beneficiaries.systemName','beneficiaries.capabilities'])} /></div>{hasSystem===true&&<><TextField label="اسم النظام" value={textAt(payload,'beneficiaries.systemName')} onChange={(v)=>setValue('beneficiaries.systemName',v)} /><div className={`${styles.checks} ${styles.wide}`}>{[['search','يدعم البحث'],['update','يدعم تحديث البيانات'],['reports','يدعم استخراج التقارير'],['organizedCases','يحتفظ بالحالات بصورة منظمة']].map(([key,label])=><CheckField key={key} label={label} checked={boolAt(payload,`beneficiaries.capabilities.${key}`)===true} onChange={(v)=>setValue(`beneficiaries.capabilities.${key}`,v)} />)}</div></>}<div className={styles.wide}><YesNo label="هل تصنف الجمعية الحالات حسب الحاجة؟" value={boolAt(payload,'beneficiaries.classifiesNeed')} onChange={(v)=>setValue('beneficiaries.classifiesNeed',v,v?[]:['beneficiaries.classifications'])} /></div>{boolAt(payload,'beneficiaries.classifiesNeed')===true&&<TextField label="التصنيفات" value={textAt(payload,'beneficiaries.classifications')} onChange={(v)=>setValue('beneficiaries.classifications',v)} />}<div className={styles.wide}><YesNo label="هل لديها نموذج/آلية موحدة لدراسة الحالات؟" value={boolAt(payload,'beneficiaries.hasCaseStudyMechanism')} onChange={(v)=>setValue('beneficiaries.hasCaseStudyMechanism',v,v?[]:['beneficiaries.caseStudyDescription'])} /></div>{boolAt(payload,'beneficiaries.hasCaseStudyMechanism')===true&&<TextField label="وصف مختصر" value={textAt(payload,'beneficiaries.caseStudyDescription')} onChange={(v)=>setValue('beneficiaries.caseStudyDescription',v)} />}</div></Section><Section title="قائمة أولية للمستفيدين"><p>اختياري — يساعد في تقييم جاهزية البيانات والمفاضلة، ولا يعد اعتمادًا نهائيًا للمستفيدين أو احتياجاتهم.</p><FileField fieldKey="initialBeneficiaryFile" accept=".xlsx" upload={upload} uploading={uploading} uploaded={attachments.includes('initialBeneficiaryFile')} /></Section></>; }

function StageFour({ payload, setValue, upload, uploading, attachments }: StageProps & UploadProps) { const hasProject=boolAt(payload,'experience.hasRecentInKindProject'); return <Section title="الخبرة والدعم السابق"><div className={styles.grid}><div className={styles.wide}><YesNo label="هل نفذت خلال السنتين الماضيتين مشروع دعم عيني أو توزيع مساعدات؟" value={hasProject} onChange={(v)=>setValue('experience.hasRecentInKindProject',v,v?[]:['experience.projectName','experience.projectYear','experience.supportType','experience.projectBeneficiaries','experience.supporter'])} /></div>{hasProject===true&&<><TextField label="اسم المشروع" value={textAt(payload,'experience.projectName')} onChange={(v)=>setValue('experience.projectName',v)} /><NumberField label="سنة التنفيذ" value={numberAt(payload,'experience.projectYear')} onChange={(v)=>setValue('experience.projectYear',v)} /><TextField label="نوع الدعم" value={textAt(payload,'experience.supportType')} onChange={(v)=>setValue('experience.supportType',v)} /><NumberField label="عدد الأسر/المستفيدين" value={numberAt(payload,'experience.projectBeneficiaries')} onChange={(v)=>setValue('experience.projectBeneficiaries',v)} /><TextField label="الجهة الداعمة — اختياري" value={textAt(payload,'experience.supporter')} onChange={(v)=>setValue('experience.supporter',v)} /><FileField fieldKey="previousProjectEvidence" upload={upload} uploading={uploading} uploaded={attachments.includes('previousProjectEvidence')} /></>}<NumberField label="عدد مشاريع الدعم العيني خلال آخر سنتين" value={numberAt(payload,'experience.recentProjectsCount')} onChange={(v)=>setValue('experience.recentProjectsCount',v)} /><NumberField label="إجمالي المستفيدين التقريبي" value={numberAt(payload,'experience.recentBeneficiariesCount')} onChange={(v)=>setValue('experience.recentBeneficiariesCount',v)} /><NumberField label="عدد مرات الاستفادة من إحسان خلال 2025" value={numberAt(payload,'experience.ehsanSupportCount2025')} onChange={(v)=>setValue('experience.ehsanSupportCount2025',v)} />{numberAt(payload,'experience.ehsanSupportCount2025')>0&&<TextField label="نوع/أنواع الدعم" value={textAt(payload,'experience.ehsanSupportTypes')} onChange={(v)=>setValue('experience.ehsanSupportTypes',v)} />}<div className={styles.wide}><YesNo label="هل حصلت على دعم مشابه سابقًا؟" value={boolAt(payload,'experience.hasPreviousSimilarSupport')} onChange={(v)=>setValue('experience.hasPreviousSimilarSupport',v,v?[]:['experience.previousSupportDescription','experience.previousSupporter','experience.previousSupportYear'])} /></div>{boolAt(payload,'experience.hasPreviousSimilarSupport')===true&&<><TextField label="وصف مختصر" value={textAt(payload,'experience.previousSupportDescription')} onChange={(v)=>setValue('experience.previousSupportDescription',v)} /><TextField label="الداعم" value={textAt(payload,'experience.previousSupporter')} onChange={(v)=>setValue('experience.previousSupporter',v)} /><NumberField label="السنة" value={numberAt(payload,'experience.previousSupportYear')} onChange={(v)=>setValue('experience.previousSupportYear',v)} /></>}</div></Section>; }

function StageFive({ payload, setValue, upload, uploading, attachments }: StageProps & UploadProps) { const revenue=numberAt(payload,'finance.revenue'), expenses=numberAt(payload,'finance.expenses'), assets=numberAt(payload,'finance.currentAssets'), liabilities=numberAt(payload,'finance.currentLiabilities'); return <><Section title="الحوكمة"><div className={styles.grid}><div className={styles.wide}><YesNo label="هل تستخدم نظامًا محاسبيًا؟" value={boolAt(payload,'finance.hasAccountingSystem')} onChange={(v)=>setValue('finance.hasAccountingSystem',v,v?[]:['finance.accountingSystemName'])} /></div>{boolAt(payload,'finance.hasAccountingSystem')===true&&<TextField label="اسم النظام" value={textAt(payload,'finance.accountingSystemName')} onChange={(v)=>setValue('finance.accountingSystemName',v)} />}<div className={styles.wide}><YesNo label="هل توجد لائحة صرف معتمدة؟" value={boolAt(payload,'finance.hasSpendingPolicy')} onChange={(v)=>setValue('finance.hasSpendingPolicy',v)} /></div>{boolAt(payload,'finance.hasSpendingPolicy')===true&&<FileField fieldKey="spendingPolicyFile" upload={upload} uploading={uploading} uploaded={attachments.includes('spendingPolicyFile')} />}</div></Section><Section title="الوضع المالي" description="وفق آخر سنة مالية معتمدة ومتاحة"><div className={styles.grid}><MoneyField label="الإيرادات" value={revenue} onChange={(v)=>setValue('finance.revenue',v)} /><MoneyField label="المصروفات" value={expenses} onChange={(v)=>setValue('finance.expenses',v)} /><MoneyField label="الأصول المتداولة" value={assets} onChange={(v)=>setValue('finance.currentAssets',v)} /><MoneyField label="الخصوم المتداولة" value={liabilities} onChange={(v)=>setValue('finance.currentLiabilities',v)} /></div><p>النتيجة المالية: <b>{formatMoney(revenue-expenses)}</b> — صافي رأس المال العامل: <b>{formatMoney(assets-liabilities)}</b></p><FileField fieldKey="financialStatementsFile" upload={upload} uploading={uploading} uploaded={attachments.includes('financialStatementsFile')} /></Section></>; }

function StageSix({ payload, setValue, upload, uploading, attachments }: StageProps & UploadProps) { function planToggle(key:string,label:string,fileKey:string){return <div className={styles.wide} key={key}><YesNo label={label} value={boolAt(payload,`planning.${key}`)} onChange={(v)=>setValue(`planning.${key}`,v)} />{boolAt(payload,`planning.${key}`)===true&&<FileField fieldKey={fileKey} upload={upload} uploading={uploading} uploaded={attachments.includes(fileKey)} />}</div>} return <Section title="التخطيط والاستدامة"><div className={styles.grid}>{planToggle('hasStrategicPlan','خطة استراتيجية سارية؟','strategicPlanFile')}{planToggle('hasOperationalPlan','خطة تشغيلية حالية؟','operationalPlanFile')}<div className={styles.wide}><YesNo label="هل توجد آلية لمتابعة الأسر بعد المساعدة؟" value={boolAt(payload,'planning.hasPostAidFollowUp')} onChange={(v)=>setValue('planning.hasPostAidFollowUp',v,v?[]:['planning.postAidFollowUpDescription'])} /></div>{boolAt(payload,'planning.hasPostAidFollowUp')===true&&<TextField label="وصف الآلية" value={textAt(payload,'planning.postAidFollowUpDescription')} onChange={(v)=>setValue('planning.postAidFollowUpDescription',v)} />}<div className={styles.wide}><YesNo label="هل تقيس الجمعية رضا المستفيدين؟" value={boolAt(payload,'planning.measuresSatisfaction')} onChange={(v)=>setValue('planning.measuresSatisfaction',v,v?[]:['planning.satisfactionTool','planning.satisfactionOther'])} /></div>{boolAt(payload,'planning.measuresSatisfaction')===true&&<><SelectField label="الأداة" value={textAt(payload,'planning.satisfactionTool')} options={SATISFACTION_OPTIONS} onChange={(v)=>setValue('planning.satisfactionTool',v)} />{textAt(payload,'planning.satisfactionTool')==='أخرى'&&<TextField label="حدد الأداة" value={textAt(payload,'planning.satisfactionOther')} onChange={(v)=>setValue('planning.satisfactionOther',v)} />}</>}<NumberField label="عدد البرامج/المشاريع خلال آخر سنة مكتملة" value={numberAt(payload,'planning.lastYearProgramsCount')} onChange={(v)=>setValue('planning.lastYearProgramsCount',v)} /><NumberField label="إجمالي المستفيدين التقريبي" value={numberAt(payload,'planning.lastYearBeneficiariesCount')} onChange={(v)=>setValue('planning.lastYearBeneficiariesCount',v)} /></div></Section>; }

function StageSeven({ payload, setValue, upload, uploading, attachments, onEdit }: StageProps & UploadProps & { onEdit:(step:number)=>void }) { const acknowledgements=[['dataAccuracy','أقر بصحة جميع البيانات المقدمة.'],['verificationPermission','أسمح بالتحقق من البيانات والمستندات.'],['auditConsent','أوافق على التدقيق والمتابعة.'],['coordinatorCommitment','ألتزم بتعيين منسق عند القبول.'],['covenantCommitment','ألتزم بالميثاق عند القبول.'],['beneficiaryListPreliminary','أفهم أن قائمة المستفيدين أولية ولا تعد اعتمادًا نهائيًا.'],['participationTerms','أوافق على شروط المشاركة.']]; return <><Section title="المرفقات"><div className={styles.grid}><FileField fieldKey="licenseFile" upload={upload} uploading={uploading} uploaded={attachments.includes('licenseFile')} />{boolAt(payload,'experience.hasRecentInKindProject')===true&&<FileField fieldKey="previousProjectEvidence" upload={upload} uploading={uploading} uploaded={attachments.includes('previousProjectEvidence')} />}{boolAt(payload,'finance.hasSpendingPolicy')===true&&<FileField fieldKey="spendingPolicyFile" upload={upload} uploading={uploading} uploaded={attachments.includes('spendingPolicyFile')} />}{boolAt(payload,'planning.hasStrategicPlan')===true&&<FileField fieldKey="strategicPlanFile" upload={upload} uploading={uploading} uploaded={attachments.includes('strategicPlanFile')} />}{boolAt(payload,'planning.hasOperationalPlan')===true&&<FileField fieldKey="operationalPlanFile" upload={upload} uploading={uploading} uploaded={attachments.includes('operationalPlanFile')} />}</div></Section><Section title="الإقرارات"><div className={styles.checks}>{acknowledgements.map(([key,label])=><CheckField key={key} label={label} checked={boolAt(payload,`acknowledgements.${key}`)===true} onChange={(v)=>setValue(`acknowledgements.${key}`,v)} />)}</div></Section><Section title="مراجعة الطلب" description="راجع الأقسام قبل الإرسال. اكتمال جميع الحقول والمرفقات المطلوبة = 100%."><div className={styles.review}>{STEP_LABELS.slice(0,6).map((label,index)=><article key={label}><strong>{index+1}. {label}</strong><button className={styles.buttonSecondary} onClick={()=>onEdit(index+1)}>تعديل</button></article>)}</div></Section></>; }

function Section({ title, description, children }:{title:string;description?:string;children:React.ReactNode}) { return <section className={styles.section}><h2>{title}</h2>{description&&<p>{description}</p>}{children}</section>; }
function Field({ label, hint, children }:{label:string;hint?:string;children:React.ReactNode}) { return <label className={styles.field}><span>{label}</span>{hint&&<small>{hint}</small>}{children}</label>; }
function TextField({label,value,onChange,type='text',dir}:{label:string;value:string;onChange:(v:string)=>void;type?:string;dir?:'ltr'|'rtl'}) { return <Field label={label}><input className={styles.input} type={type} dir={dir} value={value} onChange={(e)=>onChange(e.target.value)} /></Field>; }
function SelectField({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}) { return <Field label={label}><select className={styles.input} value={value} onChange={(e)=>onChange(e.target.value)}><option value="">— اختر —</option>{options.map((option)=><option key={option} value={option}>{option}</option>)}</select></Field>; }
function SearchableSelect({label,value,items,onChange,disabled=false,allowEmpty=false,emptyLabel='— اختر —'}:{label:string;value:string;items:GeographicUnit[];onChange:(v:string)=>void;disabled?:boolean;allowEmpty?:boolean;emptyLabel?:string}) { const [search,setSearch]=useState(''); const filtered=useMemo(()=>items.filter((item)=>!search||item.nameAr.includes(search)||item.officialCode.includes(search)).slice(0,80),[items,search]); return <Field label={label}><input className={styles.input} value={search} disabled={disabled} placeholder="ابحث بالاسم أو الرمز" onChange={(e)=>setSearch(e.target.value)} /><select className={styles.input} value={value} disabled={disabled} onChange={(e)=>onChange(e.target.value)}><option value="">{allowEmpty?emptyLabel:'— اختر —'}</option>{filtered.map((item)=><option key={item.officialCode} value={item.officialCode}>{item.nameAr}</option>)}</select></Field>; }
function PhoneField({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}) { return <Field label={label} hint="أدخل 9 أرقام تبدأ بالرقم 5"><div className={styles.phone}><input className={styles.input} inputMode="numeric" dir="ltr" maxLength={9} value={normalizePhoneForInput(value)} onChange={(e)=>onChange(normalizePhoneForInput(e.target.value))} /><span>+966</span></div></Field>; }
function NumberField({label,value,onChange}:{label:string;value:number;onChange:(v:number)=>void}) { return <Field label={label}><input className={styles.input} type="number" inputMode="numeric" min="0" value={Number.isFinite(value)?value:0} onChange={(e)=>onChange(Math.max(0,Number(e.target.value)))} /></Field>; }
function CounterField({label,value,onChange}:{label:string;value:number;onChange:(v:number)=>void}) { return <Field label={label}><div className={styles.counter}><button type="button" onClick={()=>onChange(Math.max(0,value-1))}>−</button><input className={styles.input} type="number" min="0" value={value} onChange={(e)=>onChange(Math.max(0,Number(e.target.value)))} /><button type="button" onClick={()=>onChange(value+1)}>+</button></div></Field>; }
function MoneyField({label,value,onChange}:{label:string;value:number;onChange:(v:number)=>void}) { return <Field label={label}><input className={styles.input} type="number" inputMode="decimal" min="0" step="0.01" value={value} onChange={(e)=>onChange(Math.max(0,Number(e.target.value)))} /></Field>; }
function YesNo({label,value,onChange}:{label:string;value:boolean|undefined;onChange:(v:boolean)=>void}) { return <Field label={label}><div className={styles.yesNo}><button type="button" className={value===true?styles.selected:''} aria-pressed={value===true} onClick={()=>onChange(true)}>نعم</button><button type="button" className={value===false?styles.selected:''} aria-pressed={value===false} onClick={()=>onChange(false)}>لا</button></div></Field>; }
function CheckField({label,checked,onChange}:{label:string;checked:boolean;onChange:(v:boolean)=>void}) { return <label className={styles.check}><input type="checkbox" checked={checked} onChange={(e)=>onChange(e.target.checked)} /><span>{label}</span></label>; }
function FileField({fieldKey,upload,uploading,uploaded,accept='.pdf,image/jpeg,image/png,image/webp'}:{fieldKey:string;upload:(key:string,file:File|null)=>Promise<void>;uploading:string;uploaded:boolean;accept?:string}) { const hint=accept==='.xlsx'?'ملف XLSX، بحد أقصى 8 ميجابايت':'PDF أو صورة، بحد أقصى 8 ميجابايت'; return <Field label={ATTACHMENT_LABELS[fieldKey]??fieldKey} hint={hint}><input className={styles.input} type="file" accept={accept} disabled={uploading===fieldKey} onChange={(e)=>void upload(fieldKey,e.target.files?.[0]??null)} />{uploading===fieldKey&&<small>جارٍ الرفع…</small>}{uploaded&&<small className={styles.success}>تم رفع الملف وحفظه بأمان.</small>}</Field>; }

function validateStep(step:number,payload:Payload,attachments:string[]):string { const required:Record<number,string[]>={1:['organization.name','organization.licenseNumber','organization.licenseExpiryDate','organization.category','organization.sector','organization.officialEmail','organization.officialPhone','location.regionCode','location.governorateCode','location.serviceScope','coordinator.name','coordinator.title','coordinator.phone','coordinator.email','covenantRepresentative.name','covenantRepresentative.title'],2:['executive.name','executive.phone','executive.education','executive.experienceYears','team.fullTime','team.partTime','team.activeVolunteers','team.nonSaudis','team.universityOrHigher','socialResearcher.exists','readiness.fieldTeamCount','readiness.weeklyDeliveryCapacity','readiness.hasReceiptStorage','readiness.canDocumentDigitally'],3:['beneficiaries.registeredFamilies','beneficiaries.databaseUpdatedAt','beneficiaries.hasSystem','beneficiaries.classifiesNeed','beneficiaries.hasCaseStudyMechanism'],4:['experience.hasRecentInKindProject','experience.recentProjectsCount','experience.recentBeneficiariesCount','experience.ehsanSupportCount2025','experience.hasPreviousSimilarSupport'],5:['finance.hasAccountingSystem','finance.hasSpendingPolicy','finance.revenue','finance.expenses','finance.currentAssets','finance.currentLiabilities'],6:['planning.hasStrategicPlan','planning.hasOperationalPlan','planning.hasPostAidFollowUp','planning.measuresSatisfaction','planning.lastYearProgramsCount','planning.lastYearBeneficiariesCount']}; const missing=(required[step]??[]).find((path)=>{const value=getAt(payload,path);return value===undefined||value===null||value===''}); if(missing)return 'أكمل جميع الحقول المطلوبة في هذه الخطوة قبل المتابعة.'; if(step===1&&['organization.officialPhone','coordinator.phone'].some((path)=>!/^5\d{8}$/.test(textAt(payload,path))))return 'تحقق من أرقام الجوال: يجب إدخال 9 أرقام تبدأ بالرقم 5.'; if(step===7){if(!attachments.includes('licenseFile'))return 'ملف الترخيص مطلوب قبل الإرسال.'; const acks=['dataAccuracy','verificationPermission','auditConsent','coordinatorCommitment','covenantCommitment','beneficiaryListPreliminary','participationTerms']; if(acks.some((key)=>boolAt(payload,`acknowledgements.${key}`)!==true))return 'يجب الموافقة على جميع الإقرارات قبل إرسال الطلب.';} return ''; }
function getAt(root:Payload,path:string):unknown { let value:unknown=root; for(const key of path.split('.')){if(!value||typeof value!=='object'||Array.isArray(value))return undefined; value=(value as Payload)[key];} return value; }
function setAt(root:Payload,path:string,value:unknown):Payload { const keys=path.split('.'); const clone=structuredClone(root); let current:Payload=clone; keys.forEach((key,index)=>{if(index===keys.length-1)current[key]=value; else {if(!current[key]||typeof current[key]!=='object'||Array.isArray(current[key]))current[key]={}; current=current[key] as Payload;}}); return clone; }
function removeAt(root:Payload,path:string):Payload { const keys=path.split('.'); const clone=structuredClone(root); let current:Payload=clone; for(let i=0;i<keys.length-1;i++){const next=current[keys[i]];if(!next||typeof next!=='object'||Array.isArray(next))return clone;current=next as Payload;} delete current[keys.at(-1)!]; return clone; }
function textAt(root:Payload,path:string):string { const value=getAt(root,path); return typeof value==='string'?value:''; }
function numberAt(root:Payload,path:string):number { const value=Number(getAt(root,path)); return Number.isFinite(value)?value:0; }
function boolAt(root:Payload,path:string):boolean|undefined { const value=getAt(root,path); return typeof value==='boolean'?value:undefined; }
function normalizePhoneForInput(value:string):string { let digits=value.replace(/\D/g,''); if(digits.startsWith('966'))digits=digits.slice(3); if(digits.startsWith('0'))digits=digits.slice(1); return digits.slice(0,9); }
function formatMoney(value:number):string { return new Intl.NumberFormat('ar-SA',{style:'currency',currency:'SAR',maximumFractionDigits:2}).format(Number.isFinite(value)?value:0); }
function readError(reason:unknown):string { return reason instanceof Error?reason.message:'تعذّر إكمال العملية. حاول مرة أخرى.'; }
