# مراجعة أمنية برمجية ومحاكاة — المرحلة الثامنة

**تنبيه صادق وصريح، كما في كل مراجعة سابقة في هذا المشروع**: هذه **مراجعة
أمنية برمجية آلية ومحاكاة اختبارات داخل Node.js `vm`**، وليست اختبار اختراق
(Penetration Test) مستقلًا ولا معتمَدًا. لا تغطي الشبكة، البنية التحتية،
إعدادات النشر الفعلية على Apps Script، صلاحيات Google Workspace، ولا سلوك
Google Sheets/Drive الحقيقي. راجع `RELEASE.md`/`DEPLOYMENT.md` للبنود التي
لا تزال تحتاج اختبارًا حيًا.

آخر تحديث: مُحدَّثة بعد **المرحلة التاسعة** (إعادة الهندسة بناءً على
الاختبار الحي 2026/08/01) بالفرع `claude/code-index-review-kz5k4u`.

## 0. ملحق المرحلة التاسعة: الأسطح الجديدة ومراجعتها

أُضيفت ثلاث دوال عامة وحقل استجابة واحد، وكلها مراجَعة ومختبَرة في
`tools/security-test.js` القسم 13:

| السطح الجديد | الحراسة | ما جرى التحقق منه |
|---|---|---|
| `getPortalBundle(token, page, options)` | `requireSession_` + قائمة أدوار لكل قسم (`PORTAL_PAGE_LOADERS_`) | لا توسّع أي صلاحية: جمعية تطلب `associations`/`applications` تحصل على حزمة بلا `pageData` إطلاقًا؛ وتمرير `associationId` لجمعية أخرى لا يُسرِّب صفًا واحدًا (مؤكَّد بفحص أن معرّف الجمعية الأخرى غير موجود في الحمولة كلها) |
| `retryDelivery(token, id, opId)` | `requireSession_(['DELEGATE','ADMIN','ASSOCIATION'])` + فحص ملكية صريح لكل دور | مندوب آخر يُرفض («المستفيد غير متاح لك»)، وجمعية أخرى تُرفض («ليس لديك صلاحية»)؛ لا يُعدِّل الأجهزة ولا المندوب |
| `updateDeliveryStatus(..., opId)` | كما كانت + `withIdempotency_` | النقر المكرر لا يُنشئ محاولتين |
| `_meta` في كل استجابة | — | يقتصر حرفيًا على `{traceId, op, serverMs, reads, writes}`؛ لا رمز جلسة ولا كلمة مرور ولا تجزئة ولا ملح (مؤكَّد بفحص المفاتيح والمحتوى معًا) |

**رفض المعرّف المكرَّر**: رسالة `duplicateIdMessage_` تسمّي الورقة والمعرّف
وعدد الصفوف فقط — لا تكشف أي محتوى صف (اسم/جوال/بريد)، وهذا مُختبَر صراحةً.

**المصادر المرجعية المضمَّنة**: تفعيلها افتراضيًا يزيد صرامة التحقق في
الخادم (قيم خارج القائمة تُرفض بدل قبولها كنص حر). الاستثناء الوحيد
`isGrandfatheredValue_` مقيَّد بأن تكون القيمة **مطابقة حرفيًا للمخزَّن في
السجل نفسه**، فلا يفتح بابًا لتمرير أي نص بادعاء أنه قديم — مُختبَر.

**ما لم يتغيّر**: كل ما ورد في الأقسام 1–12 أدناه يبقى ساريًا (قفل دوال
الصيانة خلف رمز وصول مؤقت، عزل الجمعيات، فحص توقيع بايتات الصور، منع
الحقن، عدم تسريب الأسرار في السجل أو الاستجابات).

---

## 1. جرد سطح النظام: كل دالة Apps Script علوية (غير المنتهية بـ `_`)

اصطلاح هذا المشروع: أي دالة منتهية بـ `_` هي دالة خاصة داخلية، `verify.js`
يتحقق آليًا أن لا شيء في `Index.html` يستدعيها مباشرة. كل ما تبقى (بلا `_`
في النهاية) هو **سطح استدعاء عام فعليًا** — أي دالة top-level بلا شرطة سفلية
تُصبح تلقائيًا قابلة للاستدعاء عبر `google.script.run` من أي صفحة يخدمها
`doGet()` بمجرد نشر المشروع كتطبيق ويب، **بصرف النظر عمّا إذا كان
`Index.html` نفسه يستدعيها فعليًا أم لا**. هذا خاصية معمارية أصيلة في Apps
Script (لا استيراد/تصدير حقيقي، كل شيء علوي غير خاص = سطح عام)، وليست خللًا
أُدخل في هذا المشروع.

### 1.1 دوال العميل (تُستدعى فعليًا من `Index.html` عبر `api()`)

| الدالة | الدور المسموح | تتطلب جلسة | نطاق الملكية | يكتب في Sheets/Drive | rate limit/lock/opId |
|---|---|---|---|---|---|
| `login(payload)` | عام (مقصود) | لا | — | لا (قراءة فقط + كتابة "آخر دخول") | `throttle_` 8/900ث لكل بريد/رمز |
| `logout(token)` | أي جلسة صالحة | نعم (يسمح بحساب مُلزَم بتغيير كلمة مرور) | نفسه فقط | لا | — |
| `submitAssociationApplication(payload)` | عام (مقصود) | لا | — | نعم (طلبات انضمام الجمعيات) | `throttle_` 5/3600ث لكل بريد |
| `getBootstrapData(token, forceFresh)` | أي دور مسجَّل | نعم | نطاق دوره (جمعيته/نفسه) | لا | ذاكرة مؤقتة بجيل مشترك |
| `getReferenceData(token)` | عام إن بلا token، وإلا يتحقق الجلسة | اختياري | — (بيانات تصنيفية عامة غير حساسة) | لا | ذاكرة مؤقتة |
| `listBeneficiaries(token, options)` | ADMIN/ASSOCIATION | نعم | الجمعية مفروضة خادميًا قبل أي بحث | لا | — |
| `saveBeneficiary(token, payload)` | ADMIN/ASSOCIATION | نعم | تحقق تطابق الجمعية عند التعديل | نعم (المستفيدون) | `LockService` لمنع تكرار التزامن |
| `importBeneficiaries(token, rows, acceptedPledge)` | ADMIN/ASSOCIATION | نعم | كل صف يُفرَض عليه جمعية الطالب (ASSOCIATION) | نعم (المستفيدون، دفعة) | `LockService` قبل الكتابة الفعلية |
| `inspectBeneficiaryExcel(token, payload)` | ADMIN/ASSOCIATION | نعم | يُفرَض associationId للجمعية | لا (يحذف ملف Drive المؤقت فورًا) | حد حجم 8MB |
| `downloadBeneficiaryImportTemplateXlsx(token)` | ADMIN/ASSOCIATION | نعم | — | لا | — |
| `assignDelegate(token, beneficiaryId, delegateId)` | ADMIN/ASSOCIATION | نعم | تحقق أن المستفيد/المندوب لنفس الجمعية، وجمعية الطالب | نعم (المستفيدون+الأجهزة) | `LockService` |
| `listDevices(token, options)` | ADMIN/ASSOCIATION | نعم | الجمعية مفروضة خادميًا | لا | — |
| `getDeviceDetail(token, deviceId)` | ADMIN/ASSOCIATION | نعم | تحقق صريح لجمعية الجهاز مقابل جمعية الطالب | لا | — |
| `saveDevice(token, payload)` | **ADMIN فقط** | نعم | — | نعم (الأجهزة) | — |
| `listAssociations(token, options)` | **ADMIN فقط** | نعم | — | لا | — |
| `saveAssociation(token, payload)` | **ADMIN فقط** | نعم | — | نعم (الجمعيات+حساب دخول) | `withIdempotency_(opId)` عند الإنشاء |
| `resetAssociationPassword(token, associationId)` | **ADMIN فقط** | نعم | — | نعم (كلمة مرور) | يُبطل كل جلسات الجمعية فورًا |
| `listDelegates(token, options)` | ADMIN/ASSOCIATION | نعم | الجمعية مفروضة خادميًا | لا | — |
| `saveDelegate(token, payload)` | ADMIN/ASSOCIATION | نعم | تحقق تطابق الجمعية عند التعديل | نعم (المناديب) | `withIdempotency_(opId)` عند الإنشاء |
| `regenerateDelegateCode(token, delegateId)` | ADMIN/ASSOCIATION | نعم | تحقق تطابق الجمعية | نعم (رمز جديد) | يُبطل الرمز القديم وجلساته فورًا |
| `setDelegateStatus(token, delegateId, status)` | ADMIN/ASSOCIATION | نعم | تحقق تطابق الجمعية | نعم (الحالة) | يُبطل الجلسات عند التعطيل |
| `updateDeliveryStatus(token, beneficiaryId, reason, notes)` | **DELEGATE فقط** | نعم | **يتحقق أن `رقم المندوب` على سجل المستفيد = المندوب الحالي فعليًا** | نعم (المستفيدون+سجل تسليمات) | يمر عبر `assertDeliveryTransition_` |
| `confirmDelivery(token, payload)` | **DELEGATE فقط** | نعم | نفس تحقق الملكية أعلاه + رفع صورة | نعم (المستفيدون+الأجهزة+Drive) | `LockService` + `withIdempotency_(opId)` |
| `listApplications(token, options)` | **ADMIN فقط** | نعم | — | لا | — |
| `listAssociationApplications(token)` | **ADMIN فقط** | نعم | — | لا | — |
| `reviewAssociationApplication(token, id, decision, reason, opId)` | **ADMIN فقط** | نعم | — | نعم (الطلب+جمعية جديدة+حساب) | `withIdempotency_(opId)` |
| `getActivitiesBundle(token)` | **ADMIN فقط** | نعم | — | لا | — |
| `saveActivity(token, payload)` | **ADMIN فقط** | نعم | — | نعم (إدارة الأنشطة) | — |
| `updateAssociationSettings(token, payload)` | **ASSOCIATION فقط** | نعم | نفسها فقط (`user.associationId`) | نعم (الجمعيات) | — |
| `changePassword(token, currentPassword, newPassword)` | ADMIN/ASSOCIATION | نعم (يسمح بحساب مُلزَم بتغيير كلمة مرور) | نفسه فقط | نعم (كلمة المرور) | يمنع إعادة استخدام آخر كلمة مرور، يُبطل الجلسات القديمة |
| `listAuditLog(token, options)` | ADMIN/ASSOCIATION | نعم | الجمعية مفروضة خادميًا | لا | — |
| `listDelegateAuditLog(token, delegateId, options)` | ADMIN/ASSOCIATION/DELEGATE | نعم | ADMIN: أي مندوب. ASSOCIATION: مندوبوها فقط. DELEGATE: نفسه فقط، وأحداث تشغيلية فقط | لا | — |

**كل الدوال الـ25 أعلاه تمر عبر `requireSession_` (أو تحقق مكافئ صريح)
باستثناء الاستثناءين المقصودين الموثَّقين صراحة**: `login`/`submitAssociationApplication`
(نقطتا الدخول العامتان الوحيدتان، محميتان بـ`throttle_`)، و`getReferenceData`
حين لا يُمرَّر `token` (بيانات تصنيفية عامة غير حساسة فقط — لا بيانات
مستفيدين ولا جمعيات ولا مناديب).

### 1.2 دوال الصيانة (Ops) — **خاصة (`_`)، ومقفلة افتراضيًا خلف رمز وصول صيانة مؤقت**

**تحديث (تصحيح أمني لاحق لهذه المرحلة)**: القسم كان يوثّق فجوة مؤكَّدة هنا
(دوال صيانة بلا أي تحقق جلسة، قابلة للاستدعاء عبر `google.script.run` من
أي زائر). **عولجت الفجوة فعليًا الآن**، لا تُركت موثَّقة فقط. كل دالة صيانة
أُعيدت تسميتها لتنتهي بشرطة سفلية (خاصة حسب اصطلاح هذا المشروع، يفرضه
`tools/verify.js` بمنع استدعائها من `Index.html`)، **و**أصبحت تتطلب رمز
وصول صيانة صالح كأول معاملاتها — دفاع مزدوج مستقل، لا طبقة واحدة.

| الدالة | تكتب؟ | الحماية الآن |
|---|---|---|
| `doGet()` | لا | نقطة الدخول العامة المقصودة — تخدم صفحة HTML فقط، لم تتغيّر |
| `grantMaintenanceAccess_(minutes)` | لا (خصائص سكربت فقط) | خاصة، تُشغَّل من المحرر فقط، توليد رمز عشوائي بالكامل |
| `revokeMaintenanceAccess_()` | لا | خاصة، تُشغَّل من المحرر فقط |
| `requireMaintenanceAccess_(token)` | لا (حارس مشترك) | خاصة، تُستدعى داخليًا من كل دالة أدناه |
| `setupSheets_(token)` | **نعم** (أوراق/أعمدة/إعدادات/أول حساب مدير) | خاصة + يتطلب رمز وصول صيانة صالح |
| `migrateReferenceData_(token)` | **نعم** (بذرة البيانات المرجعية) | خاصة + يتطلب رمز وصول صيانة صالح |
| `diagnoseReferenceDataIssues_(token)` | لا | خاصة + يتطلب رمز وصول صيانة صالح (رغم كونها قراءة فقط — لا تُسرِّب لمستخدم عام) |
| `migrateLegacyReferenceValues_(token, dryRun)` | نعم إن `dryRun=false` | خاصة + يتطلب رمز وصول صيانة صالح |
| `previewPhoneNormalization_(token)` | لا | خاصة + يتطلب رمز وصول صيانة صالح |
| `migratePhoneNumbers_(token)` | **نعم** | خاصة + يتطلب رمز وصول صيانة صالح |
| `diagnoseStateIntegrity_(token)` | لا | خاصة + يتطلب رمز وصول صيانة صالح |
| `repairStateIntegrityIssues_(token)` | **نعم** (الحالات الآمنة فقط) | خاصة + يتطلب رمز وصول صيانة صالح |
| `preflightRelease_(token)` | لا | خاصة + يتطلب رمز وصول صيانة صالح |
| `applyReleaseSchema_(token, options)` | نعم، إضافي فقط | خاصة + يتطلب رمز وصول صيانة صالح (لا `approvalCode` ثابت في الكود بعد الآن) |

## 2. ✅ خلل معماري كان مؤكَّدًا — عولج فعليًا (كان موثَّقًا هنا سابقًا دون معالجة تلقائية)

**السياق السابق (لمن يراجع تاريخ هذا الملف)**: جميع دوال القسم 1.2 كانت
بلا أي `requireSession_` أو تحقق دور أو ملكية إطلاقًا، لأن Apps Script
تُعرِّض تلقائيًا **كل دالة top-level غير خاصة** عبر `google.script.run`
بمجرد نشر `doGet()` كتطبيق ويب — بصرف النظر عمّا إذا كانت واجهة
`Index.html` تستدعيها أم لا. كان أي زائر يفتح رابط التطبيق ويستخدم أدوات
المطوّر (Console) في متصفحه يستطيع نظريًا كتابة
`google.script.run.setupSheets()` مباشرة وتنفيذها بلا أي رمز دخول.

**المعالجة الفعلية المطبَّقة الآن (لا اقتراح فقط)**:
1. **إعادة تسمية جميع دوال الصيانة العشر لتنتهي بشرطة سفلية** (`_`) — طبقة
   حماية أولى بالاصطلاح المفروض آليًا في هذا المشروع.
2. **حارس وصول مشترك جديد (`requireMaintenanceAccess_`)** يستدعيه كل واحدة
   منها كأول سطر فيها — لا يعمل أي منها بلا رمز صالح، **حتى لو استُدعيت من
   محرر Apps Script نفسه**. الرمز:
   - يولَّد عشوائيًا بالكامل عبر `grantMaintenanceAccess_()` (تُشغَّل من
     المحرر فقط، لا تتطلب جلسة ويب، خاصة هي الأخرى).
   - يُخزَّن **مُجزَّأً فقط** (`hashSecret_` + ملح فريد) في Script
     Properties — لا الرمز الخام أبدًا.
   - يُطبَع مرة واحدة فقط في سجل تنفيذ المحرر (القناة الوحيدة لرؤيته) —
     لا يُعاد في أي قيمة راجعة يمكن أن تصل لعميل ويب.
   - مؤقت (20 دقيقة افتراضيًا، حتى 60 كحد أقصى)، صالح للاستخدام المتكرر
     خلال هذه النافذة (لا أحادي الاستخدام — يخفّف عبء إعادة المنح لكل
     خطوة ضمن جلسة صيانة واحدة متصلة)، ويُقفَل تلقائيًا بعد 5 محاولات
     تخمين فاشلة بصرف النظر عن الوقت المتبقي.
   - لا يُسجَّل الرمز نفسه في أي مكان (لا `audit_`، لا سجل عمليات، لا أي
     استجابة).
3. **لا رمز موافقة ثابت في الكود إطلاقًا** — الثابت القديم
   `RELEASE_SCHEMA_APPROVAL_CODE_` (نص عربي حرفي) أُزيل بالكامل من
   `applyReleaseSchema_`، واستُبدل بنفس آلية `requireMaintenanceAccess_`
   الموحَّدة.
4. **لا دالة واحدة تشغّل كل الترحيلات/الإصلاحات تلقائيًا** — فحص المخطط
   (`preflightRelease_`) منفصل عن إضافته (`applyReleaseSchema_`) منفصل
   عن ترحيل المرجعيات (`migrateReferenceData_`/`migrateLegacyReferenceValues_`)
   منفصل عن تشخيص الحالات (`diagnoseStateIntegrity_`) منفصل عن إصلاحها
   (`repairStateIntegrityIssues_`) — لم يتغيّر هذا الفصل، فقط أُضيف حاجز
   الوصول فوق كل واحدة منها بشكل مستقل.

**تحقّق آلي فعلي لهذه المعالجة** (لا ادّعاء فقط): `tools/security-test.js`
القسمان 11–12 يحاكيان استدعاء كل دالة صيانة كما تفعل `google.script.run`
تمامًا (بلا رمز، وبرمز مزوَّر عشوائي) ويتأكدان من الرفض في الحالتين لكل
دالة من العشر، ويفحصان كامل ملفات `.gs` للتأكد من عدم وجود أي رمز موافقة
أو سرّ ثابت مكتوب حرفيًا في أي مكان.

**ما تبقى، بصراحة**: هذا حاجز برمجي مُختبَر داخل محاكاة Node.js فقط — لم
يُختبَر ضد Apps Script حي (نفس قيد كل هذا المستند). المخاطرة المتبقية
الوحيدة: إن نسي المُشغِّل حذف دالة مؤقتة تحتوي الرمز الخام قبل `git push`
(راجع تحذير `DEPLOYMENT.md` القسم 5) — هذا إجراء بشري خارج ما يمكن لأي
كود فرضه آليًا.

## 3. مراجعة الصلاحيات وIDOR — النتائج

كل ما يلي **مختبَر آليًا فعليًا** (وليس افتراضًا) عبر `security-test.js`
(91 اختبارًا) و`server-test.js` (196 اختبارًا، خصوصًا الأقسام 18–21 المضافة
هذه المرحلة وسابقتها) و`state-test.js`/`account-test.js`/`integration-test.js`:

- **IDOR بين جمعيتين**: مستفيدون، أجهزة، مناديب، سجل عمليات — جمعية A لا
  تستطيع رؤية أو تعديل أو تعيين مندوب لأي سجل يخص جمعية B، حتى بتمرير
  `associationId`/`recordId` يدويًا (`server-test.js` قسم 3، وأقسام 18/20
  الجديدة لتفاصيل الجهاز وسجل المندوب تحديدًا).
- **IDOR بين مندوبين**: مندوب A لا يستطيع تحديث حالة تسليم مستفيد غير مسند
  إليه (`updateDeliveryStatus`/`confirmDelivery` يتحققان `رقم المندوب`
  صراحة)، ولا الاطلاع على سجل عمليات مندوب آخر (`listDelegateAuditLog`،
  قسم 20 الجديد).
- **الجمعية لا تستطيع إضافة أو حذف أجهزة**: `saveDevice` مقصور على ADMIN
  فقط؛ **لا توجد أي دالة حذف جهاز في كامل المشروع** لأي دور (لا حذف بيانات
  إطلاقًا — يتوافق مع "ممنوع حذف بيانات أو وظائف قائمة").
- **حساب معطَّل/جلسة قديمة**: `assertActorEnabled_` يمنع أي فاعل تابع
  لجمعية موقوفة من إكمال أي استدعاء؛ `revokeSessions_`/`actorEpoch_`
  يُبطلان فورًا كل الجلسات المفتوحة عند تعطيل مندوب أو إعادة تعيين كلمة
  مرور جمعية (مختبَر في `account-test.js`).
- **فرض تغيير كلمة المرور مطبَّق خادميًا فعليًا**: `requireSession_` يرفض
  أي استدعاء آخر غير `changePassword`/`logout` طالما الجلسة تحمل وسم
  `mustChangePassword` — ليس إخفاءً في الواجهة فقط (`server-test.js`
  "الحساب المُلزَم بتغيير كلمة المرور لا يستطيع استدعاء دالة أخرى").

## 4. المصادقة والأسرار

- كلمات المرور المؤقتة/رموز المناديب: `createAccessCode_`/`generateStrongTempPassword_`
  عبر `Utilities.getUuid()` (عشوائية مشفَّرة من Google)، تُخزَّن **مُجزَّأة
  فقط** (`hashSecret_` + ملح فريد لكل سجل)، لا نص صريح إطلاقًا.
- الأسرار لا تظهر في سجل العمليات (`audit_` لا يمرر كلمة مرور/رمز في أي
  `notes`)، ولا في استجابات الخادم بعد أول توليد (تُعاد مرة واحدة فقط عند
  الإنشاء)، ولا في `console`/أخطاء الواجهة — مختبَر صراحة في
  `security-test.js` القسم 8.
- `constantTimeEquals_` تُستخدَم لمقارنة الأسرار (تقاوم هجمات التوقيت
  قدر الإمكان ضمن حدود Apps Script — راجع `Validation.gs`).
- `throttle_` مطبَّق على: الدخول (8/900ث)، تقديم طلب جمعية (5/3600ث). راجع
  `security-test.js` القسم 7 وaccount-test.js لإعادة تعيين كلمة مرور/رمز
  المندوب (تُبطل الجلسات القديمة فورًا عوضًا عن تحديد معدّل إضافي، فالتبديل
  نفسه يُنهي أي محاولة استغلال للرمز القديم فورًا).

## 5. المدخلات والمخرجات

- **XSS**: `esc()` تُستخدَم لكل إدراج نصي في HTML؛ `security-test.js`
  القسم 4 يختبر كتابة/قراءة فعلية لحقول تحتوي `<script>`/`onerror=`/إلخ في
  الاسم/العنوان/العلامة المميزة/الملاحظات، ويتحقق أن `esc()` تُبطل الخطر.
- **حقن صيغ Sheets/CSV**: قيم تبدأ بـ`=`/`+`/`-`/`@` تُعامَل كنص عادي عبر
  `safeCell_` عند الكتابة في Sheets (بادئة حماية)، ومختبَرة في قسم 4.
  ملف `.xlsx` الجديد (المرحلة السابعة) **لا يحمل خطر حقن صيغ إطلاقًا
  بنيويًا**: كل الخلايا النصية تُكتب بنوع `t="inlineStr"` (نص خام لا صيغة)
  عدا صف مثال بأرقام حقيقية فقط — لا خلية فيها تُقيَّم كصيغة أبدًا مهما كان
  محتوى الاسم/الملاحظات.
- **قيم طويلة/خاطئة/null/undefined**: `requiredText_`/`cleanText_`
  تقصّان الطول الأقصى بدل الفشل؛ `boundedNumber_`/`optionalCoordinate_`
  يرفضان أنواعًا وقيمًا خارج النطاق برسالة واضحة (قسم 5).
- **لا كشف تفاصيل داخلية**: كل الأخطاء المُرسَلة للعميل رسائل عربية معدَّة
  يدويًا (`throw new Error('...')`) — لا `error.stack` ولا اسم ملف داخلي
  يصل للعميل عبر `withFailureHandler` (يُعاد `error.message` فقط في
  `Index.html`، راجع `api()`).

## 6. إثبات التسليم والملفات — تحديث هذه المرحلة

**اكتشاف مؤكَّد وعولج فعليًا**: `saveProofImage_` كانت تتحقق فقط من بادئة
`data:image/(jpeg|png|webp);base64,` **المُعلَنة من العميل نفسه** — أي عميل
يستطيع تسمية أي محتوى (نص، HTML) بهذه البادئة والمرور من التحقق. أُضيفت
`verifyImageMagicBytes_()` (`Delegates.gs`) التي تتحقق من **التوقيع الفعلي
لأول بايتات الملف بعد فك base64** (JPEG: `FF D8 FF`، PNG: `89 50 4E 47 0D
0A 1A 0A`، WEBP: `RIFF....WEBP`) وترفض أي محتوى لا يطابق توقيعه المُعلَن.
مختبَر في `security-test.js` القسم 6 (رفض نص/HTML مموَّه، وقبول PNG حقيقي).

باقي الضوابط القائمة (لم تتغيّر، تحققت من استمرارها):
- امتدادات مقبولة: JPG/PNG/WEBP فقط (SVG/HTML/أي تنفيذي يُرفض صراحة).
- حجم أقصى 6 ميجابايت قبل أي رفع فعلي لـ Drive.
- اسم ملف آمن: `beneficiaryId + تاريخ/وقت + امتداد` (لا اسم مُدخَل من
  العميل، فلا مجال لـ path traversal أو أسماء ملفات خبيثة).
- لا كتابة خارج مجلد الإثباتات: `proofFolder_()` يُنشئ/يستخدم مجلد Drive
  واحد ثابت مسجَّلًا بمعرّفه في Script Properties (`PROOF_FOLDER_ID`) —
  كل ملف يُكتب داخله عبر `folder.createFile()` فقط.
- **المندوب لا يستطيع رفع إثبات لمستفيد غير مسند إليه**: `confirmDelivery_`
  يتحقق `رقم المندوب` قبل استدعاء `saveProofImage_` أصلًا.
- **منع التسليم المكرر/تكرار الرفع**: `withIdempotency_(user.id, payload.opId)`
  يمنع تكرار نفس عملية التأكيد عند إعادة إرسال الشبكة، و`LockService`
  داخل `confirmDelivery_` يعيد فحص الانتقال قبل الكتابة الفعلية، و
  `assertDeliveryTransition_` يرفض تأكيد تسليم مكتمل بالفعل (لا حلقة ذاتية
  للحالة النهائية).
- **صلاحيات مجلد Drive**: يرثها المجلد من مالك السكربت (منشئ المشروع) —
  **لم يُضبَط أي مشاركة عامة برمجيًا في أي مكان بالكود**؛ الوصول الفعلي
  للصور يعتمد على من يملك صلاحية الوصول لحساب Google المالك للمشروع نفسه
  أو من يُشارَك معه المجلد يدويًا لاحقًا من واجهة Drive — **هذا إعداد يدوي
  خارج الكود، يحتاج قرارك وتوثيقك الخاص عند التشغيل الحي** (راجع
  `DEPLOYMENT.md`).
- **أبعاد/محتوى الملف بخلاف التوقيع**: لم يُتحقَّق من أبعاد الصورة الفعلية
  (عرض/ارتفاع) أو فحص محتوى بصري (مثل كشف صور فارغة/سوداء بالكامل) — خارج
  نطاق ما يمكن التحقق منه بأدوات Apps Script القياسية بلا مكتبة معالجة
  صور خارجية (مرفوضة صراحة بشرط "لا خدمة مدفوعة"). يبقى بندًا يحتاج تقييمًا
  بشريًا عند المراجعة اليدوية للصور المرفوعة إن استُخدم لاحقًا.

## 7. الروابط والمكتبات الخارجية

- **Leaflet/OpenStreetMap**: تحميل كسول (lazy) فقط عند فتح نموذج اختيار
  الموقع — لا تُحمَّل في شاشة الدخول أو أي شاشة أخرى. مصدرها CDN عام
  (`unpkg.com`) بلا مفتاح API وبلا خدمة مدفوعة. **خطر تشغيلي موثَّق صراحة**:
  إن تعطّل الـCDN، ميزة اختيار الموقع بصريًا تفشل بأمان (سقوط مصمَّم: زر
  "نسخ الرقم"/إدخال يدوي للإحداثيات يبقى بديلًا ظاهرًا دائمًا بلا انتظار
  فشل الاتصال أولًا — مختبَر في `smoke.js` قسم 3ب). **بلاطات OpenStreetMap
  القياسية (`tile.openstreetmap.org`) تخضع لسياسة استخدام عادل صارمة من
  OSMF وغير معتمدة للاستخدام الإنتاجي المكثّف** — إن نما استخدام النظام،
  يوصى بالانتقال لمزوّد بلاطات معتمَد للإنتاج (مجاني ضمن حدود، مثل حساب
  MapTiler/Stadia Maps المجاني، أو استضافة بلاطات ذاتية) بدل الاستمرار على
  الخادم القياسي العام — **هذا خطر تشغيلي يحتاج قرارك، لم يُعالَج آليًا هنا
  لأنه يتطلب تسجيل حساب خارجي، والتعليمات تمنع إضافة خدمة/مفتاح API جديد
  في هذه الجلسة**.
- **روابط Google Maps/WhatsApp/`tel:`**: كلها روابط مبنية مباشرة (لا CDN،
  لا مفتاح API)، تحمل `target="_blank" rel="noopener noreferrer"` (مؤكَّد
  آليًا هذه المرحلة عبر `smoke.js` قسم 11).
- **Google Fonts**: `fonts.googleapis.com`/`fonts.gstatic.com` — المصدر
  الخارجي الوحيد المُحمَّل في شاشة الدخول نفسها (خط عربي، لا بديل محلي
  مضمَّن حاليًا). خطر تشغيلي بسيط مشابه (تعطّل مؤقت يعني سقوطًا لخط النظام
  الاحتياطي `Tahoma,Arial` المُعلَن في `--font`، لا كسرًا للوظائف).
- لا خدمة مدفوعة ولا مفتاح API في أي مكان بالمشروع — مؤكَّد آليًا
  (`verify.js`/`security-test.js`).

## 8. تنظيم `Index.html` — القرار

راجع `RELEASE.md` القسم 14 و`DEPLOYMENT.md` للتفصيل الكامل. **القرار:
البقاء ملفًا واحدًا موحَّدًا**، لنفس الأسباب الموثَّقة في كل مرحلة سابقة،
مع تقييم عملي جديد لخيار qest التقسيم المقترح (`Styles.html`/`AppShell.html`/
`ClientCore.html`/`AdminPortal.html`/`AssociationPortal.html`/`DelegatePortal.html`)
تحديدًا لهذه المرحلة.

## 9. preflight/dry-run/ترحيل — الحالة

`preflightRelease_(token)` و`applyReleaseSchema_(token, options)` **مُجهَّزتان
بالكامل، مقفلتان خلف رمز وصول صيانة مؤقت (القسم 2)، مختبَرتان آليًا (10
اختبارات في `server-test.js` قسم 21 + 45 اختبارًا في `security-test.js`
الأقسام 11–12)، ولم تُشغَّلا على أي بيانات حية من هذه الجلسة إطلاقًا**.
راجع `DEPLOYMENT.md` للترتيب الموصى به الكامل.

## 10. الخلاصة

لا ثغرة حرجة جديدة اكتُشفت في مسارات المصادقة/الصلاحيات/IDOR للواجهة
المُستخدَمة فعليًا (القسم 1.1) — هذه المسارات مُحكَمة ومختبَرة بعمق منذ
مراحل سابقة وتأكَّدت مجددًا هذه المرحلة. الاكتشاف المؤكَّد الوحيد ذو الأثر
الحقيقي كان **القسم 2** (دوال الإعداد/الترحيل بلا تحقق جلسة) — **عولج
فعليًا الآن** (خاصة بالاسم + رمز وصول صيانة مؤقت مُجزَّأ، لا رمز ثابت في
الكود)، لا تُرك موثَّقًا فقط. بالإضافة إلى معالجة فعلية أخرى (**التحقق من
التوقيع الحقيقي لصور إثبات التسليم**، القسم 6).
