# تقرير تسليم — نظام متابعة توزيع الأجهزة (جمعية الزاد)

آخر تحديث: يعكس هذا الملف حالة الفرع `claude/code-index-review-kz5k4u`
بعد **المرحلة التاسعة: إعادة هندسة شاملة بناءً على نتائج اختبار حي فعلي
بتاريخ 2026/08/01**. مستودع GitHub هو المصدر المعتمد الوحيد.

## المرحلة التاسعة (الأحدث): معالجة نتائج الاختبار الحي 2026/08/01

هذه المرحلة **لم تنطلق من افتراضات**، بل من سبعة عشر بندًا مرصودًا فعليًا
على النسخة المنشورة. ما يلي ما عولج، ولماذا كان يحدث:

### أ) الأداء والهندسة الخلفية

1. **ذاكرة الجداول المؤقتة كانت تعبر حدود الطلب.** `_TABLE_CACHE_` متغيّر
   عام بـTTL زمني (4 ثوانٍ)، وApps Script يُبقي المتغيّرات العامة حيّة بين
   التنفيذات داخل نفس الـwarm isolate وقد يخدم تنفيذات متزامنة من isolates
   متعددة. النتيجة: كتابة نفّذها isolate A لا تُبطل نسخة isolate B، فتعود
   قائمة أحيانًا بحالة ما قبل الكتابة. صارت الذاكرة **محصورة بالطلب
   الواحد** صراحةً عبر `beginRequest_` المستدعاة من `requireSession_` ومن
   نقاط الدخول العامة — بلا اعتماد على وقت ولا على ترتيب isolates.
2. **قياس أداء من الخادم**: كل استجابة تحمل `_meta` فيه `traceId` قصير
   و`serverMs` وعدد قراءات/كتابات الأوراق — بلا أي حقل حساس (مُختبَر).
3. **حزمة بوابة مُجمَّعة** (`getPortalBundle`): جولة `google.script.run`
   واحدة تُعيد بيانات البوابة + المصادر المرجعية + الصفحة المُرقَّمة
   الأولى، بدل ثلاث جولات متسلسلة. كل جولة في Apps Script تحمل تكلفة
   ثابتة كبيرة مستقلة عن حجم البيانات، وهي المكوّن الأكبر في زمن الدخول
   المرصود (7–10 ثوانٍ). **النتيجة المقاسة: جولات الإقلاع 2 → 1 (−50٪)**
   في كل المسارات الثلاثة (`node tools/perf-phase-bench.js`).
4. **رفض المعرّفات المكرَّرة**: `findById_`/`updateById_` تتوقفان بتقرير
   عربي واضح بدل اختيار أول صف بصمت. هذا سبب رسالة «سبق البتّ في هذا
   الطلب» المضلِّلة على `APP-000001` المكرَّر في البيانات الحية.
5. **قرار طلب الانضمام ذرّي**: القبول والرفض معًا تحت قفل + `withIdempotency_`
   (كان الرفض بلا أي منهما، والقبول بلا قفل).

### ب) تكامل البوابات وقواعد الحالات

6. **المصادر المرجعية صارت جاهزة افتراضيًا**: القائمة المعتمدة المضمَّنة
   في `ReferenceData.gs` تُستخدم مباشرة حين تكون الورقة فارغة
   (`ready:true, source:'builtin'`) بدل السقوط الصامت إلى حقول نصية حرة.
   هذا يُفعّل القوائم المنسدلة المترابطة في **كل** النماذج (تقديم
   الجمعية، إضافة/تعديل الجمعية والمستفيد، نوع الجهاز) **بلا أي كتابة في
   الملف الحي ولا أي ترحيل**. الورقة — متى عُبِّئت — تبقى المصدر الأعلى.
   وأُضيف تمرير القيم التاريخية (grandfathering) حتى لا تمنع قيمة قديمة
   تعديلَ حقل آخر في سجل قائم.
7. **عند تعذّر تحميل القوائم**: رسالة صريحة وزر إعادة محاولة داخل النموذج،
   لا سقوط صامت.
8. **«إعادة المحاولة» بعد التعذّر** (`retryDelivery`): إجراء صريح بضغطة
   واحدة يُعيد الحالة إلى «خرج مع المندوب» **دون المساس بالأجهزة ولا
   بالمندوب ولا بسبب التعذّر ولا بأي محاولة سابقة**. يحل محل المسار
   الالتفافي الذي كان الحل الوحيد عمليًا (دخول الجمعية إلى «تغيير
   المندوب» وإعادة حفظ المندوب نفسه).
9. **سجل المحاولات تراكمي**: `deliveryAttemptsFor_` يبني تاريخ كل محاولة
   (ناجحة أو متعذّرة) من ورقة التسليمات، فلا تُمحى المحاولة السابقة.
10. **اختفاء الأجهزة بعد التعذّر**: كانت الاستجابة تُعيد `record` وحده،
    والعميل يستبدل به عنصر القائمة كاملًا فيفقد `devices`. الآن كل
    استجابة تعديل في بوابة المندوب تُعيد `delegateTaskPayload_` (السجل +
    الأجهزة + المحاولات)، و`upsertRecord` تدمج بدل أن تستبدل.
11. **قائمة الجمعية كانت تبقى فارغة بعد إضافة أول مستفيد** حتى خروج
    ودخول: العرض يقرأ `bundle.total` بينما `upsertEntity` كانت تزيد
    `items` فقط. الآن العدّاد وعدد الصفحات يتحدّثان مع الإدراج الجديد.
12. **إزالة تكرار «آخر العمليات»** مرتين في لوحة الجمعية، واستبدال الجانب
    بلوحة «حالة التشغيل» (مؤشرات محسوبة من البيانات المحمَّلة أصلًا، بلا
    طلب إضافي، وكل بند ينقل إلى مصدره بفلتره).
13. **تغيير كلمة المرور المؤقتة** يُصرّح مسبقًا بأن المستخدم سيعود لتسجيل
    الدخول (التغيير يُنهي كل الجلسات عمدًا)، بدل زر «المتابعة» المضلِّل.

### ج) بوابة المندوب والتصميم

14. **مسار اليوم**: ثلاث حالات فراغ مميّزة بدل واحدة عامة (لا مهام /
    لا إحداثيات إطلاقًا فلا ترتيب ممكن / جاهز للترتيب)، وإعادة المحاولة
    متاحة داخل المسار كما في قائمة المهام.
15. **نموذج التسليم**: زر «تم التسليم» معطَّل حتى تُرفَق صورة صحيحة
    ويُوافَق على التعهد، وسبب التعطيل مكتوب ومربوط بـ`aria-describedby` —
    كان الخطأ لا يظهر إلا بعد الضغط.
16. **متابعة الأنشطة** أُعيد ابتكارها كلوحة تنفيذية بثلاث طبقات قراءة
    (شريط توزيع الحالات ← مسار المراحل كخط زمني دلالي ← بطاقات مجمَّعة
    تحت نشاطها الرئيسي) بدل الجدول التقليدي. لا حالة تُنقل باللون وحده.
17. **الشعارات**: أحجام أكبر وأوضح في الدخول والرأس وبوابة المندوب مع حفظ
    النسب بالكامل (بلا خلفية أو ظل أو قصّ).

### ما لم يُعالَج ويحتاج قرارك أو تشغيلًا حيًّا

- **تنفيذ `doGet` الشاذ (349 ثانية)**: لم يتكرر في أي قياس، والسبب الأرجح
  cold start من طرف Apps Script نفسه — متغيّر خارج سيطرة الكود تمامًا.
  يحتاج مراقبة حية عبر `traceId` الجديد لتأكيد أو نفي ذلك.
- **البيانات التاريخية المكرَّرة** (`APP-000001` مزدوج، وترتيب `ASC`
  المعكوس): **لم تُصحَّح تلقائيًا ولن تُصحَّح**. `diagnoseIdSequences_`
  جاهزة لتقرير قراءة-فقط كامل، والتصحيح قرارك اليدوي بعد مراجعته.
- **الأزمنة الفعلية بالثواني**: كل القياسات هنا من محاكاة محلية ولا تشمل
  زمن شبكة Google. التحقق النهائي يتطلب تشغيلًا حيًّا.

---

## أرشيف المراحل السابقة (١–٨)

## الحالة العامة: نسخة مرشحة للإصدار + ثماني مراحل تعزيز (حالات، حسابات، أداء، بيانات مرجعية، موقع جغرافي، ترابط البوابات، نواقص وظيفية وهوية بصرية/جوال/وصول، مراجعة أمنية نهائية وRelease Candidate)

جميع البنود المخطَّطة الأصلية (١–١٧) **مكتملة ومختبرة آليًا**، وأُضيفت إليها
**المرحلة الأولى: سلامة الحالات**، **المرحلة الثانية: إدارة حسابات
الجمعيات والمناديب**، **المرحلة الثالثة: الأداء وتقليل تكلفة عمليات Apps
Script**، **المرحلة الرابعة: توحيد البيانات المرجعية وربط القوائم
المنسدلة**، **المرحلة الخامسة: الموقع الجغرافي وأدوات المندوب ومسار
اليوم**، **المرحلة السادسة: استكمال البوابات الثلاث وربط دورة العمل
الكاملة**، **المرحلة السابعة: منع التكرار + قالب Excel حقيقي + تفاصيل
الجهاز + سجل المندوب، ثم الهوية البصرية والجوال والوصول**، **والمرحلة
الثامنة: المراجعة الأمنية النهائية (برمجية ومحاكاة)، preflight/dry-run
آمنَين لمخطط البيانات، وRelease Candidate كامل** — التفاصيل الكاملة في
الأقسام أدناه وفي `SECURITY_REVIEW.md`/`DEPLOYMENT.md`. الإجمالي الآن
**932 تأكيدًا عبر تسع أدوات اختبار وظيفية، جميعها ناجحة عند آخر commit** (بعد تصحيح أمني لاحق: قفل كل دوال الصيانة خلف رمز وصول صيانة مؤقت — راجع قسم التصحيح أدناه)
(بالإضافة إلى `tools/perf-scale-test.js` و`tools/perf-bench.js`، أداتا
تقرير أداء لا تعتمدان على نجاح/فشل). لم يُنشر أي إصدار جديد على Apps
Script الحي بهذه التعديلات، ولم تُشغَّل أي دالة إعداد أو ترحيل أو إصلاح
(بما فيها `preflightRelease`/`applyReleaseSchema` الجديدتان) على بيانات
حقيقية، ولم يُدمَج أي Pull Request — كل ذلك بانتظار قرارك.

**تنبيه مهم**: النسخة التجريبية الحية التي رُكِّبت يدويًا في مشروع Apps
Script التجريبي (بمساعدتي خطوة بخطوة) **لا تحتوي بعد** على `StateRules.gs`
ولا `Pagination.gs` (ملفان جديدان)، ولا على التعديلات الكبيرة في كل ملف
`.gs` آخر تقريبًا (`Bootstrap.gs`/`Beneficiaries.gs`/`Delegates.gs`/
`DevicesAssociations.gs`/`Applications.gs`/`ExecutionTracking.gs`/
`Normalize.gs`/`DataUtils.gs`/`Auth.gs`) ولا `Index.html` (تعديلات
جوهرية في منطق الواجهة: busy/timeout/opId/ترقيم/تحميل عند الفتح). تلك
التعديلات (المراحل الثلاث معًا) موجودة في هذا المستودع فقط حتى الآن. عند
استئناف التركيب اليدوي يلزم: (أ) إضافة ملفَي `StateRules.gs`/`Pagination.gs`
الجديدين، (ب) استبدال محتوى كل الملفات المذكورة أعلاه بالكامل بالنسخة
المحدَّثة، (ج) تشغيل `setupSheets()` مرة واحدة بعد اللصق لإضافة الأعمدة
الجديدة تلقائيًا، **قبل** أي اختبار حي — هذه المرحلة الثالثة لم تُختبَر
حيًّا إطلاقًا، فقط داخل محاكاة Node.js (راجع التفصيل الصادق في `RELEASE.md`
القسم 9).

## ما هو مكتمل ومختبر فعليًا

| # | البند | الحالة |
|---|---|---|
| 1–5 | بناء المشروع الأساسي: أمن (تشفير، جلسات، صلاحيات، منع Formula Injection)، هوية الزاد البصرية، إزالة كل inline event handlers (سبب العطل الأصلي)، أدوات فحص آلية | ✅ مكتمل |
| 6 | مصادر مرجعية موحّدة (مناطق/مدن/تصنيفات/حالات اجتماعية) مع سقوط آمن لنص حر قبل تشغيل الترحيل | ✅ مكتمل |
| 7 | إعادة هندسة الأداء: ذاكرة تخزين مؤقت داخل الطلب + ذاكرة عبر الطلبات لكامل `getBootstrapData` + قياس تجريبي فعلي (`tools/perf-bench.js`) + تقسيم `Code.gs` إلى 12 ملف `.gs` موضوعي | ✅ مكتمل (`Index.html` بقي ملفًا واحدًا عمدًا — انظر التبرير أدناه) |
| 8 | بوابة تقديم الجمعيات العامة (بلا تسجيل دخول) + مراجعة إدارية (قبول/رفض) مع إنشاء الجمعية وحسابها تلقائيًا وكلمة مرور مؤقتة تُعرض مرة واحدة فقط | ✅ مكتمل |
| 9 | لوحة إدارة تفاعلية: كل مؤشر KPI وكل تنبيه قابل للنقر وينقل لصفحة مفلترة | ✅ مكتمل |
| 10 | إدارة الأنشطة الرئيسية والفرعية من داخل بوابة الإدارة: إضافة/تعديل بنموذج كامل، تبويبات تصفية (الكل/جارية/متأخرة/مكتملة/قادمة) | ✅ مكتمل |
| 11 | موقع المستفيد الجغرافي (إحداثيات اختيارية + زر تحديد الموقع الحالي) ومسار يوم المندوب التقريبي (خوارزمية أقرب-جار مجانية بالكامل عبر Haversine، بلا أي API مدفوع) | ✅ مكتمل |
| 12 | مراجعة اللغة الرسمية + مراجعة أمنية نهائية + سجل عمليات قابل للبحث والتصفية بالقسم | ✅ مكتمل |
| 13 | دمج الشعارين الحقيقيين (الزاد ومؤسسة أبانمي) من `assets/` في الهوية البصرية فعليًا | ✅ مكتمل |
| 14 | تقييم تقسيم الملفات: `Code.gs` قُسِّم فعليًا، `Index.html` بقي عمدًا (قرار موثَّق، لا تجاهل) | ✅ مكتمل |
| 15 | مراجعة أمنية آلية شاملة عبر تسعة محاور (`tools/security-test.js`، 79 اختبارًا) | ✅ مكتمل |
| 16 | دعم الإحداثيات في الاستيراد الجماعي (CSV وExcel) مع توافق كامل مع الملفات القديمة | ✅ مكتمل |
| 17 | تجهيز خطة اختبار حي كاملة ونسخة مرشحة للإصدار (`RELEASE.md`) | ✅ مكتمل |

نتائج القياس الفعلي للأداء (`node tools/perf-bench.js`، قبل commit `c188f74`
مقابل النسخة الحالية، بعدد قراءات `getDataRange().getValues()` الكاملة —
المقياس الحقيقي لتكلفة Sheets API):

| السيناريو | قبل | بعد | التحسّن |
|---|---|---|---|
| بناء بوابة جمعية واحدة | 10 | 6 | ‑40٪ |
| بناء بوابة إدارة واحدة | 8 | 9 | +12٪ (زيادة سطر واحد بسبب فحص طلبات الانضمام الجديدة — ليست تراجعًا) |
| دخول أول لجمعية | 11 | 6 | ‑45٪ |
| دخول ثانٍ فوري (خلال دقيقة) | 11 | 0 | ‑100٪ (كاش كامل) |

لا يقيس هذا زمن شبكة فعليًا (لا تتوفر بيئة Apps Script حية في جلسات التطوير
هذه) لكنه المتغيّر المتحكم مباشرة في ذلك الزمن.

## ما أُنجز في هذه الجولة الثانية (البنود 13–17)

- **الشعاران الحقيقيان**: الملفان المرفوعان في `assets/` تحقَّقا سليمين
  (توقيع PNG صحيح، قناة شفافية فعلية، بلا خلفية صلبة)، حُوِّلا إلى data URI
  (بعد تصغير الأبعاد فقط بالحفاظ الكامل على النسبة الأصلية) وحُقنا في ثابت
  `BRAND`. يظهران الآن فعليًا في شاشة الدخول والشريط الجانبي ورأس بوابة
  المندوب، وفق نفس تصميم الـ Lockup القائم مسبقًا (الزاد رئيسيًا، الشريك
  في صف مستقل بعبارة "بالشراكة مع"، بلا دمج ولا تكرار زخرفي).
- **تقسيم `Code.gs`**: قُسِّم من ملف واحد (1810 سطرًا) إلى 12 ملف `.gs`
  موضوعي (`Config`, `ReferenceData`, `Auth`, `Bootstrap`, `Beneficiaries`,
  `Delegates`, `DevicesAssociations`, `Applications`, `ExecutionTracking`,
  `Normalize`, `DataUtils`, `Validation`) — تقسيم آمن ومختبَر لأن Apps
  Script يدمج كل ملفات `.gs` تلقائيًا ضمن نطاق عام واحد بصرف النظر عن اسم
  الملف أو ترتيبه. تحقّق فعلي عبر `diff` أن دمج الملفات الاثني عشر بالترتيب
  الأصلي يعيد المحتوى الأصلي حرفًا بحرف قبل أي تعديل لاحق. **`Index.html`
  بقي ملفًا واحدًا عمدًا** — القرار وتبريره التقني الكامل موثَّقان في رسالة
  commit التقسيم وفي قسم "المتبقي" أدناه.
- **مراجعة أمنية آلية شاملة**: أداة جديدة `tools/security-test.js` (79
  اختبارًا) تغطي: المصادقة والجلسات (سقف العمر المطلق، الإبطال الفوري)،
  مصفوفة صلاحيات شاملة لكل دالة خادم حساسة ضد كل الأدوار غير المصرَّح بها،
  عزل الجمعيات (IDOR) بسيناريو جمعيتين ومندوبين فعليين، XSS وحقن الصيغ
  بكتابة/قراءة فعلية، حدود المدخلات، رفع الملفات (أنواع MIME مزوَّرة
  وتجاوز الحجم)، تحديد المعدّل، عدم تسريب الأسرار (تحقّق ديناميكي على
  حمولة API الفعلية)، وسلامة سجل العمليات (إلحاق فقط، كل دالة تعديل
  تستدعي `audit_`). إصلاح فعلي وُجد أثناء المراجعة: `saveBeneficiary` كانت
  الدالة الوحيدة التي تستخدم `payload.id` خامًا دون `cleanId_` (غير قابل
  للاستغلال فعليًا، لكن أُصلح للاتساق الدفاعي).
- **الإحداثيات في الاستيراد الجماعي**: `importBeneficiaries` (CSV)
  و`inspectBeneficiaryExcel` (معاينة Excel) يدعمان الآن عمودين اختياريين
  لخط العرض/الطول، بتحقّق كامل عبر `optionalCoordinate_` ورفض واضح لكل
  صف خاطئ دون إسقاط الاستيراد كله — مع توافق تام مع ملفات قديمة بلا هذين
  العمودين.
- **`RELEASE.md`**: خطة اختبار حي كاملة — إنشاء نسخة تجريبية معزولة، ترتيب
  تشغيل `setupSheets`/`previewPhoneNormalization`/`migratePhoneNumbers`/
  `migrateReferenceData` (موصوف لا مُنفَّذ)، بيانات اختبار وهمية جاهزة
  للنسخ، قائمة اختبار يدوية ببنود لكل زر ومسار وحالة في البوابات الثلاث
  مع النتيجة المتوقعة لكل بند، وخطة رجوع (Rollback) واضحة.

## المرحلة الأولى: سلامة الحالات والترابط (جهاز ↔ مستفيد ↔ تسليم ↔ مندوب)

**الهدف**: منع أي تعديل مستقل لحالة الجهاز أو حالة التسليم قد ينتج عنه وضع
غير متّسق (جهاز "مخصص" بلا مستفيد، مستفيد "تم التسليم" بجهاز لا يزال
"بالمستودع"، تخصيص جهاز واحد لمستفيدين، تسليم جهاز لم يخرج مع مندوب، إلخ)،
عبر مصدر قواعد واحد يستخدمه كل مسار تعديل.

- **`StateRules.gs` (ملف جديد)**: يعرّف خريطتي انتقال مجمَّدتين
  `DEVICE_STATUS_TRANSITIONS_` و`DELIVERY_STATUS_TRANSITIONS_` — لكل حالة
  حالية، قائمة الحالات التالية المسموحة فقط. `assertDeviceTransition_` /
  `assertDeliveryTransition_` ترمي خطأ عربيًا واضحًا لأي انتقال غير مسموح
  أو حالة غير معروفة. **الحالتان النهائيتان "تم التسليم" (جهاز وتسليم) لا
  تملكان حلقة ذاتية عمدًا** — إعادة "تأكيد" تسليم مكتمل بالفعل تُرفض دائمًا،
  لا تُقبل كعملية بلا أثر (هذا فرق دقيق ومهم جرى تصحيحه أثناء التصميم قبل
  أي تشغيل).
- **قواعد الحالة المفروضة فعليًا في كل مسار تعديل**:
  - `saveDevice` (نموذج الجهاز اليدوي): لا يقبل ضبط "مع المندوب" أو "تم
    التسليم" يدويًا إطلاقًا — فقط `assignDelegate`/`confirmDelivery` تصلان
    لهما. يمنع تخصيص جهاز "نشط" (مخصص/مع المندوب/تم التسليم) لمستفيد آخر
    دون إرجاعه للمستودع أولًا. تعديل حقول وصفية (الاسم/الملاحظات) بلا تغيير
    الحالة الفعلية مسموح دائمًا حتى لو كانت الحالة نهائية.
  - `assignDelegate`: يتطلب وجود جهاز واحد على الأقل "مخصص" أو "مع المندوب"
    فعلًا للمستفيد (لا يقبل تعيين مندوب لمستفيد بلا أجهزة)، وينقل كل الأجهزة
    المخصَّصة إلى "مع المندوب" وحالة التسليم إلى "خرج مع المندوب" معًا داخل
    قفل (`LockService`).
  - `confirmDelivery`: يقبل فقط الأجهزة التي حالتها "مع المندوب" فعلًا،
    ويُحدِّث المستفيد وكل أجهزته المرتبطة إلى "تم التسليم" كوحدة واحدة داخل
    قفل، مع إعادة قراءة وتحقق من الحالة *داخل* القفل لإغلاق نافذة السباق
    عند نقرتين متتاليتين سريعتين (تأكيد مزدوج).
  - `updateDeliveryStatus` (تعذر التسليم): لا يقبل تسجيل "تعذر التسليم" إلا
    من "خرج مع المندوب" (أو إعادة محاولة سابقة فاشلة) — لا يمكن تسجيل تعذّر
    لمستفيد لم تخرج أجهزته أصلًا. لا يغيّر حالة الأجهزة (تبقى "مع المندوب").
  - `importBeneficiaries` (الاستيراد الجماعي): لا يكتب أي حالة جهاز أو
    تسليم إطلاقًا — يُنشئ سجلات مستفيدين جدد فقط، فلا يمكنه تجاوز القواعد.
- **`diagnoseStateIntegrity()` (قراءة فقط، جديدة)**: تفحص كل الأجهزة
  والمستفيدين وتُخرج تقريرًا منظَّمًا (`{ok, issueCount, bySeverity, issues}`)
  يغطي عشرة أنواع تعارض محتملة (حالة غير معروفة، جهاز يتيم الحالة، جهاز
  مرتبط بمستفيد لكن "بالمستودع"، جهاز يشير لمستفيد غير موجود، تعارض جمعية،
  مستفيد "تم التسليم" بأجهزة غير متّسقة، إلخ). آمنة للتشغيل اليدوي من محرر
  Apps Script في أي وقت — لا تكتب أي شيء (مُتحقَّق منها باختبار مخصَّص).
- **`repairStateIntegrityIssues()` (مُجهَّزة، لم تُشغَّل)**: تُصلح فقط
  الحالات الآمنة ذات التفسير الوحيد القاطع، وتترك أي حالة غامضة (كتعارض
  الجمعية) دون لمس مع توثيقها في `skipped`. **غير مُستدعاة من أي دالة أخرى
  في المشروع ولا تُشغَّل تلقائيًا أبدًا** (مُتحقَّق منه بفحص أن اسمها يظهر
  مرة واحدة فقط في كامل المصدر — تعريفها نفسه). لم تُشغَّل على أي بيانات —
  لا محاكاة ولا حية — تنفذ فقط عند استدعائها يدويًا صراحة من محرر Apps
  Script بعد مراجعة تقرير `diagnoseStateIntegrity()`.
- **اختبارات جديدة (`tools/state-test.js`, 53 تأكيدًا)**: تغطي كل زوج
  انتقال مسموح ومرفوض على مستوى الوحدة، دورة حياة كاملة ناجحة عبر الدوال
  الفعلية (تخصيص → خروج مع مندوب → تسليم)، رفض إعادة التأكيد المزدوج مع
  التحقق من عدم إضافة أكثر من سجل تسليم واحد، دورة تعذّر ثم إعادة محاولة
  ناجحة، رفض التخصيص المزدوج لجهاز واحد لمستفيدين، منع الضبط اليدوي
  للحالات المحمية، سيناريوهات فساد بيانات مُفتعلة ثلاثة يكتشفها التشخيص
  ويُصلحها الإصلاح المُجهَّز بنجاح، وفحوص ثابتة تتأكد أن كل مسارات التعديل
  الأربعة تستخدم فعليًا دوال التحقق المركزية نفسها.
- **ملاحظة تصميم موثَّقة**: حالة التسليم الوسيطة "جاري التجهيز" لم تعد
  تُستخدم فعليًا من أي مسار حالي (بقيت في `DELIVERY_STATUSES` وخريطة
  الانتقال لتوافق بيانات قديمة/مستوردة فقط) — `assignDelegate` ينقل مباشرة
  من "لم يبدأ" إلى "خرج مع المندوب" بمجرد تخصيص جهاز وتعيين مندوب معًا.

**لم يُنفَّذ بعد من نطاق سلامة الحالات (يحتاج قرارًا أو عملًا لاحقًا)**:
- تحديث واجهة `deviceForm()` في `Index.html`: القائمة المنسدلة لحالة
  الجهاز لا تزال تعرض بصريًا كل الحالات الخمس (بما فيها "مع المندوب"/"تم
  التسليم") رغم أن الخادم يرفضها دائمًا الآن برسالة واضحة. تحسين شكلي
  (تعطيل/إخفاء تلك الخيارات في القائمة) مؤجَّل عمدًا — الطلب الصريح كان
  عدم البدء بتحسينات الشكل قبل إنهاء سلامة الحالات أولًا.
- تشغيل `repairStateIntegrityIssues()` على أي بيانات حية: بانتظار تشغيل
  `diagnoseStateIntegrity()` أولًا على النسخة الحية بعد تركيب هذه الملفات،
  ومراجعة تقريرها، وموافقتك الصريحة.
- المرحلة الثانية (إن وُجدت) من سلامة الحالات: هذا العمل غطى فقط ما طُلب
  صراحة (المرحلة الأولى) — أي توسعة أخرى (كإدارة "تالف" أعمق، أو سيناريوهات
  استرجاع/عكس بعد التسليم) تحتاج طلبًا صريحًا جديدًا.

## المرحلة الثانية: إدارة حسابات الجمعيات والمناديب واستعادة الوصول

**الهدف**: منح الإدارة أداة آمنة لاستعادة وصول جمعية فقدت كلمة مرورها أو
اشتُبه في اختراق حسابها، ومنح الجمعية رؤية وضبطًا كاملَين على مناديبها،
دون أي ثغرة صلاحيات أو تسريب سرّ — مع فرض كل ذلك من الخادم لا من الواجهة.

### أولًا: حساب الجمعية
- **`resetAssociationPassword(token, associationId)` (جديدة، `DevicesAssociations.gs`، ADMIN فقط)**:
  تُنشئ كلمة مرور مؤقتة قوية عشوائية (`generateStrongTempPassword_`، تجتاز
  `assertStrongPassword_` دائمًا)، تحفظ كلمة المرور القديمة وملحها في عمودين
  جديدين "سابقين" (لمنع إعادة استخدامها لاحقًا)، تضبط عمود "يجب تغيير كلمة
  المرور" إلى "نعم"، وتُبطل جلسة حساب الجمعية نفسه فورًا (لا تمسّ جلسات
  مناديبها — سرّان مستقلان). محمية بقفل (`LockService`) وتحديد معدل
  (`throttle_`، 5 محاولات/15 دقيقة لكل جمعية) لمنع تكرار أو تزامن يُبقي أكثر
  من كلمة مرور صالحة. تُعيد كلمة المرور الجديدة **فقط** في استجابة هذا
  الاستدعاء — لا تُسجَّل في `audit_` (يُسجَّل اسم الجمعية فقط)، ولا تُخزَّن
  في أي مكان آخر نصًا صريحًا.
- **إجبار تغيير كلمة المرور المؤقتة عند أول دخول**: عمود جديد "يجب تغيير
  كلمة المرور" في ورقة "المستخدمون". `requireSession_` (الآن يقبل `opts`
  ثالثًا) يرفض **كل** استدعاء خادم لحساب جمعية يحمل هذا الوسم إلا
  `changePassword`/`logout` — رفض حقيقي من الخادم، لا إخفاء في الواجهة، حتى
  لو استُدعيت أي دالة أخرى مباشرة متجاوزةً الواجهة. `loginUser_` لا يعيد
  `bootstrap` إطلاقًا لحساب بهذا الوسم — فقط `{token, user, mustChangePassword:true}`.
- **منع إعادة استخدام كلمة المرور السابقة**: `changePassword` (`ExecutionTracking.gs`)
  يقارن كلمة المرور الجديدة بالحالية (رفض إن كانت مطابقة) وبالسابقة المحفوظة
  (رفض إن كانت مطابقة) — طبقة حماية واحدة ضمن البنية الحالية (لا يُحفظ سجل
  كامل لكل كلمات المرور التاريخية، فقط آخر واحدة سبقت الحالية، محفوظة فعليًا
  في العمودين الجديدين عند كل تغيير ناجح أو إعادة تعيين).
- **الواجهة**: زر "إعادة تعيين كلمة المرور" في صفحة تفاصيل الجمعية (ADMIN
  فقط) يفتح نافذة تأكيد وتحذير واضحة (تحذير قطع الجلسات فورًا)، ثم يعرض
  كلمة المرور الجديدة مرة واحدة بزر نسخ صريح ورسالة تحذّر من عدم ظهورها
  مجددًا وتوصي بإعادة تعيين جديدة إن ضاعت بدل محاولة استرجاعها. شاشة كاملة
  إلزامية جديدة (`renderForcePasswordChange`) تظهر تلقائيًا بعد تسجيل دخول
  حساب بهذا الوسم، ولا تسمح بأي تنقّل قبل تغيير كلمة المرور فعليًا؛ بعد
  النجاح تُقطع الجلسة (بما يشمل الحالية) ويُعاد المستخدم لشاشة الدخول
  برسالة واضحة.

### ثانيًا: حساب المندوب
- **صفحة تفاصيل المندوب (`viewDelegate`, جديدة, `Index.html`)**: تعرض رقم
  الجوال، الجمعية، آخر دخول، عدد المهام الحالية، عدد المستفيدين المخدومين
  (تم تسليمهم)، وجدول كامل بالمستفيدين المكلَّف بهم حاليًا (الاسم ورقمه
  وحالة التسليم) — بيانات مُشتقة بالكامل من `state.data` المُصفّى مسبقًا من
  الخادم حسب الدور (لا استدعاء خادم إضافي، ولا كشف بيانات خارج نطاق الفاعل).
- **`regenerateDelegateCode`**: مُعزَّزة بقفل (`LockService`) وتحديد معدل
  (`throttle_`، 8 محاولات/15 دقيقة لكل مندوب) لمنع بقاء أكثر من رمز صالح
  واحد مع طلبات متكررة أو متزامنة. الرمز القديم يُبطَل فور استبدال تجزئته
  وملحه، وجلسة المندوب المفتوحة بالرمز القديم تُقطع فورًا (`revokeSessions_`).
  الواجهة: نافذة تأكيد قبل التنفيذ، وعرض الرمز الجديد مرة واحدة بزر نسخ
  (كان معروضًا بلا زر نسخ سابقًا — أُضيف الآن).
- **تفعيل/تعطيل المندوب (`setDelegateStatus`, موجودة مسبقًا)**: مؤكَّد أنها
  تقطع جلسات المندوب فورًا عند التعطيل، وأن دخوله بالرمز يُرفض فور التعطيل
  (كلاهما مختبر الآن صراحة في `tools/account-test.js`). نافذة تأكيد موجودة
  مسبقًا في الواجهة.
- لا يُسجَّل رمز الدخول (الجديد أو القديم) كنص صريح في `audit_` عند أي من
  هذه العمليات — مُتحقَّق منه اختباريًا بفحص محتوى سجل العمليات فعليًا.

### ثالثًا: الصلاحيات والأمان
- `resetAssociationPassword`: `requireSession_(token, ['ADMIN'])` — لا تصل
  إليها أي جمعية أو مندوب مهما حاول.
- عزل الجمعيات في إدارة المناديب: الفحص الموجود مسبقًا
  (`if (user.role === 'ASSOCIATION' && ...associationId !== user.associationId)`)
  في `regenerateDelegateCode`/`setDelegateStatus`/`saveDelegate` مؤكَّد الآن
  باختبار فعلي بجمعيتين ومندوبين حقيقيين: جمعية لا تستطيع إعادة إنشاء رمز
  أو تعطيل مندوب جمعية أخرى، ومحاولاتها المرفوضة لا تترك أي أثر جزئي.
  الإدارة (ADMIN) تحتفظ بصلاحيتها الإشرافية الكاملة على كل المناديب — لم
  تُقيَّد، لأن ذلك لم يكن مطلوبًا وكان سيكسر سلوكًا قائمًا مقصودًا.
  Admin لا يستقبل أي سرّ قديم عبر `resetAssociationPassword` — فقط السرّ
  الجديد المُنشَأ للتو.
- التحقق من الدور والملكية مفروض بالكامل من الخادم (`requireSession_`
  بالأدوار الصريحة + فحوص `associationId` صريحة) — لا اعتماد على إخفاء
  أزرار الواجهة في أي مسار من هذه المرحلة.
- تحديد معدل وقفل تزامن على `resetAssociationPassword` و`regenerateDelegateCode`
  كما ورد أعلاه — مختبَر بمحاكاة استدعاءات متتالية سريعة فعليًا.
- لم تُعدَّل أي بيانات حية ولم تُشغَّل أي دالة ترحيل أو إصلاح خلال هذه
  المرحلة — كل ما سبق مُنفَّذ ومختبَر داخل بيئة محاكاة Node.js فقط.

### رابعًا: تحسين دورة الدخول
- رسائل الخطأ العامة (بريد/كلمة مرور/رمز مندوب خاطئ) كانت بالفعل موحَّدة
  وغير كاشفة لوجود الحساب من قبل هذه المرحلة (`'بيانات الدخول غير صحيحة'`،
  `'رمز الدخول غير صحيح أو المندوب غير نشط'`) — رُوجعت ومؤكَّد أنها لا تزال
  كذلك، لم تُغيَّر.
- رسالة انتهاء الجلسة (`'انتهت الجلسة. يرجى تسجيل الدخول مجددًا'`) كانت
  موجودة وواضحة مسبقًا وتُعيد المستخدم لشاشة الدخول تلقائيًا (`reportError`
  → `doLogout`) — لم تُغيَّر.
- **جديد**: حساب مُلزَم بتغيير كلمة مرور مؤقتة أو حساب معطَّل لا يستطيع
  تجاوز القيود عبر استدعاء API مباشر — مُتحقَّق منه الآن بفحص اختباري صريح
  يستدعي `getBootstrapData`/`saveBeneficiary` مباشرة بتوكن حساب مُلزَم
  بالتغيير ويؤكد الرفض من الخادم لا الواجهة فقط.
- **جديد**: واجهة تغيير كلمة المرور المؤقتة عند أول دخول للجمعية
  (`renderForcePasswordChange` في `Index.html`) — شاشة كاملة إلزامية، لا
  اختيارية ولا قابلة للتجاوز بالتنقّل.

### اختبارات جديدة (`tools/account-test.js`, 53 تأكيدًا)
عشرة أقسام: نجاح إعادة تعيين كلمة مرور الجمعية وقوة الكلمة الناتجة، إبطال
الجلسة القديمة للجمعية (مع تأكيد عدم تأثّر جلسة مندوبها)، فرض تغيير كلمة
المرور عند أول دخول عبر الدوال الفعلية (بما في ذلك محاولة تجاوز مباشرة عبر
API)، عدم تسجيل أي سرّ في سجل العمليات، إعادة إنشاء رمز مندوب وإبطال
القديم فورًا (مع قطع جلسته المفتوحة)، منع دخول مندوب معطَّل ومتابعة جلسة
قديمة، عزل الجمعيات الكامل عن مناديب بعضها، مقاومة تكرار الطلب والتزامن
(كلمة مرور/رمز واحد صالح فقط رغم استدعاءات متتالية، وتحديد المعدّل يمنع
سلسلة طويلة)، عدم كشف أي تجزئة أو ملح أو سرّ قديم في أي استجابة خادم فعلية
(بمقارنة القيم الحرفية لا أسماء الحقول فقط)، وأخيرًا فحص صريح أن قواعد
سلامة الحالات من `c449d3d` لم تتأثر بهذه التعديلات.

**لم يُنفَّذ بعد من نطاق إدارة الحسابات (يحتاج قرارًا أو عملًا لاحقًا)**:
- إجبار تغيير كلمة المرور عند **إنشاء** حساب جمعية جديد (طلب انضمام مقبول
  أو إضافة يدوية من الإدارة) — طُبِّق الإلزام فقط على مسار **إعادة
  التعيين** الصريح لأن هذا ما طلبه النص حرفيًا. تمديده لمسار الإنشاء
  الابتدائي منطقي ومتّسق لكنه قرار مستقل يحتاج طلبًا صريحًا.
- منع إعادة استخدام كلمة المرور محدود بعمق واحد فقط (الكلمة السابقة
  مباشرة، لا سجل كامل لكل كلمات المرور التاريخية) — موثَّق صراحة كقيد
  "ضمن البنية الحالية" في الكود والاختبارات، لا كسهو.
- لا توجد صفحة "تفاصيل حساب الجمعية" مستقلة موسَّعة (سجل تسجيلات دخول
  مفصَّل، مثلًا) — التفاصيل الحالية ضمن النافذة المنبثقة القائمة فقط.

## المرحلة الثالثة: الأداء، تعليق الواجهة، وتكلفة عمليات Apps Script

**الهدف**: معالجة أعطال أداء حية مؤكَّدة (دخول 9–10 ثوانٍ، تعليق واجهة أكثر
من 50 ثانية بلا رسالة، أزرار معطَّلة حتى تحديث الصفحة) دون حذف أي ميزة
قائمة، ودون تشغيل أي شيء على بيانات حية.

### أولًا: منع التعليق
- **مهلة موحّدة** (`API_TIMEOUT_MS = 25000` في `Index.html`): كل استدعاء
  `google.script.run` يُرفض تلقائيًا بعد 25 ثانية برسالة صريحة موسومة
  `timeout:true`، بدل انتظار غير محدود. رد متأخر يصل بعد انتهاء المهلة
  يُتجاهَل عمدًا (`settled` flag) حتى لا يُحدِّث الحالة بعد أن رأى المستخدم
  رسالة الفشل بالفعل.
- **تعطيل مُوجَّه لا شامل**: `busy()` أُعيدت كتابتها بالكامل — تُعطِّل فقط
  `state.activeTrigger` (الزر أو النموذج الذي بدأ العملية فعليًا، يُضبط في
  موزِّعَي click/submit المركزيَّين قبل استدعاء أي معالج) بدل `$$('[data-act]')`
  التي كانت تُعطِّل **كل** زر في الصفحة سابقًا — هذا السبب المباشر لعطل
  "بقيت جميع الأزرار معطلة حتى تحديث الصفحة".
- **فك تعطيل مضمون دائمًا**: كل استدعاء `busy(true)` في الملف (21 موضعًا)
  يُقابله الآن `.finally(() => busy(false))` بدل `.then(fn)` العادية —
  `.finally()` يعمل مهما كانت نتيجة السلسلة (نجاح/فشل/مهلة/حتى لو رمى
  معالج النجاح نفسه خطأً)، بخلاف `.then()` وحدها التي كانت تتخطّى `busy(false)`
  إن رُفضت أي مرحلة سابقة في السلسلة.
- **معالجة صادقة عند المهلة تحديدًا**: `reportError()` عند `error.timeout===true`
  لا تفترض نجاحًا ولا فشلًا — تعرض رسالة "لا نعرف نتيجتها بعد" ثم تُحدِّث
  `getBootstrapData` بأمان في الخلفية للتحقق الفعلي من الحالة.
- **معرّفات عمليات (opId) لمنع التكرار عند إعادة المحاولة**: `withIdempotency_`
  جديدة (`DataUtils.gs`) تُنفِّذ العملية مرة واحدة فقط لكل (فاعل + opId)
  خلال 5 دقائق، وتُعيد نفس النتيجة الأصلية عند التكرار بدل تنفيذها من
  جديد. مُطبَّقة على: إنشاء جمعية (`saveAssociation`)، إنشاء مندوب
  (`saveDelegate`)، قبول طلب انضمام (`reviewAssociationApplication`)،
  وتأكيد تسليم (`confirmDelivery` — الأهم: تأكيد التسليم حالة نهائية
  بلا حلقة ذاتية في `StateRules.gs`، فإعادة محاولة عادية بعد نجاح فعلي
  أول كانت سترمي خطأ "غير مسموح" مربكًا للمندوب رغم أن التسليم نجح).
  `opIdFor`/`clearOpId` في `Index.html` يُنشئان المعرّف مرة عند أول محاولة
  ويُعاد استخدامه فقط عند إعادة الضغط على نفس الزر بعد فشل/مهلة، لا عند
  عملية جديدة تمامًا.

### ثانيًا: تقليل تكلفة الخادم
- **لا `getBootstrapData` كاملة بعد كل حفظ**: `saveBeneficiary`،
  `saveDevice`، `saveDelegate`، `saveAssociation`، `assignDelegate`،
  `confirmDelivery`، `updateDeliveryStatus`، `saveActivity`،
  `updateAssociationSettings`، `reviewAssociationApplication` تُعيد الآن
  فقط `{record, summary}` (أو ما يعادلهما) بدل بوابة كاملة. الواجهة تدمج
  ذلك عبر `upsertRecord()`/`mergeSummary()` الجديدتين بدل استبدال
  `state.data` بأكمله — تحديث جزئي حقيقي، لا إعادة تحميل.
- **`computeCoreSummary_`/`computeAssociationsCount_`** (`Bootstrap.gs`):
  ملخّص مصغَّر يتجنّب قراءة جدولي الأنشطة/الشواهد لعمليات لا تمسّهما
  (حفظ مستفيد/جهاز/مندوب)، ويُقيَّد بجمعية واحدة لحسابات الجمعية بدل
  حساب نطاق النظام كله.
- **تحميل عند الفتح لا عند الدخول**: سجل العمليات (`listAuditLog`)،
  الأنشطة/المراحل/الشواهد (`getActivitiesBundle`)، طلبات الانضمام
  (`listApplications`) خرجت من `getBootstrapData` الأولي بالكامل — تُجلب
  فقط عند فتح صفحتها (`goTo()` تستدعي `fetchLazyPage`/`fetchActivitiesBundle`).
  لوحة البيانات احتفظت بمعاينة صغيرة ثابتة (10 سطور) من سجل العمليات
  فقط (`getAuditRows_(10, ...)`) لأنها تحتاجها فعليًا لعرض "آخر العمليات".
- **`invalidateTableCache_`/`clearDashboardCache` بلا تغيير جوهري** —
  كانتا سليمتين أصلًا؛ مؤكَّدتان الآن باختبار صريح (`tools/perf-test.js`
  القسم 3) أن الكتابة تُبطل الذاكرة المؤقتة فورًا قبل أي قراءة تالية.

### ثالثًا: الجداول والبحث — ترقيم خادمي (`Pagination.gs` جديد)
- **بنية عامة**: `paginate_`/`applySearch_`/`applySort_` — يستخدمها كل
  `list*()`. حد أقصى لحجم الصفحة (100) يمنع طلب صفحة ضخمة تلتف حول
  الترقيم بالكامل.
- **`listBeneficiaries`** (`Beneficiaries.gs`) — **مُدمَجة بالكامل في
  الواجهة**: صفحة المستفيدين تستخدمها فعليًا الآن (لا مصفوفة كاملة محليًا
  لهذا الجدول تحديدًا... **إلا أنها لا تزال أيضًا موجودة كمصفوفة كاملة في
  Bootstrap** — انظر التوضيح الحرج أدناه).
- **`listAuditLog`** (`Normalize.gs`) و**`listApplications`** (`Applications.gs`)
  — مُدمَجتان بالكامل، بصفحة/بحث/فلترة خادميين حقيقيين وأزرار تنقّل
  (`pagerControls`).
- **`listDevices`/`listAssociations`/`listDelegates`** — **مُنشَأة
  ومختبَرة خادميًا بالكامل** (`tools/perf-test.js`) لكن **لم تُدمَج في
  واجهة الجداول المقابلة بعد** (تفصيل القرار وسببه في القسم التالي).
- **عزل الجمعيات مفروض من الخادم في كل `list*`**: دور `ASSOCIATION` يُقيَّد
  بجمعيته دومًا بصرف النظر عن أي `associationId` يُرسله العميل (مختبَر
  صراحة في `tools/perf-test.js`).

**⚠️ توضيح حرج وصادق — لماذا `beneficiaries`/`devices`/`delegates`/`associations`
بقيت أيضًا كمصفوفات كاملة في `getBootstrapData`**: خطة العمل الأصلية كانت
إزالتها بالكامل والاعتماد على `list*` فقط. أثناء التنفيذ تبيَّن أن عشرات
المواضع في `Index.html` (قوائم اختيار الجمعية/المستفيد في نموذج الجهاز،
اختيار المندوب في نافذة التعيين، `associationName()`/`delegateName()`/
`findBeneficiary()` المستخدَمة في كل صفحة تقريبًا للعرض والربط، تصفية KPI
القابلة للنقر) تفترض وجود هذه المصفوفات كاملة محليًا. إعادة كتابة كل ذلك
إلى نمط "جلب صفحة عند الحاجة" ممكنة تقنيًا لكنها **إعادة هيكلة أوسع من
نطاق مرحلة واحدة يمكن التحقق منها بأمان دون متصفح حي** (بيئة هذه الجلسة
لا تصل إلى Apps Script حقيقي لأي اختبار بصري فعلي). القرار: تنفيذ الترقيم
الخادمي الكامل والمدمَج فعليًا في الواجهة لأكبر مصدرَي نمو حقيقيَّين
(المستفيدون، سجل العمليات) والأقسام الثقيلة منخفضة التكرار (الأنشطة،
الطلبات)، وتجهيز بقية الجداول (`listDevices`/`listAssociations`/`listDelegates`)
جاهزة ومختبَرة خادميًا لدمج لاحق آمن — بدل مخاطرة تغيير شامل غير مُتحقَّق
منه بصريًا. **الأثر العملي**: `beneficiaries` تُرسَل مرتين فعليًا حاليًا
(مصفوفة كاملة في Bootstrap + عبر `listBeneficiaries` عند فتح الصفحة) — هذا
تكرار مؤقت غير مثالي، موثَّق صراحة، وليس الوضع النهائي المرغوب.

### رابعًا: قياس الأداء
- **`perfTime_`/`_PERF_`** (`DataUtils.gs`): يطبع سطر JSON واحدًا (اسم
  العملية، الزمن بالمللي ثانية، عدد قراءات/كتابات الجداول الفعلية — بلا
  أي بيانات مُدخَلة أو حسّاسة) لكل: `login`, `getBootstrapData`,
  `listBeneficiaries`, `listDevices`, `listAssociations`, `listDelegates`,
  `listApplications`, `listAuditLog`, `getActivitiesBundle`,
  `saveBeneficiary`, `assignDelegate`, `confirmDelivery`. يظهر في سجل
  التنفيذ (Executions) الحقيقي عند التشغيل على Apps Script فعليًا.
- **`tools/perf-scale-test.js`** (جديدة): قياس بأحجام محاكاة 100/1000/5000
  مستفيد، داخل Node.js فقط، بلا أي كتابة على Sheets حية. النتائج الكاملة
  والتفسير الصادق (زمن محلي لا زمن شبكة، عدد القراءات ثابت عبر الأحجام،
  الزمن ينمو خطيًا) في `RELEASE.md` القسم 9 — **لم يُشغَّل أي قياس حي**.
- **الأهداف المطلوبة (تنقّل محلي <1ث، بحث معتاد <2ث، حفظ معتاد <3ث)**:
  محقَّقة في القياس المحلي لأحجام حتى ~1000–2000 مستفيد بهامش واسع؛ عند
  5000 يصبح زمن المعالجة المحلي وحده (~150ms) ملموسًا وسيتراكم فوق زمن
  الشبكة الحقيقي غير المُقاس هنا — قيد معماري في بنية Sheets موثَّق، لا
  إخفاق في هذه المرحلة.

### خامسًا: تنظيم `Index.html`
- **لم يُقسَّم** — القرار نفسه من إعادة الهندسة الأولى (البند 7) أُعيد
  فحصه وتأكيده: `HtmlService.include()` يُدمَج نصيًا على الخادم قبل
  الإرسال فلا يقلّل الحمولة الفعلية، ويزيد خطر أعطال حدود الملفات
  (بالضبط فئة العطل الأصلية للمشروع). الملف كبر أكثر بإضافات هذه المرحلة
  (ترقيم، معالجة مهلة، opId) لكن التقسيم لا يزال غير مبرَّر تقنيًا.
- تقليل تكرار محدود عبر مكوّنات مشتركة جديدة: `pagerControls()`
  (أزرار تنقّل صفحات موحَّدة)، `showSecretReveal()` (كانت `showAccessCode`
  فقط)، `upsertRecord()`/`mergeSummary()` (دمج جزئي موحَّد).

### اختبارات جديدة
- **`tools/perf-test.js`** (32 تأكيدًا): ترقيم/بحث/فلترة/عزل أدوار لـ
  `listBeneficiaries`/`listAssociations`/`listAuditLog`/`listApplications`،
  استجابات حفظ جزئية (لا `data` كاملة) لكل دوال الحفظ الرئيسية، إبطال
  الذاكرة المؤقتة الصحيح بعد الكتابة، عدم تراجع سلامة الحالات أو إدارة
  الحسابات، وتأكيد استخدام `withIdempotency_`/`perfTime_` فعليًا في الكود.
- **`tools/perf-scale-test.js`** (تقرير، ليس اختبار نجاح/فشل): تفصيل أعلاه.

**لم يُنفَّذ بعد من نطاق الأداء (يحتاج قرارًا أو عملًا لاحقًا)**:
- دمج `listDevices`/`listAssociations`/`listDelegates` في واجهة صفحاتها
  (جاهزة ومختبَرة خادميًا، الواجهة الأمامية لم تُحوَّل بعد — القرار وسببه
  موثَّق أعلاه بالتفصيل).
- إزالة `beneficiaries` من `getBootstrapData` فعليًا (تبقى مُرسَلة مرتين
  حاليًا) — يتطلّب إعادة كتابة كل موضع في `Index.html` يفترض وجودها محليًا
  (قوائم منسدلة، دوال البحث بالاسم) إلى نمط "بحث/جلب عند الحاجة" (endpoint
  خفيف id+name، مثلًا)، مع اختبار بصري حي غير متاح في هذه الجلسة.
- ترتيب (sort) تفاعلي بالنقر على رأس عمود في الجداول — الخادم يدعم
  `sortBy`/`sortDir` بالكامل (`applySort_`)، لكن لا واجهة مستخدم لاختياره
  بعد (الترتيب الافتراضي فقط مُطبَّق حاليًا).
- اختبار حي فعلي لأي من سلوكيات هذه المرحلة (زمن دخول حقيقي، تعليق واجهة
  حقيقي، ترقيم على بيانات حية) — كل ما سبق مؤكَّد داخل محاكاة Node.js
  فقط. القسم 5.6 الجديد في `RELEASE.md` يُجهِّز قائمة اختبار يدوية له.

## المرحلة الرابعة: توحيد البيانات المرجعية وربط القوائم المنسدلة

**الهدف**: أعطال حية مؤكَّدة — المنطقة/المدينة/التصنيف تظهر كحقول كتابة حرة
رغم وجود بنية مرجعية في الكود (لأن `migrateReferenceData()` لم تُشغَّل بعد
على النسخة الحية)، رقم الهاتف يفقد الصفر الأول عند العرض، وبعض التواريخ
تظهر بصيغة JavaScript إنجليزية تقنية. الإصلاح دون حذف بيانات قديمة أو
افتراض تشغيل أي ترحيل.

### أولًا: عطلان حقيقيان في الكود — تم إصلاحهما عند الجذر
- **فقدان الصفر الأول في الهاتف**: السبب `safeCell_()` (`DataUtils.gs`) لا
  يحمي نصًا رقميًا يبدأ بصفر (`"0501234567"`) من تحويل Apps Script التلقائي
  إلى رقم عند `setValues()`/`setValue()` — تحويل صامت يحذف الصفر الأول
  فعليًا في الخلية المخزَّنة. **الإصلاح عند الجذر**: امتداد `safeCell_()`
  ليسبق أي نص رقمي يبدأ بصفر بعلامة اقتباس (`'`) — نفس آلية "فرض النص"
  المستخدَمة أصلًا لمنع Formula Injection، بلا أي تعديل آخر مطلوب في أي
  موضع كتابة (كل الكتابات تمر عبر `appendObjects_`/`updateById_`/
  `updateRowByMatch_`، وكلها تستخدم `safeCell_` أصلًا). **شبكة أمان إضافية
  للقراءة**: `displayPhone_()` جديدة (`Validation.gs`) تُعيد بناء الصفر
  الأول المرجَّح لبيانات قديمة فسدت *قبل* هذا الإصلاح (قراءة فقط، لا تُعدِّل
  أي قيمة مخزَّنة) — مُستخدَمة في كل موضع يعرض هاتفًا
  (`normalizeBeneficiary_`, `normalizeAssociation_`, `normalizeDelegate_`,
  `normalizeApplication_`).
- **تاريخ JavaScript خام غير منسَّق**: السبب الوحيد `normalizeApplication_`
  (`Applications.gs`) كانت الدالة الوحيدة في كامل المصدر التي تعيد
  `String(row['تاريخ...'])` خامًا بدل `formatDateTime_(parseDate_(...))`
  (النمط المستخدَم في كل مكان آخر) — عندما يخزّن Sheets الخلية كنوع Date
  فعليًا، `String()` على كائن Date الخام ينتج صيغة إنجليزية تقنية مثل
  `"Thu Jan 01 2026 00:00:00 GMT+0300"`. **الإصلاح**: إعادة كتابة
  `normalizeApplication_` لتمرّ بنفس مسار التنسيق العربي بتوقيت
  `Asia/Riyadh` (`APP.timezone`) المستخدَم في كل مكان آخر.

### ثانيًا: مصدر مرجعي موحَّد ومصادقة خادمية لينة (لا تكسر بيانات قديمة)
- **`REFERENCE_SEED_REGIONS_CITIES`** (موجودة مسبقًا، `ReferenceData.gs`):
  13 منطقة سعودية وكل مدنها تتبع منطقتها. لم تُعدَّل بنيتها.
  `REFERENCE_SEED_DEVICE_TYPES`/`SOCIAL_STATUSES`/`ASSOCIATION_CATEGORIES`
  كذلك موجودة مسبقًا ولم تُعدَّل.
  `migrateReferenceData()` (موجودة مسبقًا، مُجهَّزة وغير مُشغَّلة) تبذر ورقة
  "البيانات المرجعية" من هذه الثوابت — لم تُشغَّل هذه المرحلة أيضًا.
- **ثلاثة تحقّقات لينة جديدة** (`ReferenceData.gs`، تُطبَّق نفس مبدأ
  `validateRegionCity_` الموجود مسبقًا): `validateSocialStatus_`
  (إلزامية)، `validateAssociationCategory_` (اختيارية — تصنيف الجمعية
  ليس إلزاميًا كالمنطقة/المدينة/الحالة الاجتماعية)، `validateDeviceType_`
  (إلزامية). **قبل تشغيل `migrateReferenceData()`**: تتصرف كنص حر تمامًا
  كما كانت (`cleanText_`/`requiredText_` فقط) — **توافق كامل صفري الخطر مع
  الوضع الحالي**. **بعد تشغيلها**: تفرض القائمة المعتمدة وترفض القيمة غير
  المعروفة برسالة عربية واضحة — **من الخادم دائمًا، لا من الواجهة فقط**،
  حتى لو استُدعيت الدالة مباشرة متجاوزةً أي نموذج.
- **وُصِّلت في كل مسار كتابة ذي صلة**: `saveBeneficiary`/`importBeneficiaries`/
  معاينة `inspectBeneficiaryExcel` (الحالة الاجتماعية لم تكن تُتحقَّق فيها
  إطلاقًا سابقًا — ثغرة أُغلقت الآن)، `saveAssociation`/
  `submitAssociationApplication` (التصنيف)، `saveDevice` (نوع الجهاز، كانت
  `requiredText_` بلا تحقّق قائمة سابقًا).

### ثالثًا: القوائم المنسدلة المترابطة في الواجهة
- **`regionCityFields()`/`onRegionChange()`** (موجودتان مسبقًا،
  `Index.html`): مترابطتان فعليًا وموصولتان مسبقًا في نموذج تقديم الجمعية
  العام، إضافة/تعديل الجمعية، إضافة/تعديل المستفيد. عند تغيير المنطقة تُصفَّر
  المدينة تلقائيًا وتُقفَل حتى اختيار منطقة (`— اختر المنطقة أولًا —`) —
  كان يعمل مسبقًا، لا يحتاج تعديلًا. **قبل تشغيل الترحيل**: تسقط تلقائيًا
  لحقلي نص حر كما كانت — هذا هو سبب ظهورهما "كنص حر" في النسخة الحية،
  وهو **سلوك متوافق صحيح** بانتظار تشغيل `migrateReferenceData()`، وليس
  عطلًا في هذا الكود.
- **`associationCategoryField()`** (موجودة مسبقًا): نفس المبدأ، موصولة في
  نموذج الجمعية.
- **`deviceTypeField()` (جديدة، `Index.html`)**: الفجوة الوحيدة الفعلية
  التي وُجدت — `deviceForm()` كانت تستخدم `textField('type', ...)` نصًا
  حرًا دائمًا، غير موصولة بـ `ref.deviceTypes` إطلاقًا. أُصلحت الآن بنفس نمط
  `associationCategoryField()` (قائمة بعد الترحيل، نص حر قبله).
- **`socialStatusOptions()`** (موجودة مسبقًا): تستخدم القائمة المعتمدة من
  الخادم بعد الترحيل، وتسقط للثابت المحلي `SOCIAL_STATUSES` قبله.

### رابعًا: البحث والقوائم الطويلة (متطلبات 4/13/14)
- **البحث**: `listBeneficiaries`/`listAssociations` أضيف لهما حقلا
  `region`/`city` ضمن حقول `applySearch_` (كانا مقصورين على
  الاسم/المعرّف/الهاتف) — البحث عن "الرياض" مثلًا يجد الآن مستفيدي/جمعيات
  تلك المنطقة والمدينة أيضًا، بصرف النظر عن كون الحقل قائمة منسدلة أو نصًا
  حرًا (`applySearch_` بحث نصي جزئي case-insensitive في كل الحالات).
- **قوائم طويلة قابلة للبحث/سهلة على الجوال**: لا حاجة لمكوّن جديد —
  `<select>` الأصلي في كل المتصفحات (بما فيها الجوال) يدعم بحث الكتابة
  المباشرة (type-ahead) والتنقّل بالكيبورد افتراضيًا؛ وأكبر قائمة مدن فعليًا
  هي المنطقة الشرقية بعشر مدن فقط (`REFERENCE_SEED_REGIONS_CITIES`) — حجم
  لا يبرر مكوّن بحث مخصَّص إضافي.
- **الوصول بالكيبورد وحالة عدم وجود نتائج**: تم فحصها — `onRegionChange()`
  الموجودة مسبقًا تُقفل قائمة المدينة برسالة عربية واضحة
  (`— اختر المنطقة أولًا —`) حين لا توجد مدن بعد، بدل قائمة فارغة صامتة؛
  كل القوائم عناصر `<select>`/`<input>` قياسية موصولة بالكيبورد افتراضيًا
  بلا أي معالج مخصَّص يكسر ذلك.

### خامسًا: أداتا التشخيص والترحيل الآمن (`ReferenceData.gs`، مُجهَّزتان وغير مُشغَّلتين)
- **`diagnoseReferenceDataIssues()` (قراءة فقط، جديدة)**: تقارن كل
  المستفيدين/الجمعيات/الأجهزة بالقائمة المعتمدة (فقط بعد تشغيل
  `migrateReferenceData()` — قبله تُعيد `ready:false` بلا أي فحص) وتُخرج
  تقريرًا مُصنَّفًا (`{ok, ready, issueCount, bySeverity, byType, issues}`)
  بخمسة أنواع تعارض: `UNKNOWN_REGION`، `CITY_REGION_MISMATCH`،
  `UNKNOWN_SOCIAL_STATUS`، `UNKNOWN_CATEGORY`، `UNKNOWN_DEVICE_TYPE` —
  كل سجل بموقعه الدقيق (الورقة والمعرّف والحقل والقيمة). آمنة للتشغيل
  اليدوي من محرر Apps Script في أي وقت — مُتحقَّق منها اختباريًا أنها لا
  تكتب أي شيء.
- **`migrateLegacyReferenceValues(dryRun)` (جديدة، ⚠️ ترحيل كتابة فعلي،
  **لم تُستدعَ تلقائيًا من أي مكان**)**: `dryRun` افتراضيًا `true` (معاينة
  آمنة بالكامل — بنية الاستدعاء نفسها مصمَّمة بحيث النسيان الافتراضي آمن لا
  خطر). تبني قائمة اقتراحات (`proposals`: القيمة القديمة والجديدة المقترحة
  لكل سجل) ولا تكتب شيئًا إطلاقًا في وضع المعاينة. **لا تُخمِّن أبدًا**: تقترح
  تصحيحًا فقط عند (أ) تطابق حرفي بعد تنظيف المسافات، (ب) مرادف معروف بثقة
  ضمن قائمة قصيرة جدًا (`REFERENCE_LEGACY_REGION_SYNONYMS` — أربعة اختصارات
  شائعة فقط: "مكة"←"مكة المكرمة"، "المدينة"←"المدينة المنورة"،
  "الشرقيه"←"الشرقية"، "الجوف "←"الجوف")، أو (ج) مدينة تتبع منطقة واحدة فقط
  بلا غموض في الفهرس العكسي. أي حالة غير ذلك (حالة اجتماعية/تصنيف/نوع جهاز
  غير معروف بلا مرادف، أو منطقة/مدينة غامضة كليًا) تُدرَج في `ambiguous[]`
  مع سبب واضح **دون أي تعديل مقترَح لها إطلاقًا** — تحتاج مراجعة يدوية دائمًا،
  لا تخمينًا. عند `dryRun=false`: تطبّق فقط التعديلات المؤكَّدة (لا الغامضة).
  **قابلة لإعادة التشغيل بأمان بلا تكرار أو إفساد**: بعد تطبيق تصحيح، القيمة
  تصبح مطابقة للقائمة المعتمدة فتُستبعَد تلقائيًا من `proposals` في أي
  تشغيل تالٍ (آلية طبيعية عبر `newValue === oldValue`، لا بحاجة سجل "تم
  الترحيل" منفصل) — مُتحقَّق منه اختباريًا بتشغيلَين متتاليَين على نفس
  البيانات.

### سادسًا: التوافق مع البيانات القديمة
لا يعتمد النظام على تشغيل أي ترحيل ليعمل: كل حقل مرجعي (منطقة/مدينة/حالة
اجتماعية/تصنيف/نوع جهاز) له **سقوط آمن (fallback) مزدوج** — في الواجهة
(`regionCityFields`/`associationCategoryField`/`deviceTypeField`/
`socialStatusOptions` تسقط لحقل نص حر أو ثابت محلي إن لم تكن
`getReferenceData().ready`)، وفي الخادم (كل `validate*_` الأربعة تتصرف
كنص حر/مطلوب فقط دون فرض قائمة قبل الترحيل). لا حذف ولا استبدال تلقائي لأي
قيمة قديمة في أي مسار — التصحيح الوحيد الممكن هو عبر `migrateLegacyReferenceValues`
الصريح وغير المُشغَّل.

### اختبارات جديدة (`tools/reference-test.js`, 60 تأكيدًا)
عشرة أقسام تطابق كل بند مطلوب صراحة: ترابط المنطقة/المدينة، رفض مدينة لا
تتبع منطقتها، التوافق قبل الترحيل + اكتشاف القيم القديمة/غير المعروفة
بعده (بخمسة أنواع تعارض)، وضع dry-run لا يعدّل أي بيانات (بمقارنة لقطة
الجدول قبل/بعد الاستدعاء حرفيًا)، الترحيل الفعلي قابل لإعادة التشغيل بأمان
(تشغيلان متتاليان: الأول يطبّق، الثاني لا يقترح شيئًا جديدًا ولا يفسد
السجل الغامض)، تقديم الجمعية العام وحفظ المستفيد والاستيراد الجماعي عبر
التحقق الموحَّد (نجاح ورفض)، حفظ الصفر الأول في الهاتف (كتابة وقراءة فعليتان
عبر `safeCell_`/`displayPhone_`)، تنسيق التاريخ العربي بتوقيت `Asia/Riyadh`
بلا أي أثر إنجليزي تقني (`GMT`/أسماء أيام)، عزل صلاحيات الجمعيات (بلا
تراجع بعد إضافة التحقق المرجعي)، وفحوص ثابتة تتأكد أن `repairStateIntegrityIssues`/
`migrateLegacyReferenceValues`/`diagnoseReferenceDataIssues` لا تزال غير
مُستدعاة تلقائيًا من أي مكان.

**لم يُنفَّذ بعد من نطاق هذه المرحلة (يحتاج قرارًا أو عملًا لاحقًا)**:
- **اختبار حي فعلي**: كل ما سبق مؤكَّد داخل محاكاة Node.js فقط — لا وصول
  لـ Apps Script حقيقي من بيئة هذه الجلسة (نفس القيد الموثَّق في المراحل
  السابقة).
- **تشغيل `migrateReferenceData()`/`migrateLegacyReferenceValues()` على أي
  بيانات حقيقية**: بانتظار مراجعتك وموافقتك الصريحة، بالترتيب: تشغيل
  `migrateReferenceData()` أولًا، ثم `diagnoseReferenceDataIssues()`
  لمراجعة حجم القيم القديمة الفعلي، ثم `migrateLegacyReferenceValues(true)`
  لمعاينة الاقتراحات، ثم قرارك بتشغيل `migrateLegacyReferenceValues(false)`
  أو تركه.
- **فلاتر Dropdown مستقلة بالمنطقة/المدينة في شاشات القوائم**: البحث النصي
  يغطي المنطقة/المدينة الآن (أعلاه)، لكن لا يوجد قائمة تصفية منفصلة بجانب
  مربع البحث — قرار تصميم واجهة إضافي يحتاج طلبًا صريحًا إن رغبت به.

## المرحلة الخامسة: الموقع الجغرافي للمستفيد، الاستيراد الجماعي، وأدوات المندوب ومسار اليوم

**الهدف**: اختيار موقع بصري سهل للمستفيد (فوق الإدخال اليدوي القائم، لا
بدلًا عنه)، تحقّق خادمي حقيقي للإحداثيات كأرقام لا نص، دعم الإحداثيات في
الاستيراد الجماعي مع معاينة واضحة، معالجة موثوقة لأزرار الاتصال/واتساب/
الموقع لدى المندوب مع بدائل عند الحجب، ومسار يوم مرتَّب مفيد — كل ذلك
بلا أي خدمة خرائط أو مفتاح API مدفوع.

### أولًا: بيانات موقع المستفيد — نموذج موحَّد ومصدر حقيقة واحد
- **أعمدة جديدة في ورقة "المستفيدون"** (إضافية بالكامل، لا تمس أي عمود
  قائم): "علامة مميزة" (وصف حر اختياري، كـ"بجانب المسجد")، "مصدر الموقع"
  (خريطة/الموقع الحالي/استيراد/يدوي)، "تاريخ تحديث الموقع". المنطقة
  والمدينة والعنوان وخطا العرض/الطول كانت موجودة مسبقًا ولم تتغيّر بنيتها.
- **`optionalCoordinate_` (`Validation.gs`) أُعيدت كتابتها بالكامل**: كانت
  تُحصر ضمن مربع تقريبي لحدود السعودية فقط (15–33/33–56) وتُسقط أي طرف
  ناقص من الزوج بصمت. الآن: **نطاق عالمي قياسي** (خط العرض -90..90، خط
  الطول -180..180 — تعديل سلوك مقصود بناءً على طلب هذه المرحلة صراحة)،
  **ترفض NaN/Infinity/أي نص غير رقمي** عبر `isFinite`، **ترفض طرفًا واحدًا
  من الزوج بلا الآخر صراحة** برسالة عربية واضحة بدل إسقاطه بصمت كما كان.
  لا تزال الإحداثيات فارغتان معًا حالة صالحة تمامًا (توافق كامل مع بيانات
  قديمة/مستفيدين بلا موقع).
- **`validateLocationSource_` (جديدة)**: فارغة دائمًا بلا إحداثيات (لا معنى
  لمصدر بلا موقع)، تصحِّح قيمة غير معروفة إلى "يدوي" بدل الرفض (حقل وصفي
  مساعد فقط، لا يستحق صرامة حقول التحقق الإلزامية).
- **`saveBeneficiary` (`Beneficiaries.gs`)**: لا يُحدِّث "مصدر الموقع"/"تاريخ
  تحديث الموقع" إلا إذا **تغيّرت الإحداثيات فعليًا** بهذا الحفظ (أو سُجِّلت
  لأول مرة) — تعديل حقل آخر لا علاقة له بالموقع (كالملاحظات) لا يُعيد ضبط
  "آخر تحديث للموقع" زورًا. عند مسح الإحداثيات يُمسَح المصدر والتاريخ معهما.
- **`importBeneficiaries`**: "مصدر الموقع" يُضبَط تلقائيًا إلى "استيراد"
  عند وجود إحداثيات (لا يُترَك للمستخدم اختياره في هذا المسار تحديدًا).
- **لا استنتاج ولا تعديل تلقائي لبيانات قديمة**: كل هذا يعمل فقط عند حفظ
  أو استيراد صريح جديد؛ لا دالة ترحيل تلمس السجلات القائمة.

### ثانيًا: اختيار الموقع بصريًا — Leaflet + OpenStreetMap، بلا مفتاح API
- **القرار التقني**: Leaflet.js مع بلاطات OpenStreetMap — مكتبة ومصدر
  بيانات مجانيان بالكامل، بلا تسجيل ولا مفتاح API ولا حد استخدام مدفوع،
  ومتوافقان مع وضع `IFRAME` الحالي لـ`HtmlService` (لا يعتمدان على
  `postMessage` عبر أصل مختلف ولا أي امتياز CSP خاص غير تحميل سكربت/صورة
  خارجيَين عاديين، وهو مسموح افتراضيًا في تلك الوضعية). البديل المدفوع
  (Google Maps JavaScript API) استُبعِد فورًا لأنه يتطلّب مفتاح فوترة —
  ممنوع صراحة بهذه المرحلة.
- **تحميل كسول حصريًا عند فتح نموذج المستفيد**: `initLocationPicker()`
  تُستدعى فقط من `onMount` الممرَّر لـ`openModal()` عند `beneficiaryForm()`
  تحديدًا — لا في شاشة الدخول ولا لوحة الإدارة ولا أي صفحة أخرى
  (`openModal` أُضيف لها وسيط رابع اختياري `onMount` جديد لهذا الغرض بالضبط).
- **سقوط آمن كامل عند فشل التحميل**: `loadLeaflet_()` بمهلة صريحة (6
  ثوانٍ) — أي خلل (شبكة، CSP، حظر إعلانات) يتحوّل دائمًا لرفض Promise
  ملتقَط في `initLocationPicker`، لا استثناءً متزامنًا يكسر فتح النموذج
  (مُتحقَّق منه اختباريًا بمحاكاة بيئة بلا `document.head`). عند الفشل:
  `showMapFallback_()` تُخفي حاوية الخريطة فقط وتعرض رسالة عربية واضحة —
  **حقول العنوان، الإدخال اليدوي لخط العرض/الطول، زر "استخدم موقعي
  الحالي"، وزر "فتح في خرائط جوجل" تبقى تعمل جميعًا بلا أي اعتماد على
  نجاح تحميل الخريطة**، ولا شيء يمنع حفظ بقية بيانات المستفيد.
- **الوظائف المطلوبة كلها مُنفَّذة**: عرض الموقع الحالي إن وُجدت إحداثيات
  (مركز الخريطة + علامة عند فتح النموذج)، تحديد/تحديث نقطة بالنقر على
  الخريطة أو سحب العلامة (`setLocationFromLatLng`)، زر "استخدم موقعي
  الحالي" (يُزامن الخريطة والحقول اليدوية والمصدر معًا)، عرض الإحداثيات
  المختارة نصيًا بوضوح (`#locationCoordsHint`)، زر "مسح الموقع"
  (`clearLocationFields`)، وزر "فتح في خرائط جوجل" (`openLocationInMaps`
  — بالإحداثيات إن وُجدت، وإلا بالعنوان النصي، وإلا تنبيه واضح بدل رابط
  فارغ تمامًا كما طُلب).
- **الإدخال اليدوي بديل دائم لا احتياطي مخفي**: حقلا خط العرض/الطول داخل
  `<details>` "إدخال يدوي للإحداثيات" — مرئيان وقابلان للاستخدام سواء
  نجحت الخريطة أم لا، ونطاقهما HTML (`min`/`max`) أصبح `-90..90`/`-180..180`
  ليطابق التحقق الخادمي الجديد.

### ثالثًا: الاستيراد الجماعي
- **CSV (`parseCsv`، عميل)**: عمود "علامة مميزة" اختياري جديد في نهاية
  الترتيب (بعد خط الطول) — ملفات قديمة بلا هذا العمود تعمل كما كانت
  تمامًا (خلية فارغة).
- **Excel (`inspectBeneficiaryExcel`، خادم)**: `keyMap` وسِّع ليقبل
  مرادفات إنجليزية شائعة لخط العرض/الطول (`Latitude`/`Longitude`/
  `Lat`/`Lng`، تصدرها بعض تطبيقات GPS) وعمود "العلامة المميزة"/`Landmark`
  الاختياري. كل صف يُعيد الآن `row`(رقم الصف)، `valid`، و`error` (إن
  وُجد) — لا فقط اسمًا مقروءًا من الأعمدة.
- **معاينة حقيقية في الواجهة**: نافذة الاستيراد الجماعي تعرض الآن جدول
  معاينة (`renderBulkPreviewTable`) بكل صف: رقم الصف، الاسم، الإحداثيات
  المقروءة (أو "—")، وحالته (✓ صحيح / ✗ خاطئ مع سبب الخطأ نصيًا) — لملف
  Excel تحديدًا (مسار CSV يبقى بملخّص مختصر فقط، موضَّح صراحة في رسالة
  الواجهة أنه بلا معاينة مسبقة للأخطاء لأنه لا يمر بجولة خادم قبل الاستيراد).
- **"لا تستورد الصفوف الخاطئة" — منفَّذ فعليًا لمسار Excel**: بعد المعاينة،
  `state.pendingRows` تُصفَّى لتحوي فقط الصفوف الصحيحة (`row.valid !== false`)
  قبل استدعاء `importBeneficiaries` — الصفوف الخاطئة تبقى معروضة في
  الجدول برقمها وسببها، ولا تُرسَل للاستيراد إطلاقًا.
- **الصفر الأول في الجوال وحقن CSV/الصيغ**: لم يتغيّرا في هذه المرحلة —
  محميان مسبقًا (`safeCell_`/`displayPhone_` من المرحلة الرابعة)، ومختبَران
  الآن أيضًا ضمن مسارات الاستيراد الجديدة.

### رابعًا: أدوات المندوب — اتصال، واتساب، موقع، نسخ
- **`beneficiaryContactActions(item)` (جديدة، `Index.html`)**: دالة موحَّدة
  تُستخدَم الآن في قائمة المهام ومسار اليوم كلاهما (كانتا مكرَّرتين
  سابقًا)، تنتج دائمًا: زر واتساب (صيغة دولية عبر `phoneIntl` — موجودة
  مسبقًا)، زر اتصال (`tel:` + `call-fallback` الموجود مسبقًا لحجب iframe)،
  **زر "نسخ الرقم" صريح جديد** (`handleCopyPhone`، لا ينتظر فشل الاتصال
  أولًا كما كان الحال مع بديل النسخ التلقائي القديم)، وزر موقع **محروس**:
  يُعرَض كرابط خرائط حقيقي فقط إن وُجدت إحداثيات أو عنوان/مدينة نصية،
  وإلا زر `no-location-alert` يعرض تنبيهًا عربيًا واضحًا بدل فتح رابط فارغ
  (السلوك القديم كان يبني رابطًا بمُدخلات فارغة دون حراسة).
- **العلامة المميزة تظهر في عنوان بطاقة المستفيد** لدى المندوب
  (`beneficiaryAddressLine`) إن وُجدت.
- **الصلاحيات**: لم تتغيّر — `listBeneficiaries`/بيانات بوابة المندوب
  مقيَّدة بمستفيدي المندوب المُسنَدين إليه فقط، كما كانت (مُتحقَّق منه
  اختباريًا في هذه المرحلة أيضًا ضمن قسم عزل الجمعيات).

### خامسًا: مسار يوم المندوب
- **الترتيب**: خوارزمية "أقرب جار" بصيغة Haversine (موجودة مسبقًا من
  المرحلة الأولى، غير مدفوعة، لا تغيّرت رياضيًا) — لم تُعَد كتابتها، فقط
  **البطاقات المعروضة لكل محطة أُثريَت بالكامل**: الاسم، المدينة، العنوان
  (مع العلامة المميزة)، الأجهزة المخصَّصة (كانت غائبة عن بطاقات المسار
  سابقًا)، المسافة التقريبية، وأزرار اتصال/واتساب/نسخ/موقع (عبر
  `beneficiaryContactActions` نفسها)، وحالة التسليم.
- **الإفصاح الصريح المطلوب**: نص المقدّمة ووسم كل محطة يقولان الآن حرفيًا
  "مسافة تقريبية بخط مستقيم" — لا ادّعاء بتوجيه مروري فعلي في أي مكان.
- **المستفيدون بلا إحداثيات**: يظهرون في قسم منفصل بعد المحطات المرتَّبة
  كما كان، **مع نفس أزرار التواصل الكاملة الآن** (لم تكن موجودة في تلك
  البطاقات المصغَّرة سابقًا) — لا يُستبعَدون من أي شيء، فقط خارج الترتيب
  التلقائي بالمسافة.
- **تحديث دون إعادة تحميل كامل — إصلاح خلل خفي حقيقي**: `upsertRecord`
  تستبدل مرجع كائن المستفيد بالكامل عند كل حفظ ناجح (لا تُعدِّله في مكانه)؛
  هذا كان يعني أن بطاقة محطة في `state.delegateRoute.ordered` المبنية من
  المرجع القديم **تبقى تعرض حالة تسليم قديمة** بعد تأكيد التسليم أو تسجيل
  تعذّره حتى لو أُعيد رسم الصفحة. الإصلاح: كل بطاقة محطة تُعيد قراءة حالة
  التسليم/الأجهزة الحيّة عبر `findBeneficiary(id)` عند كل رسم، مع الإبقاء
  على الموقع/العنوان/المسافة كما حُسبت وقت بناء المسار (لا داعي لإعادة
  حساب الترتيب لمجرد تحديث حالة). محطة مُسلَّمة تُعطَّل زر تأكيدها تلقائيًا
  (منع تكرار التأكيد) وتُعطَّل زر "تعذّر" أيضًا، وتُميَّز بصريًا (`dg-card-done`).
- **لا تتبّع خلفي لموقع المندوب**: الموقع يُطلَب فقط عند ضغط "ترتيب المسار
  من موقعي الحالي" صراحة (`startDelegateRoute`)، ولا يُخزَّن ولا يُرسَل
  للخادم إطلاقًا (يبقى في `state.delegateRoute` بالمتصفح فقط لهذه الجلسة).

### سادسًا: الخصوصية والأمان
- إذن الموقع يُطلَب فقط بضغط المستخدم على "استخدم موقعي الحالي" أو "ترتيب
  المسار من موقعي الحالي" — لا في شاشة الدخول ولا `restoreSession` ولا أي
  تحميل تلقائي (مُتحقَّق منه اختباريًا بفحص أن كود الدخول واستعادة الجلسة
  لا يحتوي `geolocation` إطلاقًا، وأن استدعاءات `getCurrentPosition` كلها
  محصورة في معالجَي هذين الزرين بالضبط).
- `confirmDelivery`/`updateDeliveryStatus` لا يكتبان أي إحداثيات أو موقع
  حي للمندوب في سجل التسليمات أو سجل العمليات — مُتحقَّق منه اختباريًا.
- التحقق من الإحداثيات مفروض بالكامل من الخادم (`optionalCoordinate_` في
  `saveBeneficiary`/`importBeneficiaries`/`inspectBeneficiaryExcel`) — لا
  اعتماد على تحقق الواجهة فقط، حتى لو استُدعيت الدالة مباشرة متجاوزةً
  النموذج.
- حقل "علامة مميزة" الجديد يُخزَّن كنص خام محدود الطول (`cleanText_`، 200
  حرفًا) بلا تنفيذ HTML — الهروب الفعلي في الواجهة عبر `esc()` كما كل حقل
  نصي آخر (مُتحقَّق منه بمحاولة حقن `<img onerror>` فعلية في `security-test.js`).
- كل رابط خارجي جديد (`open-location-maps`, روابط الخرائط/واتساب في
  بطاقات المندوب) يستخدم `target="_blank" rel="noopener noreferrer"` أو
  `window.open(..., 'noopener,noreferrer')` مع تصفير `opener` صراحةً.

### سابعًا: الأداء والتوافق
- Leaflet لا يُحمَّل إطلاقًا إلا عند فتح نموذج موقع مستفيد تحديدًا — صفر
  تأثير على شاشة الدخول أو لوحة الإدارة أو أي صفحة أخرى.
- لم تُمَس مهلة `API_TIMEOUT_MS` (25 ثانية) ولا آلية الاستجابات الجزئية
  بعد الحفظ (`record`/`summary` بدل Bootstrap كامل) من `commit 0241d09` —
  `saveBeneficiary` لا تزال تُعيد فقط `{record, summary}`.
- RTL والجوال ولوحة المفاتيح: مراجعة كودية (لا اختبار متصفح حي) — كل
  الأزرار الجديدة عناصر `<button>`/`<a>` قياسية ضمن `dg-acts`/`location-acts`
  القابلة للتنقّل بالكيبورد ولمس الجوال افتراضيًا، بلا أي `tabindex` سلبي
  أو معالج يكسر التنقّل الافتراضي.

### اختبارات جديدة/مُوسَّعة
- **`tools/server-test.js`** (+18 تأكيدًا، الإجمالي 146): حدود الإحداثيات
  العالمية الجديدة (-90/90 و-180/180، بما فيها القيم الحدّية بالضبط)،
  رفض NaN/Infinity، رفض القيمة الجزئية (طرف واحد بلا الآخر)،
  `validateLocationSource_`، مصدر/تاريخ تحديث الموقع في `saveBeneficiary`
  (يُضبَط عند التغيير فقط، لا عند إعادة حفظ حقل آخر)، ومصدر "استيراد"
  التلقائي في `importBeneficiaries`.
- **`tools/smoke.js`** (+18 تأكيدًا، الإجمالي 148): حقل العلامة المميزة
  وحاوية الخريطة وأزرارها في نموذج المستفيد، `initLocationPicker` لا يفشل
  بلا شبكة، `setLocationFromLatLng`/`clearLocationFields`/
  `openLocationInMaps` (بالإحداثيات، بالعنوان، وبلا أي منهما — تنبيه لا
  رابط فارغ)، ظهور العلامة المميزة وزر النسخ في بطاقات المندوب، حراسة زر
  الموقع بلا عنوان ولا إحداثيات، إثراء بطاقات مسار اليوم (تواصل كامل
  وأجهزة)، **تحديث بطاقة المسار فورًا بعد استبدال مرجع الكائن (محاكاة
  upsertRecord) دون إعادة حساب المسار**، ومعاينة استيراد Excel (رقم الصف،
  الإحداثيات المقروءة، سبب الخطأ، التمييز البصري بين الصحيح والخاطئ).
- **`tools/security-test.js`** (+8 تأكيدات، الإجمالي 87): حقن HTML عبر
  العلامة المميزة، عدم طلب الموقع تلقائيًا عند الدخول، حصر استدعاءات
  `getCurrentPosition` باثنين فقط (الزرّان الصريحان)، عدم تسريب إحداثيات
  حيّة في سجل العمليات/التسليمات.
- محاكاة Sheets في أدوات الاختبار (`ALL_HEADERS`) حُدِّثت في كل الملفات
  السبعة ذات الصلة لتضمين الأعمدة الثلاثة الجديدة.

**لم يُنفَّذ بعد من نطاق هذه المرحلة (يحتاج قرارًا أو عملًا لاحقًا)**:
- **اختبار خريطة/جوال حي فعلي**: كل ما سبق مؤكَّد داخل محاكاة Node.js
  `vm` (بلا DOM حقيقي ولا شبكة فعلية لتحميل Leaflet/بلاطات OSM) — لم
  تُفتَح الخريطة فعليًا في أي متصفح حقيقي من هذه الجلسة، ولم يُختبَر
  `tel:`/واتساب على جهاز جوال حقيقي. القيد نفسه الموثَّق في كل مرحلة سابقة
  (لا وصول لـ Apps Script حي من بيئة التنفيذ).
- **تخزين مؤقت محلي لبلاطات الخريطة (Service Worker/Cache)**: غير منفَّذ
  — عند شبكة بطيئة تتكرر التحميل في كل فتح نموذج بدل التخزين الدائم؛ تحسين
  ممكن لاحقًا إن ثبتت الحاجة الفعلية بعد استخدام حي.
  المكتبة نفسها تُحمَّل مرة واحدة فقط لكل جلسة عبر `window.L`/`data-src`.
- **فلاتر بحث Dropdown منفصلة بالمنطقة/المدينة في شاشات القوائم**: البحث
  النصي يغطي المنطقة/المدينة (من المرحلة الرابعة)، ولم يُضَف فلتر Dropdown
  مستقل بجانب مربع البحث في هذه المرحلة أيضًا.
- **قالب استيراد قابل للتنزيل**: النظام لا يزال يعتمد على وصف نصي للأعمدة
  المطلوبة في نافذة الاستيراد بدل ملف نموذج `.xlsx`/`.csv` جاهز للتنزيل —
  لم يُطلَب صراحة وقد يكون تحسينًا مفيدًا لاحقًا.
- **تغييرات مخطط البيانات لم تُرحَّل بعد**: الأعمدة الثلاثة الجديدة في
  "المستفيدون" ("علامة مميزة"، "مصدر الموقع"، "تاريخ تحديث الموقع")
  ستُنشأ تلقائيًا فقط عند تشغيل `setupSheets()` (آمنة، إضافية بالكامل، لا
  تمس أي عمود قائم) — **لم يُشغَّل `setupSheets()` على أي بيانات حقيقية**.

## المرحلة السادسة: استكمال بوابات الإدارة والجمعية والمندوب وربط دورة العمل الكاملة

**الهدف**: إغلاق نواقص وظيفية مؤكَّدة اكتُشفت بمراجعة شاملة للنظام — أهمها
أن `listBeneficiaries`/`listAssociations`/`listDevices`/`listDelegates`
(جاهزة ومختبَرة خادميًا منذ المرحلة الثالثة) **لم تكن موصولة بالواجهة
إطلاقًا رغم توثيق سابق يزعم عكس ذلك** — كل الصفحات كانت تعرض المصفوفة
الكاملة من Bootstrap مُصفّاة محليًا فقط. صُحِّح هذا التوثيق هنا صراحةً.

### أولًا: عطلان حقيقيان وُجدا أثناء المراجعة — أُصلحا عند الجذر
- **إضافة مستفيد من حساب الإدارة كانت مكسورة فعليًا**: `beneficiaryForm()`
  لا تعرض حقل اختيار الجمعية للإدارة، فيصل `associationId` فارغًا دائمًا
  للخادم، الذي يرفضه برسالة "اختر جمعية صحيحة" — أي محاولة إدارية لإضافة
  مستفيد كانت تفشل دائمًا. **الإصلاح**: `beneficiaryAssociationField()`
  جديدة، حقل اختيار جمعية إلزامي يظهر فقط للإدارة (`ADMIN`)؛ الجمعية نفسها
  لا تحتاجه لأن الخادم يُقيِّدها بجمعيتها ضمنيًا.
- **ذاكرة Bootstrap المؤقتة لم تكن تُبطَل فعليًا إلا للإدارة**:
  `clearDashboardCache()` كانت تحذف مفتاح `'bootstrap:ADMIN'` فقط رغم أن
  كل مسار حفظ (بما فيها حفظ مستفيد/جهاز/استيراد جماعي من حساب جمعية) يستدعيها
  معتقدًا أنها تُبطل ذاكرته الخاصة أيضًا — فتبقى لوحة بيانات الجمعية أو
  المندوب تعرض أرقامًا قديمة حتى انتهاء `APP.cacheSeconds` (60 ثانية) رغم
  أن القوائم (`listBeneficiaries` وغيرها) تعرض البيانات الصحيحة فورًا من
  نفس اللحظة — **مخالفة مباشرة لمطلب "راجع الأرقام المحسوبة في لوحات
  البيانات وتأكد أنها ناتجة من المصدر نفسه"**. **الإصلاح**: جيل مشترك
  (`dashboardCacheGeneration_`، نفس مبدأ `actorEpoch_` المُستخدَم لإبطال
  الجلسات) — `clearDashboardCache()` ترفع الجيل فتُبطل كل مفاتيح Bootstrap
  (إدارة/كل جمعية/كل مندوب) دفعة واحدة بصرف النظر عن الفاعل، بدل محاولة
  تتبّع كل مفتاح على حدة عبر 13 موضع استدعاء مختلفًا.
- **حساب جمعية جديد (من قبول طلب انضمام أو إضافة إدارية مباشرة) لم يكن
  يُفرَض عليه تغيير كلمة المرور عند أول دخول** — كان الإلزام مُطبَّقًا فقط
  على مسار *إعادة التعيين* الصريح (المرحلة الثانية)، وكان هذا موثَّقًا
  صراحة كنقص معروف في `HANDOFF.md`. **الإصلاح**: `createAssociationUser_`
  (`DevicesAssociations.gs`) تضبط الآن `'يجب تغيير كلمة المرور': 'نعم'`
  دائمًا — سواء كانت كلمة مرور مؤقتة مُولَّدة تلقائيًا أو اختارها المدير
  يدويًا، فكلتاهما تصل الجمعية من خارجها لا من اختيارها هي، بنفس آلية
  `resetAssociationPassword` القائمة (شاشة تغيير إلزامية، لا bootstrap قبلها).

### ثانيًا: بوابة الإدارة
- **مؤشرات لوحة البيانات**: كل مؤشر كان بالفعل مربوطًا بـ`data-page`/
  `data-filter` (من مرحلة سابقة)، لكن `kpi-nav` كانت تستدعي `goTo(page)`
  ثم تضبط `state.filter` **بعد** الجلب — فلا يصل الفلتر للخادم فعليًا بعد
  ربط القوائم الأربع بالترقيم الخادمي هذه المرحلة. أُصلح بتمرير الفلتر إلى
  `goTo(page, filter)` مباشرة فيُرسَل مع طلب الصفحة الأولى نفسه.
- **ثلاثة أقسام تشغيلية للأنشطة في لوحة البيانات (جديد)**:
  `renderDashboardActivitySections()` — مكتمل (مع رابط الشاهد إن وُجد)،
  جارٍ حاليًا (بنسبة التقدّم)، والمهمة القادمة (أقرب نشاط بحالة "لم يبدأ"
  حسب موعد النهاية، مع "بعد N يومًا"/"تجاوز الموعد بـ N يومًا" — `daysFromToday()`
  جديدة). كل عنصر قابل للنقر ويفتح نموذج تعديل النشاط (`edit-activity`
  الموجودة مسبقًا). تُبنى من نفس `getActivitiesBundle` المُستخدَم في صفحة
  "متابعة المشروع" (طلب شبكة واحد يُعاد استخدامه، يُجلَب عند الدخول/استعادة
  الجلسة للإدارة فقط). الأنشطة المتأخرة تبقى بنفس تصميم `.alert.critical`
  الموجود مسبقًا (لا ألوان جديدة مبالَغ فيها).
- **صفحة تفاصيل الجمعية أُثريت**: كانت بيانات أساسية + زر إعادة تعيين
  كلمة المرور فقط. أُضيف: عيّنة من مستفيدي الجمعية ومناديبها (بحالتهم)،
  وأزرار "عرض الكل ←" تنتقل لقوائم المستفيدين/المناديب/الأجهزة/سجل
  العمليات **مفلترة بهذه الجمعية تحديدًا** (`goToAssociationScope`، يستخدم
  فلتر الجمعية الجديد)، وزر "تعديل البيانات" (لم يكن موجودًا في هذه
  النافذة، فقط من قائمة الجمعيات).
- **صفحة تفاصيل المندوب**: كانت تعرض البيانات والمهام الحالية فقط (من
  مرحلة سابقة). أُضيف زرا "رمز دخول جديد"/"تفعيل أو تعطيل" مباشرة في
  نافذة التفاصيل (كانا مقصورين على بطاقة القائمة). **سجل عمليات مندوب
  محدَّد لم يُنفَّذ** — يحتاج دالة خادم جديدة لتصفية سجل العمليات بمعرّف
  الفاعل (غير موجودة حاليًا)؛ موثَّق أدناه كبند متبقٍ.
- **فلتر الجمعية العام للإدارة (جديد)**: `assocFilterSelect()` يظهر في
  المستفيدين/الأجهزة/المناديب/سجل العمليات — يستخدم `options.associationId`
  الجاهز خادميًا مسبقًا في كل هذه الدوال.
- **ترقيم/بحث/فلترة/ترتيب خادمي حقيقي — الإصلاح البنيوي الأكبر**:
  `listBeneficiaries`/`listAssociations`/`listDevices`/`listDelegates`
  وُصِلت أخيرًا بـ`LAZY_PAGES`/`fetchLazyPage` (نفس نمط سجل العمليات
  وطلبات الانضمام تمامًا). `state.data.beneficiaries/associations/devices/delegates`
  (المصفوفات الكاملة من Bootstrap) **بقيت كما هي** لاستخدامها في القوائم
  المنسدلة والبحث بالاسم (`findBeneficiary` وما شابه) — تغيير بأقل مخاطرة
  ممكنة بدل إعادة هيكلة شاملة غير مُتحقَّق منها بصريًا، كما أوصت `HANDOFF.md`
  من مرحلة سابقة صراحة.
- **`upsertEntity()` جديدة**: كل حفظ/تفعيل/تعطيل كان يُحدِّث `state.data.X`
  فقط عبر `upsertRecord` — بعد ربط الترقيم الخادمي، القائمة المعروضة فعليًا
  تُقرَأ من `state.lazy.X.items` منفصلة، فتبقى تعرض حالة قديمة بعد أي
  إجراء رغم إعادة الرسم (لأن `upsertRecord` تستبدل مرجع الكائن لا تُعدِّله).
  `upsertEntity(key, record)` تُزامن كلا المصدرين معًا؛ كل موضع استدعاء
  (٩ مواضع) صُحِّح ليستخدمها.
- **ترتيب بعناوين الأعمدة**: `sortableTh()` جديدة للجدول الوحيد الحقيقي
  (الأجهزة). القوائم الأخرى بطاقات لا جداول، فاستُخدمت قائمة "ترتيب حسب"
  منسدلة (`sortSelect()`) بدلًا من ذلك — أنسب للواجهة كما طُلب صراحة.

### ثالثًا: بوابة تقديم الجمعيات
راجُعت كاملة ووُجدت مكتملة فعليًا من مراحل سابقة (القوائم المرجعية،
التحقق من الهاتف/البريد/التكرار خادميًا، رقم الطلب المعروض، رسالة نجاح
رسمية، القبول/الرفض بسبب رفض إلزامي، منع تكرار إنشاء الجمعية عبر
`withIdempotency_`، عدم تسجيل السرّ، `resetAssociationPassword` كوسيلة
استعادة، تنسيق التاريخ بالعربية بتوقيت الرياض) — **البند الوحيد الناقص
فعليًا كان إجبار تغيير كلمة المرور المؤقتة عند أول دخول**، وهو مُصلَح
الآن (أعلاه).

### رابعًا: بوابة الجمعية
- **العزل الخادمي**: رُوجعت كل دالة (`listBeneficiaries`/`saveBeneficiary`/
  `assignDelegate`/`listDelegates`/`listDevices`/`listAuditLog` وغيرها) —
  كلها تُقيِّد `ASSOCIATION` بـ`user.associationId` من الجلسة، لا من أي
  مُدخَل عميل. **مُتحقَّق منه الآن اختباريًا عبر رحلة تكامل كاملة بجمعيتين
  حقيقيتين** (`tools/integration-test.js`، القسم 13) — جمعية لا تستطيع رؤية
  ولا تعديل ولا تعيين مندوب لمستفيد جمعية أخرى حتى باستدعاء API مباشر.
- **لوحة المؤشرات**: كانت مكتملة أصلًا (مستفيدون، أجهزة معتمدة/مستلمة/
  مسلَّمة، نسبة إنجاز، مناديب، آخر عمليات) — الإصلاح الوحيد هنا هو تصحيح
  ذاكرة Bootstrap المؤقتة أعلاه، الذي كان يجعل هذه الأرقام تحديدًا تتأخر
  عن التحديث الفعلي.
- **الاستيراد الجماعي**: أُضيف قالب CSV قابل للتنزيل (`buildImportTemplateCsv`/
  `downloadImportTemplate`) بعمود مثال كامل (بما فيه الإحداثيات والعلامة
  المميزة)، بادئة BOM لفتح Excel للعربية بترميز صحيح دون خطوات يدوية.
  التعليمات النصية حُدِّثت لذكر مرادفات الأعمدة الإنجليزية واشتراط ظهور
  الإحداثيتين معًا (من المرحلة الخامسة). **"لا تُستورَد الصفوف الخاطئة
  بصمت" مُنفَّذ فعليًا الآن لمسار Excel تحديدًا**: بعد المعاينة، الصفوف
  الصحيحة فقط تُرسَل لـ`importBeneficiaries` الفعلي؛ الخاطئة تبقى ظاهرة
  في جدول المعاينة برقمها وسببها دون استيراد (من المرحلة الخامسة، مؤكَّد
  هنا ضمن السياق الكامل). **الصفوف المكرَّرة**: لا يوجد تحقّق من تكرار
  رقم جوال داخل الملف نفسه أو ضد مستفيدين قائمين — بند متبقٍ (راجع القسم
  أدناه).
- **إدارة المناديب**: الإضافة/التفاصيل/التعديل/التعطيل والتفعيل/إعادة
  إنشاء الرمز/تعيين المستفيدين — كلها كانت موجودة من مراحل سابقة ومكتملة.
- **عرض الأجهزة**: الجمعية تراها في جدول للقراءة فقط (لا زر إضافة/حذف
  يظهر لها) — كان صحيحًا مسبقًا، رُوجع ومؤكَّد.
- **الإعدادات وتغيير كلمة المرور وتسجيل الخروج**: مكتملة من مراحل سابقة.

### خامسًا: بوابة المندوب
راجُعت كاملة ووُجدت شبه مكتملة من المرحلة الخامسة (بطاقة تعرض المستفيد/
المدينة/العنوان والعلامة المميزة/الأجهزة/اتصال/واتساب/الموقع/نسخ الرقم/
تعذّر/تسليم؛ `confirmDelivery` تتحقق من الصورة والإقرار وتمنع التكرار عبر
`opId`؛ السجل للقراءة فقط؛ المندوب المعطَّل أو صاحب الجلسة القديمة يُرفض
فورًا؛ لا وصول لمستفيد غير مسنَد) — **لم يُعثَر على أي نقص وظيفي جديد
هنا هذه المرحلة**، فقط تأكيد عبر رحلة التكامل الكاملة (الأقسام 9–11).

### سادسًا: حالات الواجهة
حالات التحميل (`skeletonCards`)، الفراغ المفسَّرة (`emptyState` برسالة
سياقية)، عدم تعليق الأزرار (`.finally(() => busy(false))` من المرحلة
الثالثة)، نوافذ التأكيد للعمليات الحساسة (`window.confirm` قبل كل تعطيل/
إعادة تعيين)، ورسائل خطأ عربية غير تقنية (`reportError`) — كلها كانت
مطبَّقة من مراحل سابقة. **جديد هذه المرحلة**: القوائم الأربع المُعاد ربطها
بالترقيم الخادمي (المستفيدون/الجمعيات/الأجهزة/المناديب) أُلحقت بنفس النمط
(هيكل تحميل، فراغ مفسَّر يفرّق بين "لا سجلات إطلاقًا" و"لا نتائج مطابقة"،
أزرار تنقّل صفحات تختفي تلقائيًا إن كانت صفحة واحدة).

### سابعًا: الاتساق والصلاحيات
- قواعد الحالات المركزية (`StateRules.gs`) لم تُمَس ولا تزال المصدر
  الوحيد؛ مُتحقَّق منها الآن أيضًا عبر `diagnoseStateIntegrity()` في نهاية
  رحلة التكامل الكاملة (القسم 12) — صفر تعارضات بعد كل الخطوات.
- الأرقام المحسوبة في لوحات البيانات مطابقة الآن فعليًا لما تعرضه القوائم
  (إصلاح ذاكرة Bootstrap أعلاه)، مُتحقَّق منه اختباريًا بمقارنة مباشرة.
- `opId`/`withIdempotency_` لم يتغيّرا — لا تزال محصورة بالعمليات الحساسة
  فعليًا (إنشاء جمعية/مندوب، قبول طلب انضمام، تأكيد تسليم) كما كانت.

### اختبار جديد: الرحلة الكاملة (`tools/integration-test.js`, 43 تأكيدًا)
14 قسمًا متسلسلة متصلة (لا معزولة) بنفس البيانات عبر الرحلة كلها: تقديم
طلب ← قبول ← دخول بكلمة مرور مؤقتة وإجبار تغييرها فعليًا (رُفض أي استدعاء
آخر قبل التغيير، تحقّق مباشر) ← إضافة مندوب ← إضافة مستفيد فرديًا (ببيانات
موقع كاملة) ← استيراد جماعي (صفّان صحيحان + رفض استيراد بلا تعهّد) ←
تخصيص جهاز ← تعيين مندوب ← دخول مندوب برمزه ← تعذّر تسليم ثم إعادة محاولة
ناجحة ← إثبات تسليم (يرفض بلا صورة، يحدّث المستفيد والجهاز والتسليم معًا،
يُسجَّل في سجل تسليمات المندوب فورًا) ← تطابق ملخّص Bootstrap مع
`listBeneficiaries`/`diagnoseStateIntegrity` (صفر تعارضات) وسجل العمليات
يوثّق كل خطوة ← عزل جمعية ثانية كاملة عن الأولى (لا رؤية، لا تعديل، لا
تعيين مندوب، سجل عمليات منفصل) ← إبطال رمز المندوب القديم وجلسته بعد
إعادة الإنشاء، وإبطال كلمة مرور الجمعية القديمة وجلستها بعد إعادة التعيين
من الإدارة.

**لم يُنفَّذ بعد من نطاق هذه المرحلة (كما كانت الحال قبل المرحلة السابعة)**:
الأربعة بنود التالية — سجل عمليات مندوب محدَّد، نافذة تفاصيل جهاز مستقلة،
رفض التكرار صراحة في الاستيراد، وقالب `.xlsx` حقيقي — **نُفِّذت جميعها لاحقًا
في المرحلة السابعة** (راجع القسم أدناه). أُبقي هذا الوصف القديم دون حذف
لتوثيق مسار القرار فقط.
- **اختبار حي فعلي**: كل ما سبق (وما بعده في المرحلة السابعة) مؤكَّد داخل
  محاكاة Node.js `vm`/متصفح Chromium محلي فقط — لا وصول لـ Apps Script
  حقيقي من بيئة هذه الجلسة (نفس القيد الموثَّق في كل مرحلة سابقة). الخريطة
  (Leaflet)، أزرار `tel:`/واتساب/تنزيل CSV، وفتح ملف `.xlsx` داخل Excel
  حقيقي، لم تُختبَر في متصفح حقيقي متصل بنشر Apps Script فعلي.

## المرحلة السابعة: إغلاق النواقص الوظيفية، ثم الهوية البصرية والجوال والوصول

نُفِّذت على دفعتين فوق commit `2225364` (نهاية المرحلة السادسة)، على الفرع
نفسه. راجع `RELEASE.md` القسم 13 للتفصيل الكامل — هنا ملخّص تنفيذي فقط.

### الدفعة الأولى: النواقص الوظيفية (commits `60da6a7`، `320065a`، `05407b2`)

1. **منع التكرار في المستفيدين** (`Beneficiaries.gs`): مطابق مؤكَّد (رقم
   الجوال، ضمن الجمعية نفسها) يُرفض دائمًا في الإضافة الفردية والاستيراد
   وبين صفوف الملف نفسه — لا اعتماد على الاسم وحده أبدًا. مطابق محتمل
   (اسم+مدينة) استشاري غير مانع. قفل تزامن يعيد الفحص مباشرة قبل الكتابة
   لمنع إدخال نفس الرقم مرتين من طلبين متزامنين. لا كشف لبيانات جمعية أخرى.
2. **قالب Excel حقيقي** (`ExcelTemplate.gs` جديد): حزمة OOXML فعلية عبر
   `Utilities.zip` المدمجة (بلا مكتبة/خدمة مدفوعة، لا شيء يُحمَّل عند
   الدخول)، بالأعمدة الرسمية، ورقة تعليمات، تنسيق نصي للجوال، وقوائم Excel
   منسدلة. اختبار بنيوي حقيقي عبر `zipfile`/`ElementTree` في Python يثبت
   أنه أرشيف ZIP سليم فعليًا لا CSV بامتداد مغاير. CSV بقي بديلًا احتياطيًا.
3. **تفاصيل الجهاز** (`getDeviceDetail` في `DevicesAssociations.gs`): نافذة
   تفاصيل كاملة (الحالة، الأطراف المرتبطة، كل التواريخ، سجل عمليات خاص
   بالجهاز)، من الجدول وصفحة المستفيد. تاريخا التخصيص والخروج مع المندوب
   يُشتقّان من سجل العمليات الموجود أصلًا — **بلا أي عمود جديد أو ترحيل**.
4. **سجل عمليات المندوب** (`listDelegateAuditLog` في `Normalize.gs`،
   و`listAuditLog` اكتسبت فلتر `recordId`): عزل صحيح بين الأدوار الثلاثة
   (إدارة/جمعية/مندوب نفسه)، ولا كشف لأي بيانات إدارية حساسة أو رمز دخول
   أو موقع حي للمندوب في سجله الخاص.
5. ١٤ اختبار خادم جديد (`server-test.js` أقسام ١٨–٢٠): تكرار مؤكَّد/محتمل،
   استثناء التعديل الذاتي، تكرار داخل الملف وضد قاعدة البيانات، عدم تسريب
   بيانات جمعية أخرى، وجود القفل في المسارين، صحة xlsx بنيويًا، تفاصيل
   الجهاز، وIDOR بين جمعيتين وبين مندوبين.

### الدفعة الثانية: الهوية البصرية والجوال والوصول (هذه الجولة)

**خلل مؤكَّد اكتُشف وأُصلح فعليًا عبر Chromium حقيقي (Playwright، أداة
تطوير محلية بحتة، غير مثبَّتة كتبعية للمشروع ولم تُستخدم في مسار النشر)**:
قاعدة CSS لإخفاء الشريط الجانبي على الجوال (`≤820px`) كانت تُزيحه بالاتجاه
الخاطئ في RTL (`translateX(110%)` بدل `translateX(-110%)`)، فيبقى نحو ٨٩
بكسل منه ظاهرًا فوق المحتوى بدل الاختفاء الكامل. تحقّق فعلي عبر
`getBoundingClientRect()`/`getComputedStyle()` قبل وبعد الإصلاح، ولقطات
شاشة عند 360/390/768/1024/1440px لكل البوابات الأربع وعدة نوافذ منبثقة،
على بيانات وهمية بالكامل من `tools/preview.js` (المُوسَّع هذه المرحلة
بدوال قوائم وهمية ليصبح فعليًا قابلًا لمعاينة صفحات الجداول لأول مرة).

إصلاحات/تحسينات إضافية مؤكَّدة بنفس الطريقة:
- شاشة الدخول على الجوال: كانت تتمركز رأسيًا داخل `100vh` كاملة فتترك
  فراغًا كبيرًا أعلى النموذج رغم اختفاء لوحة الفن الجانبية — أصبحت تبدأ
  قريبًا من الأعلى.
- `env(safe-area-inset-bottom)` على تذييل النافذة المنبثقة (نمط الورقة
  السفلية على الجوال) وحاوية التنبيهات.
- **حصر تركيز فعلي (focus trap)** داخل النوافذ المنبثقة عبر Tab — لم يكن
  موجودًا رغم وجود نقل/إعادة التركيز مسبقًا. تحقّق فعليًا: Tab من آخر عنصر
  يعيد التركيز لأول عنصر بالضبط.
- **تحذير قبل إغلاق نافذة فيها تغييرات غير محفوظة**: تتبّع `input`/`change`
  داخل `.modal`؛ الإغلاق العرضي فقط (Escape أو نقر خارج النافذة) يعرض
  `window.confirm` أصليًا؛ أزرار الإلغاء/الإغلاق الصريحة والإغلاق البرمجي
  بعد حفظ ناجح تبقى فورية تمامًا كما كانت — تحقّق فعليًا عبر Playwright.
- روابط خارجية: تأكيد (لا تغيير) أن الكل يحمل `rel="noopener noreferrer"`،
  مع اختبار انحدار صريح جديد له.
- `aria-live`/`role` على التنبيهات مضبوطان حسب النوع؛ نوع "تحذير" (`warn`)
  جديد لتنبيه التكرار المحتمل من الدفعة الأولى.

**لم يُعثر على تمرير أفقي غير مقصود في أي صفحة رُوجعت** (قياس
`scrollWidth - clientWidth === 0` فعليًا) عند كل الأحجام الخمسة المطلوبة،
للوحة البيانات، قائمة المستفيدين (تتحول لبطاقات على الجوال)، نافذتي تفاصيل
المستفيد والجهاز، وبوابتي الجمعية والمندوب.

**الشعاران**: كانا مُضمَّنين مسبقًا كـ`data:image/png;base64,...` من مرحلة
سابقة موثَّقة (بالضبط الحل الآمن لـ HtmlService بلا مسار GitHub هشّ) —
لم يتغيّر شيء هنا، فقط تأكيد استمرار عمله عبر `tools/smoke.js`.

**الاختبارات**: ١٤ اختبار انحدار جديد في `tools/smoke.js` (قسم ١١). **كل
مجموعات الاختبار الحالية شُغِّلت ونجحت**: `verify.js` 31/31، `smoke.js`
186/186، `server-test.js` 183/183، `security-test.js` 87/87، `state-test.js`
53/53، `account-test.js` 53/53، `perf-test.js` 32/32، `reference-test.js`
60/60، `integration-test.js` 43/43 — **إجمالي 728 تأكيدًا ناجحًا**.

**حجم `Index.html`**: 374687 بايت (٣٧٠٢ سطرًا) — بقي ملفًا واحدًا موحَّدًا
عمدًا لنفس السبب الموثَّق في المراحل السابقة (لا فائدة حقيقية من التقسيم مع
`HtmlService.include()`، وخطر أخطاء حدود الملفات أعلى من الفائدة). لم تتغيّر
بنية ملفات `.gs` في هذه الدفعة تحديدًا فلم تُعدَّل أدوات الدمج.

**بنود تحتاج اختبارًا حيًا**: فتح ملف `.xlsx` داخل Microsoft Excel حقيقي؛
تباين الألوان وفق WCAG AA بأداة قياس فعلية؛ سلوك لوحة مفاتيح الجوال الفعلية؛
التكبير الفعلي للنص إلى 200%؛ وقارئ شاشة فعلي (لم يُستخدم — الاعتماد على
العناصر الدلالية/ARIA الصحيحة فقط).

## المرحلة الثامنة: المراجعة الأمنية النهائية، preflight/dry-run، وRelease Candidate

**التفصيل الكامل في `SECURITY_REVIEW.md` (جديد) و`DEPLOYMENT.md` (جديد)** —
هذا القسم مؤشر موجز فقط.

- **جرد سطح النظام الكامل**: كل دالة Apps Script علوية (35 دالة عميل +
  9 دوال تشغيلية) موثَّقة في `SECURITY_REVIEW.md` §1 (الدور المسموح،
  الجلسة، الملكية، الكتابة، rate limit/lock/opId).
- **⚠️ اكتشاف معماري مؤكَّد، موثَّق دون معالجة تلقائية للدوال القديمة**:
  دوال الإعداد/الترحيل (`setupSheets`/`migrateReferenceData`/
  `migrateLegacyReferenceValues`/`migratePhoneNumbers`/`repairStateIntegrityIssues`)
  بلا أي تحقق جلسة — خاصية معمارية أصيلة في Apps Script (كل دالة علوية
  عامة تلقائيًا عبر `google.script.run`)، لا خللًا محليًا. التفصيل الكامل
  والأثر الفعلي والتخفيف المقترَح في `SECURITY_REVIEW.md` §2 — **يحتاج
  قرارك صراحة قبل أي نشر عام واسع**.
- **بديل آمن جديد (`ReleaseOps.gs`)**: `preflightRelease_(token)` (قراءة فقط
  بالكامل) و`applyReleaseSchema_(token, options)` (إضافي فقط، خاصتان ومقفلتان خلف رمز وصول صيانة مؤقت، ترفضان العمل بلا
  `options.approvalCode` مطابق حرفيًا، آمنة لإعادة التشغيل) — موصى بهما
  بدل `setupSheets()` المباشرة لأي عمل مخطط بيانات مستقبلي.
- **معالجة أمنية فعلية**: `verifyImageMagicBytes_()` جديدة تتحقق من
  التوقيع الحقيقي لبايتات صورة إثبات التسليم بعد فك base64 — لا الاكتفاء
  بتصديق بادئة `data:image/...` المُعلَنة من العميل وحدها كما كان سابقًا.
- **تنظيم Index.html**: أُعيد تقييمه عمليًا لخيار تقسيم محدَّد
  (`Styles.html`/`AppShell.html`/`ClientCore.html`/`AdminPortal.html`/
  `AssociationPortal.html`/`DelegatePortal.html`) — تقرَّر البقاء موحَّدًا
  (لا فائدة حمولة فعلية مع `HtmlService.include()`، ويزيد خطر التركيب
  اليدوي فعليًا). التفصيل الكامل في `RELEASE.md` §14.
- **تصحيح توثيقي**: قسم "أصول الهوية البصرية" في `README.md` كان يصف حالة
  قديمة (شعارين فارغين يحتاجان حقنًا يدويًا) رغم أن الشعارين مُضمَّنان
  فعليًا منذ مرحلة سابقة — صُحِّح ليعكس الواقع الحالي.
- **17 اختبارًا جديدًا/محدَّثًا**: 10 في `server-test.js` (preflight/
  applyReleaseSchema)، 4 في `security-test.js` (التحقق من التوقيع
  الحقيقي للصور)، 1 محدَّث في `reference-test.js` (توقّع استدعاء
  `diagnoseReferenceDataIssues` من `preflightRelease` الجديدة)، بالإضافة
  لتحديث fixture الصورة الوهمية (base64 حقيقي بدل نص عادي) في
  `integration-test.js`/`state-test.js`/`perf-scale-test.js` بعد تفعيل
  التحقق من التوقيع الحقيقي.
- **لم يُشغَّل** أي من `setupSheets`/`preflightRelease`/`applyReleaseSchema`/
  أي دالة ترحيل أو إصلاح على أي بيانات حية من هذه الجلسة. لم يُنشر أي
  إصدار، لم يُدمَج أي Pull Request.

## تصحيح أمني لاحق للمرحلة الثامنة: قفل كل دوال الصيانة عن google.script.run

القسم أعلاه وثَّق فجوة معمارية مؤكَّدة (دوال الإعداد/الترحيل بلا أي تحقق
جلسة، قابلة للاستدعاء نظريًا من أي زائر عبر console المتصفح) **دون
معالجتها تلقائيًا** في حينها. طُلب لاحقًا تصحيحها فعليًا، ونُفِّذ كاملًا:

- **إعادة تسمية عشر دوال صيانة لتنتهي بشرطة سفلية** (`setupSheets_`،
  `migrateReferenceData_`، `migrateLegacyReferenceValues_`،
  `previewPhoneNormalization_`، `migratePhoneNumbers_`،
  `diagnoseReferenceDataIssues_`، `diagnoseStateIntegrity_`،
  `repairStateIntegrityIssues_`، `preflightRelease_`، `applyReleaseSchema_`).
- **حارس وصول جديد (`requireMaintenanceAccess_` في `ReleaseOps.gs`)**
  تستدعيه كل واحدة منها أولًا — لا تعمل أي منها بلا رمز وصول صيانة مؤقت،
  حتى من محرر Apps Script نفسه.
- **`grantMaintenanceAccess_()`**: تُشغَّل يدويًا من المحرر فقط (لا جلسة
  ويب)، تولّد رمزًا عشوائيًا بالكامل، تخزّن بصمته المُجزَّأة فقط في Script
  Properties، تطبع الرمز الخام مرة واحدة فقط في سجل التنفيذ (لا يُعاد في
  أي قيمة راجعة). صالح 20 دقيقة افتراضيًا (حتى 60)، يُقفَل تلقائيًا بعد 5
  محاولات فاشلة. `revokeMaintenanceAccess_()` لإبطاله يدويًا قبل الأوان.
- **لا رمز موافقة ثابت في الكود بعد الآن** — الثابت القديم
  `RELEASE_SCHEMA_APPROVAL_CODE_` أُزيل بالكامل من `applyReleaseSchema_`.
- الفصل بين فحص المخطط/إضافته/ترحيل المرجعيات/تشخيص الحالات/إصلاحها بقي
  كما هو (لم يتغيّر) — فقط أُضيف حاجز الوصول فوق كل دالة بشكل مستقل.
- **`DEPLOYMENT.md` أُعيد ترقيمه وتحديثه بالكامل** (قسم 5 جديد: منح
  الوصول قبل preflight)، وحُذفت أي توصية بتشغيل `setupSheets` مباشرة
  كخطوة نشر عامة. `RELEASE.md`/`SECURITY_REVIEW.md`/`README.md` حُدِّثت
  للأسماء والآلية الجديدة.
- **56 اختبارًا جديدًا**: `security-test.js` أقسام 11–12 (محاكاة استدعاء
  كل دالة صيانة كما تفعل `google.script.run` بلا رمز وبرمز مزوَّر، والتحقق
  من عدم وجود أي سرّ/رمز موافقة ثابت في المستودع)، بالإضافة لتحديث
  اختبارات `server-test.js`/`reference-test.js`/`state-test.js`/
  `account-test.js`/`integration-test.js` للأسماء والتوقيعات الجديدة.
- **لم يُشغَّل** أي من هذه الدوال، ولا `grantMaintenanceAccess_` نفسها، على
  أي بيانات أو مشروع Apps Script حي من هذه الجلسة.

## المتبقي (قرارات مدروسة موثَّقة، وليست إغفالًا)

- **`Index.html` لم يُقسَّم**: تقسيمه عبر `HtmlService.createTemplateFromFile`
  + `include()` لا يقلّل حجم ما يصل للمتصفح فعليًا (الأجزاء تُدمَج نصيًا في
  الخادم قبل الإرسال، فالنتيجة مطابقة لملف واحد) — الفائدة تنظيمية بحتة.
  المخاطرة حقيقية: كل جافاسكربت الواجهة برنامج واحد مترابط عبر نطاق عام
  مشترك وتفويض أحداث (event delegation) على DOM واحد، وتقسيم ملف التأليف
  يفتح بابًا لأخطاء حدود الملفات — بالضبط فئة العطل الذي بدأ منه هذا
  المشروع (inline event handlers). القرار: الإبقاء عليه ملفًا واحدًا؛
  يُعاد النظر فقط إن أصبح حجمه (~2500 سطر) عائقًا فعليًا على التحرير.
- **اختبار حي فعلي على Apps Script/Google Sheets**: لم يحدث من داخل أي
  جلسة تطوير (حجب شبكي مؤكَّد لـ `script.google.com`/`docs.google.com` من
  بيئة التنفيذ). `RELEASE.md` يُجهِّز كل ما تحتاجه لتنفيذه بنفسك — هذا هو
  الأثر العملي الوحيد المتبقي فعليًا قبل أي نشر واسع.
- **اختبار اختراق يدوي مستقل احترافي**: `tools/security-test.js` فحص آلي
  على مستوى الكود والمنطق داخل بيئة محاكاة، وليس بديلًا عن مراجعة أمنية
  مستقلة بمعايير OWASP/PTES تغطي الشبكة والبنية التحتية والنشر الفعلي.
- **الاستجابة الكاملة عبر كل حجم شاشة**: تحسينات جزئية مطبَّقة (جداول
  قابلة للتمرير الأفقي، شريط جانبي منهار على الجوال) لكن لم يُجرَ اختبار
  فعلي في متصفح حقيقي على أجهزة متعددة — بند في قائمة الاختبار اليدوي
  بـ`RELEASE.md` (القسم 5.5) لتنفيذه أنت مباشرة.
- **إثبات تسليم فاشل بتفاصيل أوسع**: الأساس موجود (`FAILED_REASONS`،
  `updateDeliveryStatus`) لكن لم تُراجَع تجربة المندوب الكاملة لحالات
  التعذر المتكررة أو إعادة الجدولة.

## الترحيلات المكتوبة ولم تُشغَّل (بانتظار قرارك)

كل ما يلي **آمن ومتكرر التشغيل (idempotent)** ولا يحذف بيانات، لكنه **لم
يُشغَّل على أي بيانات حقيقية** بناءً على تعليماتك الصريحة بعدم تشغيل أي
ترحيل دون إذن مباشر. **`RELEASE.md` القسم 3 يوثّق الترتيب الموصى به
بالتفصيل** — الملخص هنا:

- `setupSheets()` — تُنشئ كل الأوراق والأعمدة الناقصة (بما فيها "طلبات
  انضمام الجمعيات" وعمودا الموقع الجغرافي). آمنة للتشغيل المتكرر.
- `previewPhoneNormalization()` — قراءة فقط، تقرير بكل رقم سيتغيّر تنسيقه.
- `migratePhoneNumbers()` — يطبّق التطبيع الفعلي؛ لا تُشغّلها قبل مراجعة
  تقرير `previewPhoneNormalization()`.
- `migrateReferenceData()` — يبذر ورقة "البيانات المرجعية" فتتحوّل حقول
  المنطقة/المدينة/التصنيف/الحالة الاجتماعية/نوع الجهاز من نص حر لقوائم
  منسدلة مترابطة في كل النماذج، وتُفعِّل التحقق الخادمي الصارم.
- `diagnoseReferenceDataIssues()` — قراءة فقط، تقرير بكل قيمة قديمة/غير
  معروفة وموقعها؛ يعمل فقط بعد `migrateReferenceData()`.
- `migrateLegacyReferenceValues(dryRun)` — معاينة (افتراضي) أو تطبيق فعلي
  للتصحيحات المؤكَّدة فقط في المنطقة/المدينة القديمة؛ لا تُخمِّن أبدًا، وآمنة
  لإعادة التشغيل. راجع `RELEASE.md` القسم 10 للترتيب الموصى به بالتفصيل.

**المرحلة الخامسة لم تُضِف أي دالة ترحيل جديدة** — الأعمدة الثلاثة الجديدة
في "المستفيدون" ("علامة مميزة"، "مصدر الموقع"، "تاريخ تحديث الموقع")
تُنشَأ تلقائيًا فقط عبر `setupSheets()` القائمة أعلاه (إضافية بالكامل).

## ملفات مهمة

| الملف | الدور |
|---|---|
| `Config.gs` … `Validation.gs` + `StateRules.gs` + `Pagination.gs` + `ExcelTemplate.gs` (16 ملفًا) | الخادم الكامل، مقسَّم موضوعيًا — انظر جدول الملفات في `README.md` |
| `Index.html` | الواجهة الكاملة (ملف واحد عمدًا، 374687 بايت/3702 سطرًا) — اختيار الموقع بصريًا عبر Leaflet/OpenStreetMap (تحميل كسول)، حصر تركيز وتحذير إغلاق غير محفوظ في النوافذ المنبثقة (المرحلة السابعة) |
| `assets/` | ملفا الشعار الأصليان (مرجع؛ النسخة المستخدَمة فعليًا مضمَّنة data URI في `Index.html`) |
| `tools/gs-manifest.js` | ترتيب دمج ملفات `.gs` لأدوات الاختبار المحلية فقط |
| `tools/preview.js` | يبني معاينة HTML ببيانات وهمية (بلا `google.script.run` حقيقي) لمراجعة الواجهة بصريًا محليًا |
| `tools/verify.js` | فحص ثابت — 31 تأكيدًا |
| `tools/smoke.js` | اختبار DOM تشغيلي — 186 تأكيدًا |
| `tools/server-test.js` | اختبار منطق خادم بمحاكاة Apps Script — 196 تأكيدًا |
| `tools/security-test.js` | مراجعة أمنية آلية — 91 اختبارًا |
| `ReleaseOps.gs` | `preflightRelease_`/`applyReleaseSchema_` (خاصتان + رمز وصول صيانة مؤقت) — بديل آمن لـ`setupSheets_()` كخطوة نشر عامة |
| `DEPLOYMENT.md` | دليل تركيب ونشر تنفيذي كامل (المرحلة الثامنة) |
| `SECURITY_REVIEW.md` | جرد سطح النظام الكامل ومصفوفة الصلاحيات (المرحلة الثامنة) |
| `tools/state-test.js` | اختبار سلامة الحالات والترابط — 53 تأكيدًا |
| `tools/account-test.js` | اختبار إدارة الحسابات: كلمة مرور الجمعية، رمز المندوب، العزل، التزامن، الأسرار — 53 تأكيدًا |
| `tools/perf-test.js` | اختبار أداء وظيفي: ترقيم/بحث/فلترة/عزل أدوار، استجابات حفظ جزئية، إبطال الذاكرة المؤقتة — 32 تأكيدًا |
| `tools/reference-test.js` | اختبار البيانات المرجعية: ترابط منطقة/مدينة، تشخيص، dry-run وإعادة تشغيل آمنة، صفر الهاتف الأول، تنسيق التاريخ، عزل الجمعيات — 60 تأكيدًا |
| `tools/integration-test.js` | **جديد** — رحلة تكامل كاملة متصلة عبر البوابات الثلاث (تقديم ← قبول ← تسليم فعلي ← عزل ← إبطال جلسات) — 43 تأكيدًا |
| `tools/perf-scale-test.js` | تقرير أداء بأحجام 100/1000/5000 (لا نجاح/فشل) |
| `tools/perf-bench.js` | قياس تجريبي لعدد قراءات الشيت من المرحلة الأولى (اختياري) |
| `README.md` | تعليمات التثبيت والتشغيل والنشر الكاملة على Apps Script |
| `RELEASE.md` | خطة الاختبار الحي الكاملة والنسخة المرشحة للإصدار وخطة الرجوع، بما فيها نتائج قياس الأداء (القسم 9)، الموقع الجغرافي (القسم 11)، وترابط البوابات (القسم 12) |

## نتائج الاختبارات (آخر تشغيل قبل هذا التسليم)

```
node tools/verify.js            → 31/31
node tools/smoke.js             → 186/186
node tools/server-test.js       → 198/198
node tools/security-test.js     → 141/141
node tools/state-test.js        → 56/56
node tools/account-test.js      → 53/53
node tools/perf-test.js         → 32/32
node tools/reference-test.js    → 61/61
node tools/integration-test.js  → 43/43
```

**المجموع بعد المرحلة التاسعة: 932/932، بلا أي تراجع وظيفي عبر كل
commit في هذا الفرع.** (كان 801 قبلها.)

| الأداة | قبل المرحلة التاسعة | بعدها |
|---|---|---|
| `tools/verify.js` | 31 | 31 |
| `tools/smoke.js` | 186 | 228 |
| `tools/server-test.js` | 210 | 240 |
| `tools/security-test.js` | 141 | 151 |
| `tools/state-test.js` | 56 | 56 |
| `tools/account-test.js` | 53 | 53 |
| `tools/perf-test.js` | 32 | 36 |
| `tools/reference-test.js` | 61 | 67 |
| `tools/integration-test.js` | 43 | 70 |
| **المجموع** | **813** | **932** |

ثلاث حالات اختبار قديمة عُدِّلت (لا أُضعِفت): كانت تُثبِّت السقوط الصامت
إلى حقول نصية حرة قبل ترحيل المصادر المرجعية، وقد استُبدلت بتأكيد السلوك
الأقوى الجديد (رفض القيم خارج القائمة + رسالة صريحة وزر إعادة محاولة).
(بالإضافة إلى `tools/perf-scale-test.js` و`tools/perf-bench.js`، أداتا تقرير أداء لا تعتمدان على نجاح/فشل.)

## نقطة المتابعة الدقيقة للجلسة القادمة (إن وُجدت)

كل الأعمال البرمجية المخطَّطة مكتملة ومدفوعة. لا توجد أي وظيفة نصف
منجزة أو تعديلات غير محفوظة في هذا الفرع. الخطوة العملية الوحيدة
المتبقية هي **بشرية وليست برمجية**: تنفيذ قائمة الاختبار الحي في
`RELEASE.md` بنفسك على نسخة Google Sheets تجريبية (بعد تركيب ملفات
المراحل الثلاث كاملة أولًا — انظر التنبيه أعلى هذا الملف)، ثم القرار
بالنشر الفعلي أو الدمج بعد رضاك عن النتائج. إن أردت جلسة عمل جديدة لاحقًا:

1. شغّل التسعة (`verify.js`/`smoke.js`/`server-test.js`/`security-test.js`/
   `state-test.js`/`account-test.js`/`perf-test.js`/`reference-test.js`/
   `integration-test.js`) أولًا للتأكد من عدم وجود تراجع منذ آخر commit.
2. راجع نتائج اختبارك الحي وفق `RELEASE.md`؛ أي بند فشل يستحق تحقيقًا
   قبل أي عمل جديد.
3. إن رغبت بإزالة التكرار المتبقي: `beneficiaries`/`associations`/
   `devices`/`delegates` لا تزال تُرسَل مرتين (مصفوفة كاملة في Bootstrap
   + عبر `list*` عند فتح الصفحة) — إزالة النسخة الكاملة من Bootstrap
   يتطلّب إعادة كتابة كل موضع يفترض وجودها محليًا (قوائم منسدلة، بحث
   بالاسم) لنمط "بحث/جلب عند الحاجة" (موثَّق بالتفصيل في قسم المرحلة الثالثة).
4. بنود المرحلة السابعة المتبقية للاختبار الحي فقط (الكود مكتمل ومختبر
   آليًا): فتح ملف `.xlsx` داخل Excel حقيقي، تباين WCAG AA بأداة قياس
   فعلية، لوحة مفاتيح الجوال الفعلية، التكبير لـ200%، وقارئ شاشة فعلي
   (راجع قسم المرحلة السابعة أعلاه للتفصيل الكامل).
5. إن رغبت في إطلاق واسع النطاق: اطلب اختبار اختراق مستقل احترافي — لم
   تُجرَ أي مراجعة أمنية نهائية بعد ضمن نطاق المرحلة السابعة (مؤجَّلة عمدًا).

## روابط

- الفرع: `claude/code-index-review-kz5k4u`
- المستودع: `mroan7241-create/-`
- Pull Request #1 (مفتوح، غير مدموج): https://github.com/mroan7241-create/-/pull/1
