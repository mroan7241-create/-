# تقرير تسليم — نظام متابعة توزيع الأجهزة (جمعية الزاد)

آخر تحديث: هذا الملف يعكس حالة الفرع `claude/code-index-review-kz5k4u` عند
commit **`600b0af`** (أحدث commit عند كتابة هذا التقرير). مستودع GitHub هو
المصدر المعتمد الوحيد — تجاهل أي نسخة مطابقة من ملفات لصقت سابقًا في المحادثة.

## الحالة العامة

النظام يعمل ومختبر آليًا (216+ اختبارًا عبر ثلاث أدوات، جميعها ناجحة عند كل
commit). لم يُنشر أي إصدار جديد على Apps Script الحي، ولم تُشغَّل أي دالة
ترحيل على بيانات إنتاجية، ولم يُدمَج أي Pull Request — كل ذلك بانتظار قرارك.

## ما هو مكتمل ومختبر فعليًا

| # | البند | الحالة |
|---|---|---|
| 1–5 | بناء المشروع الأساسي: أمن (تشفير، جلسات، صلاحيات، منع Formula Injection)، هوية الزاد البصرية، إزالة كل inline event handlers (سبب العطل الأصلي)، أدوات فحص آلية | ✅ مكتمل |
| 6 | مصادر مرجعية موحّدة (مناطق/مدن/تصنيفات/حالات اجتماعية) مع سقوط آمن لنص حر قبل تشغيل الترحيل | ✅ مكتمل |
| 7 | إعادة هندسة الأداء: ذاكرة تخزين مؤقت داخل الطلب (4 ثوانٍ) + ذاكرة عبر الطلبات لكامل `getBootstrapData` + قياس تجريبي فعلي (`tools/perf-bench.js`) | ✅ **الجزء الأساسي مكتمل.** تقسيم `Code.gs`/`Index.html` إلى ملفات متعددة **لم يُنفَّذ** (أُجِّل عمدًا — انظر "المتبقي") |
| 8 | بوابة تقديم الجمعيات العامة (بلا تسجيل دخول) + مراجعة إدارية (قبول/رفض) مع إنشاء الجمعية وحسابها تلقائيًا وكلمة مرور مؤقتة تُعرض مرة واحدة فقط | ✅ مكتمل |
| 9 | لوحة إدارة تفاعلية: كل مؤشر KPI وكل تنبيه قابل للنقر وينقل لصفحة مفلترة | ✅ مكتمل |

نتائج القياس الفعلي للأداء (`node tools/perf-bench.js`، قبل commit `c188f74`
مقابل النسخة الحالية، بعدد قراءات `getDataRange().getValues()` الكاملة —
المقياس الحقيقي لتكلفة Sheets API):

| السيناريو | قبل | بعد | التحسّن |
|---|---|---|---|
| بناء بوابة جمعية واحدة | 10 | 6 | ‑40٪ |
| بناء بوابة إدارة واحدة | 8 | 8 | بلا تغيير (لا تكرار أصلًا) |
| دخول أول لجمعية | 11 | 6 | ‑45٪ |
| دخول ثانٍ فوري (خلال دقيقة) | 11 | 0 | ‑100٪ (كاش كامل) |

لا يقيس هذا زمن شبكة فعليًا (لا تتوفر بيئة Apps Script حية في جلسات التطوير
هذه) لكنه المتغيّر المتحكم مباشرة في ذلك الزمن.

## المتبقي (لم يُبدأ أو أُجِّل عمدًا)

كل ما يلي **لم يُنفَّذ بعد**. لا تفترض أي جزء منه جاهزًا:

- **تقسيم ملفات Code.gs/Index.html** (الجزء المتبقي من البند 7): حاليًا كل
  شيء في ملف خادم واحد وملف واجهة واحد. Apps Script يدعم تقسيم `.gs` إلى
  عدة ملفات (تُدمج تلقائيًا بنطاق عام واحد) وتقسيم HTML عبر
  `HtmlService.createTemplateFromFile` + `include()`. أُجِّل هذا لأنه إعادة
  هيكلة عالية الخطورة (تتطلب إعادة اختبار كل مسار) بعائد أداء أقل من الكاش
  الذي طُبِّق فعلًا؛ يُنصح بتنفيذه فقط إن أصبح حجم `Index.html` (~2200 سطر
  حاليًا) عائقًا فعليًا على التحرير، وليس كهدف بحد ذاته.
- **البند 10 — إدارة الأنشطة والفرعية**: الصفحة الحالية (`renderActivities`)
  تعرض فقط؛ لا توجد شاشات "مكتمل/جارٍ/قادم" منفصلة، ولا نموذج إضافة/تعديل
  نشاط رئيسي أو فرعي من الواجهة (البيانات تُدار من شيت "إدارة الأنشطة"
  مباشرة حاليًا).
- **البند 11 — موقع المستفيد الجغرافي ومركز يوم المندوب**: لا يوجد حقل
  إحداثيات (خط طول/عرض) في نموذج المستفيد، ولا خريطة تفاعلية لعرض
  المستفيدين، ولا شاشة "مسار اليوم" مجمّعة للمندوب. الموجود حاليًا فقط:
  رابط Google Maps للبحث بالعنوان النصي (`renderDelegateList`)، وهو أساس
  جيد للبناء عليه لاحقًا (تمت الموافقة سابقًا على استخدام مسار مجاني
  تقريبي بلا Directions/Distance Matrix API المدفوعة — القرار ما زال ساريًا).
- **إثبات تسليم فاشل بتفاصيل أوسع**: الأساس موجود (`FAILED_REASONS`،
  `updateDeliveryStatus`) لكن لم تُراجَع تجربة المندوب الكاملة لحالات
  التعذر المتكررة أو إعادة الجدولة.
- **البند 9 (جزء إضافي محتمل)**: لوحة الإدارة أصبحت تفاعلية، لكن لم تُضَف
  بعد شاشات "drill-down" مخصصة أعمق من مجرد فتح الصفحة مع فلتر (مثل تقرير
  تفصيلي منبثق لكل مؤشر).
- **البند 12 — مراجعة اللغة الرسمية**: لم تُجرَ مراجعة شاملة منهجية لكل
  نص في `Index.html` لاستبدال أي صياغة غير رسمية. الصياغة الحالية رسمية
  بشكل عام (كُتبت هكذا من البداية) لكن لم تُدقَّق سطرًا بسطر بحثًا عن
  استثناءات.
- **مراجعة أمنية نهائية شاملة**: الأساسيات مطبّقة ومختبرة (تشفير كلمات
  المرور بالملح، جلسات قابلة للإبطال الفوري، تحديد معدّل الدخول، منع
  Formula Injection، حراسة الأدوار على كل دالة خادم حساسة `15` دالة محروسة
  حاليًا حسب `tools/verify.js`، عزل الجمعيات عن بعضها عبر `associationId`).
  **لم يُجرَ بعد**: مراجعة مخصصة لرفع الملفات (استيراد Excel/صور الإثبات)
  ضد أنواع ملفات خبيثة أو أحجام مفرطة بما يتجاوز الفحوص الحالية، ولا
  اختبار اختراق يدوي شامل.
- **الشعارات الفعلية**: لم تُضمَّن أي بيانات صورة حقيقية لشعار الزاد أو
  شعار مؤسسة أبانمي — كلاهما لا يزال فارغًا في ثابت `BRAND` أعلى
  `Index.html` (النظام يعرض بديلًا نصيًا أنيقًا بدلًا منهما تلقائيًا، وهذا
  ليس عطلًا بل سلوك مقصود). سبب عدم التضمين: كل الصور المرسلة سابقًا عبر
  المحادثة إما غير قابلة للوصول من هذه البيئة (حجب شبكي مؤكّد على
  `i.postimg.cc` و`script.google.com` و`docs.google.com`) أو وصلت تالفة/غير
  مكتملة عند إعادة البناء من نص المحادثة (تحقّق فعلي عبر فاحص PNG مخصص، لا
  تخمين). **للإكمال**: أرفق ملفَي الشعار كملفات حقيقية (رفع مباشر إلى بيئة
  عمل قادمة، أو مسار في نفس المستودع) لا كصور مضمّنة في نص المحادثة، ثم
  حوّلهما إلى Data URI حسب التعليمات في `README.md` والصقهما في `BRAND`.
- **الاستجابة الكاملة عبر كل حجم شاشة**: تحسينات جزئية طُبِّقت (الجداول
  تُغلَّف بـ `table-wrap` قابل للتمرير الأفقي، الشريط الجانبي ينهار على
  الجوال). لم تُجرَ مراجعة شاملة منهجية لكل نافذة منبثقة (modal) وكل جدول
  عند 390px تحديدًا كما طُلب، ولا اختبار فعلي في متصفح حقيقي (بيئة هذه
  الجلسة بلا واجهة رسومية تفاعلية مع Apps Script الحي).

## الترحيلات المكتوبة ولم تُشغَّل (بانتظار قرارك)

كل ما يلي **آمن ومتكرر التشغيل (idempotent)** ولا يحذف بيانات، لكنه **لم
يُشغَّل على أي بيانات حقيقية** بناءً على تعليماتك الصريحة بعدم تشغيل أي
ترحيل دون إذن مباشر:

- `migrateReferenceData()` — يبذر ورقة "البيانات المرجعية" (مناطق/مدن/
  تصنيفات/حالات اجتماعية معتمدة). بعده تتحوّل حقول المنطقة/المدينة من نص
  حر إلى قوائم منسدلة مترابطة تلقائيًا في كل النماذج.
- `previewPhoneNormalization()` ثم `migratePhoneNumbers()` — يقرأ الأول
  تقريرًا بكل رقم جوال مخزَّن سيتغيّر تنسيقه، والثاني يطبّق التطبيع
  الفعلي. لا تُشغّل الثاني قبل مراجعة تقرير الأول.
- `setupSheets()` — آمنة للتشغيل المتكرر (لا تحذف أعمدة أو صفوفًا قائمة،
  تُضيف فقط الأعمدة/الأوراق الناقصة). **يجب تشغيلها مرة واحدة بعد كل نشر
  لهذا الفرع** لإنشاء ورقة "طلبات انضمام الجمعيات" الجديدة (البند 8)، وإلا
  ستفشل بوابة تقديم الجمعيات برسالة عربية واضحة ("استقبال الطلبات غير
  مفعّل حاليًا") بدل أي خطأ غامض.

## ملفات مهمة

| الملف | الوصف |
|---|---|
| `Code.gs` | الخادم الكامل (~1650 سطرًا تقريبًا بعد كل التعديلات) |
| `Index.html` | الواجهة الكاملة (~2250 سطرًا تقريبًا) |
| `tools/verify.js` | فحص ثابت — 31 تأكيدًا |
| `tools/smoke.js` | اختبار DOM تشغيلي — 88 تأكيدًا |
| `tools/server-test.js` | اختبار منطق خادم بمحاكاة Apps Script — 100 تأكيد |
| `tools/perf-bench.js` | قياس تجريبي لعدد قراءات الشيت (اختياري، ليس جزءًا من بوابة النجاح) |
| `README.md` | تعليمات التثبيت والتشغيل والنشر الكاملة على Apps Script |

## نتائج الاختبارات (آخر تشغيل قبل هذا التسليم)

```
node tools/verify.js       → 31/31
node tools/smoke.js        → 88/88
node tools/server-test.js  → 100/100
```

**المجموع: 219/219، بلا أي تراجع وظيفي عبر كل commit في هذا الفرع.**

## خطوات مقترحة لجلسة العمل القادمة

بالترتيب المقترح حسب الأولوية المتبقية من طلبك الأصلي:

1. شغّل الفحوص الثلاثة أولًا للتأكد من عدم وجود تراجع منذ آخر commit.
2. البند 10 (إدارة الأنشطة): أضف نماذج إضافة/تعديل نشاط رئيسي/فرعي من
   الواجهة بدل الاعتماد الكامل على تحرير الشيت مباشرة، وشاشات تصفية
   "مكتمل/جارٍ/قادم/متأخر".
3. البند 11 (الموقع الجغرافي): أضف حقلي خط الطول/العرض لنموذج المستفيد
   (اختياريان، لا يكسران البيانات القائمة)، وشاشة "مسار اليوم" للمندوب
   تُرتّب المستفيدين المتبقين تقريبيًا حسب القرب الجغرافي بلا أي API مدفوع.
4. مراجعة أمنية نهائية مركّزة على رفع الملفات تحديدًا (حدود الحجم والنوع
   الفعلية المطبّقة في `inspectBeneficiaryExcel` و`saveProofImage_`).
5. مراجعة لغوية شاملة سطرًا بسطر لكل نص في `Index.html`.
6. عند توفر ملفَي الشعار الفعليَّين كملفات: حوّلهما وأدرجهما في `BRAND`.
7. لا تنسَ تشغيل `setupSheets()` مرة واحدة بعد أي نشر لهذا الفرع على بيئة
   حية، لإنشاء ورقة طلبات الانضمام الجديدة.

## روابط

- الفرع: `claude/code-index-review-kz5k4u`
- آخر commit عند كتابة هذا التقرير: `600b0af`
- المستودع: `mroan7241-create/-`
- Pull Request #1 (مفتوح، غير مدموج): https://github.com/mroan7241-create/-/pull/1
