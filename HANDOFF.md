# تقرير تسليم — نظام متابعة توزيع الأجهزة (جمعية الزاد)

آخر تحديث: هذا الملف يعكس حالة الفرع `claude/code-index-review-kz5k4u` بعد
إضافة المرحلة الثانية (إدارة حسابات الجمعيات والمناديب، أدناه). مستودع
GitHub هو المصدر المعتمد الوحيد — تجاهل أي نسخة مطابقة من ملفات لصقت سابقًا
في المحادثة.

## الحالة العامة: نسخة مرشحة للإصدار (Release Candidate) + مرحلتا سلامة حالات وإدارة حسابات

جميع البنود المخطَّطة الأصلية (١–١٧) **مكتملة ومختبرة آليًا**، وأُضيفت إليها
**المرحلة الأولى: سلامة الحالات** (قواعد انتقال مركزية للجهاز/التسليم +
تشخيص/إصلاح تعارضات) **والمرحلة الثانية: إدارة حسابات الجمعيات والمناديب
واستعادة الوصول الآمنة** — التفاصيل الكاملة في القسمين أدناه. الإجمالي الآن
**461 تأكيدًا عبر ست أدوات اختبار، جميعها ناجحة عند آخر commit**. لم يُنشر
أي إصدار جديد على Apps Script الحي بهذه التعديلات الجديدة، ولم تُشغَّل أي
دالة ترحيل أو إصلاح على بيانات حقيقية، ولم يُدمَج أي Pull Request — كل ذلك
بانتظار قرارك.

**تنبيه مهم**: النسخة التجريبية الحية التي رُكِّبت يدويًا في مشروع Apps
Script التجريبي (بمساعدتي خطوة بخطوة) **لا تحتوي بعد** على `StateRules.gs`
ولا على التعديلات الجديدة في `DevicesAssociations.gs`/`Beneficiaries.gs`/
`Delegates.gs`/`Auth.gs`/`ExecutionTracking.gs`/`Config.gs`/`Index.html`.
تلك التعديلات (المرحلتان الأولى والثانية معًا) موجودة في هذا المستودع فقط
حتى الآن. عند استئناف التركيب اليدوي يلزم: (أ) إضافة ملف `StateRules.gs`
الجديد، و(ب) استبدال محتوى الملفات المذكورة أعلاه بالكامل بالنسخة
المحدَّثة، و(ج) تشغيل `setupSheets()` مرة واحدة بعد اللصق لإضافة أعمدة
"يجب تغيير كلمة المرور" وكلمة المرور/الرمز السابقَين تلقائيًا لورقة
"المستخدمون" — قبل أي اختبار حي لمسارات التخصيص أو إعادة تعيين كلمة
المرور أو إعادة إنشاء رمز المندوب.

## ما هو مكتمل ومختبر فعليًا

| # | البند | الحالة |
|---|---|---|
| 1–5 | بناء المشروع الأساسي: أمن (تشفير، جلسات، صلاحيات، منع Formula Injection)، هوية الزاد البصرية، إزالة كل inline event handlers (سبب العطل الأصلي)، أدوات فحص آلية | ✅ مكتمل |
| 6 | مصادر مرجعية موحّدة (مناطق/مدن/تصنيفات/حالات اجتماعية) مع سقوط آمن لنص حر قبل تشغيل الترحيل | ✅ مكتمل |
| 7 | إعادة هندسة الأداء: ذاكرة تخزين مؤقت داخل الطلب + ذاكرة عبر الطلبات لكامل `getBootstrapData` + قياس تجريبي فعلي (`tools/perf-bench.js`) + تقسيم `Code.gs` إلى 12 ملف `.gs` موضوعي | ✅ مكتمل (`Index.html` بقي ملفًا واحدًا عمدًا — انظر التبرير أدناه) |
| 8 | بوابة تقديم الجمعيات العامة (بلا تسجيل دخول) + مراجعة إدارية (قبول/رفض) مع إنشاء الجمعية وحسابها تلقائيًا وكلمة مرور مؤقتة تُعرض مرة واحدة فقط | ✅ مكتمل |
| 9 | لوحة إدارة تفاعلية: كل مؤشر KPI وكل تنبيه قابل للنقر وينقل لصفحة مفلترة | ✅ مكتمل |
| 10 | إدارة الأنشطة الرئيسية والفرعية من داخل بوابة الإدارة: إضافة/تعديل بنموذج كامل، تبويبات تصفية (الكل/جارية/متأخرة/مكتملة/قادمة) | ✅ مكتمل |
| 11 | موقع المستفيد الجغرافي (إحداثيات اختيارية + زر تحديد الموقع الحالي) ومسار يوم المندوب التقريبي (خوارزمية أقرب-جار مجانية بالكامل عبر Haversine، بلا أي API مدفوع) | ✅ مكتمل |
| 12 | مراجعة اللغة الرسمية + مراجعة أمنية نهائية + سجل عمليات قابل للبحث والتصفية بالقسم | ✅ مكتمل |
| 13 | دمج الشعارين الحقيقيين (الزاد ومؤسسة أبانمي) من `assets/` في الهوية البصرية فعليًا | ✅ مكتمل |
| 14 | تقييم تقسيم الملفات: `Code.gs` قُسِّم فعليًا، `Index.html` بقي عمدًا (قرار موثَّق، لا تجاهل) | ✅ مكتمل |
| 15 | مراجعة أمنية آلية شاملة عبر تسعة محاور (`tools/security-test.js`، 79 اختبارًا) | ✅ مكتمل |
| 16 | دعم الإحداثيات في الاستيراد الجماعي (CSV وExcel) مع توافق كامل مع الملفات القديمة | ✅ مكتمل |
| 17 | تجهيز خطة اختبار حي كاملة ونسخة مرشحة للإصدار (`RELEASE.md`) | ✅ مكتمل |

نتائج القياس الفعلي للأداء (`node tools/perf-bench.js`، قبل commit `c188f74`
مقابل النسخة الحالية، بعدد قراءات `getDataRange().getValues()` الكاملة —
المقياس الحقيقي لتكلفة Sheets API):

| السيناريو | قبل | بعد | التحسّن |
|---|---|---|---|
| بناء بوابة جمعية واحدة | 10 | 6 | ‑40٪ |
| بناء بوابة إدارة واحدة | 8 | 9 | +12٪ (زيادة سطر واحد بسبب فحص طلبات الانضمام الجديدة — ليست تراجعًا) |
| دخول أول لجمعية | 11 | 6 | ‑45٪ |
| دخول ثانٍ فوري (خلال دقيقة) | 11 | 0 | ‑100٪ (كاش كامل) |

لا يقيس هذا زمن شبكة فعليًا (لا تتوفر بيئة Apps Script حية في جلسات التطوير
هذه) لكنه المتغيّر المتحكم مباشرة في ذلك الزمن.

## ما أُنجز في هذه الجولة الثانية (البنود 13–17)

- **الشعاران الحقيقيان**: الملفان المرفوعان في `assets/` تحقَّقا سليمين
  (توقيع PNG صحيح، قناة شفافية فعلية، بلا خلفية صلبة)، حُوِّلا إلى data URI
  (بعد تصغير الأبعاد فقط بالحفاظ الكامل على النسبة الأصلية) وحُقنا في ثابت
  `BRAND`. يظهران الآن فعليًا في شاشة الدخول والشريط الجانبي ورأس بوابة
  المندوب، وفق نفس تصميم الـ Lockup القائم مسبقًا (الزاد رئيسيًا، الشريك
  في صف مستقل بعبارة "بالشراكة مع"، بلا دمج ولا تكرار زخرفي).
- **تقسيم `Code.gs`**: قُسِّم من ملف واحد (1810 سطرًا) إلى 12 ملف `.gs`
  موضوعي (`Config`, `ReferenceData`, `Auth`, `Bootstrap`, `Beneficiaries`,
  `Delegates`, `DevicesAssociations`, `Applications`, `ExecutionTracking`,
  `Normalize`, `DataUtils`, `Validation`) — تقسيم آمن ومختبَر لأن Apps
  Script يدمج كل ملفات `.gs` تلقائيًا ضمن نطاق عام واحد بصرف النظر عن اسم
  الملف أو ترتيبه. تحقّق فعلي عبر `diff` أن دمج الملفات الاثني عشر بالترتيب
  الأصلي يعيد المحتوى الأصلي حرفًا بحرف قبل أي تعديل لاحق. **`Index.html`
  بقي ملفًا واحدًا عمدًا** — القرار وتبريره التقني الكامل موثَّقان في رسالة
  commit التقسيم وفي قسم "المتبقي" أدناه.
- **مراجعة أمنية آلية شاملة**: أداة جديدة `tools/security-test.js` (79
  اختبارًا) تغطي: المصادقة والجلسات (سقف العمر المطلق، الإبطال الفوري)،
  مصفوفة صلاحيات شاملة لكل دالة خادم حساسة ضد كل الأدوار غير المصرَّح بها،
  عزل الجمعيات (IDOR) بسيناريو جمعيتين ومندوبين فعليين، XSS وحقن الصيغ
  بكتابة/قراءة فعلية، حدود المدخلات، رفع الملفات (أنواع MIME مزوَّرة
  وتجاوز الحجم)، تحديد المعدّل، عدم تسريب الأسرار (تحقّق ديناميكي على
  حمولة API الفعلية)، وسلامة سجل العمليات (إلحاق فقط، كل دالة تعديل
  تستدعي `audit_`). إصلاح فعلي وُجد أثناء المراجعة: `saveBeneficiary` كانت
  الدالة الوحيدة التي تستخدم `payload.id` خامًا دون `cleanId_` (غير قابل
  للاستغلال فعليًا، لكن أُصلح للاتساق الدفاعي).
- **الإحداثيات في الاستيراد الجماعي**: `importBeneficiaries` (CSV)
  و`inspectBeneficiaryExcel` (معاينة Excel) يدعمان الآن عمودين اختياريين
  لخط العرض/الطول، بتحقّق كامل عبر `optionalCoordinate_` ورفض واضح لكل
  صف خاطئ دون إسقاط الاستيراد كله — مع توافق تام مع ملفات قديمة بلا هذين
  العمودين.
- **`RELEASE.md`**: خطة اختبار حي كاملة — إنشاء نسخة تجريبية معزولة، ترتيب
  تشغيل `setupSheets`/`previewPhoneNormalization`/`migratePhoneNumbers`/
  `migrateReferenceData` (موصوف لا مُنفَّذ)، بيانات اختبار وهمية جاهزة
  للنسخ، قائمة اختبار يدوية ببنود لكل زر ومسار وحالة في البوابات الثلاث
  مع النتيجة المتوقعة لكل بند، وخطة رجوع (Rollback) واضحة.

## المرحلة الأولى: سلامة الحالات والترابط (جهاز ↔ مستفيد ↔ تسليم ↔ مندوب)

**الهدف**: منع أي تعديل مستقل لحالة الجهاز أو حالة التسليم قد ينتج عنه وضع
غير متّسق (جهاز "مخصص" بلا مستفيد، مستفيد "تم التسليم" بجهاز لا يزال
"بالمستودع"، تخصيص جهاز واحد لمستفيدين، تسليم جهاز لم يخرج مع مندوب، إلخ)،
عبر مصدر قواعد واحد يستخدمه كل مسار تعديل.

- **`StateRules.gs` (ملف جديد)**: يعرّف خريطتي انتقال مجمَّدتين
  `DEVICE_STATUS_TRANSITIONS_` و`DELIVERY_STATUS_TRANSITIONS_` — لكل حالة
  حالية، قائمة الحالات التالية المسموحة فقط. `assertDeviceTransition_` /
  `assertDeliveryTransition_` ترمي خطأ عربيًا واضحًا لأي انتقال غير مسموح
  أو حالة غير معروفة. **الحالتان النهائيتان "تم التسليم" (جهاز وتسليم) لا
  تملكان حلقة ذاتية عمدًا** — إعادة "تأكيد" تسليم مكتمل بالفعل تُرفض دائمًا،
  لا تُقبل كعملية بلا أثر (هذا فرق دقيق ومهم جرى تصحيحه أثناء التصميم قبل
  أي تشغيل).
- **قواعد الحالة المفروضة فعليًا في كل مسار تعديل**:
  - `saveDevice` (نموذج الجهاز اليدوي): لا يقبل ضبط "مع المندوب" أو "تم
    التسليم" يدويًا إطلاقًا — فقط `assignDelegate`/`confirmDelivery` تصلان
    لهما. يمنع تخصيص جهاز "نشط" (مخصص/مع المندوب/تم التسليم) لمستفيد آخر
    دون إرجاعه للمستودع أولًا. تعديل حقول وصفية (الاسم/الملاحظات) بلا تغيير
    الحالة الفعلية مسموح دائمًا حتى لو كانت الحالة نهائية.
  - `assignDelegate`: يتطلب وجود جهاز واحد على الأقل "مخصص" أو "مع المندوب"
    فعلًا للمستفيد (لا يقبل تعيين مندوب لمستفيد بلا أجهزة)، وينقل كل الأجهزة
    المخصَّصة إلى "مع المندوب" وحالة التسليم إلى "خرج مع المندوب" معًا داخل
    قفل (`LockService`).
  - `confirmDelivery`: يقبل فقط الأجهزة التي حالتها "مع المندوب" فعلًا،
    ويُحدِّث المستفيد وكل أجهزته المرتبطة إلى "تم التسليم" كوحدة واحدة داخل
    قفل، مع إعادة قراءة وتحقق من الحالة *داخل* القفل لإغلاق نافذة السباق
    عند نقرتين متتاليتين سريعتين (تأكيد مزدوج).
  - `updateDeliveryStatus` (تعذر التسليم): لا يقبل تسجيل "تعذر التسليم" إلا
    من "خرج مع المندوب" (أو إعادة محاولة سابقة فاشلة) — لا يمكن تسجيل تعذّر
    لمستفيد لم تخرج أجهزته أصلًا. لا يغيّر حالة الأجهزة (تبقى "مع المندوب").
  - `importBeneficiaries` (الاستيراد الجماعي): لا يكتب أي حالة جهاز أو
    تسليم إطلاقًا — يُنشئ سجلات مستفيدين جدد فقط، فلا يمكنه تجاوز القواعد.
- **`diagnoseStateIntegrity()` (قراءة فقط، جديدة)**: تفحص كل الأجهزة
  والمستفيدين وتُخرج تقريرًا منظَّمًا (`{ok, issueCount, bySeverity, issues}`)
  يغطي عشرة أنواع تعارض محتملة (حالة غير معروفة، جهاز يتيم الحالة، جهاز
  مرتبط بمستفيد لكن "بالمستودع"، جهاز يشير لمستفيد غير موجود، تعارض جمعية،
  مستفيد "تم التسليم" بأجهزة غير متّسقة، إلخ). آمنة للتشغيل اليدوي من محرر
  Apps Script في أي وقت — لا تكتب أي شيء (مُتحقَّق منها باختبار مخصَّص).
- **`repairStateIntegrityIssues()` (مُجهَّزة، لم تُشغَّل)**: تُصلح فقط
  الحالات الآمنة ذات التفسير الوحيد القاطع، وتترك أي حالة غامضة (كتعارض
  الجمعية) دون لمس مع توثيقها في `skipped`. **غير مُستدعاة من أي دالة أخرى
  في المشروع ولا تُشغَّل تلقائيًا أبدًا** (مُتحقَّق منه بفحص أن اسمها يظهر
  مرة واحدة فقط في كامل المصدر — تعريفها نفسه). لم تُشغَّل على أي بيانات —
  لا محاكاة ولا حية — تنفذ فقط عند استدعائها يدويًا صراحة من محرر Apps
  Script بعد مراجعة تقرير `diagnoseStateIntegrity()`.
- **اختبارات جديدة (`tools/state-test.js`, 53 تأكيدًا)**: تغطي كل زوج
  انتقال مسموح ومرفوض على مستوى الوحدة، دورة حياة كاملة ناجحة عبر الدوال
  الفعلية (تخصيص → خروج مع مندوب → تسليم)، رفض إعادة التأكيد المزدوج مع
  التحقق من عدم إضافة أكثر من سجل تسليم واحد، دورة تعذّر ثم إعادة محاولة
  ناجحة، رفض التخصيص المزدوج لجهاز واحد لمستفيدين، منع الضبط اليدوي
  للحالات المحمية، سيناريوهات فساد بيانات مُفتعلة ثلاثة يكتشفها التشخيص
  ويُصلحها الإصلاح المُجهَّز بنجاح، وفحوص ثابتة تتأكد أن كل مسارات التعديل
  الأربعة تستخدم فعليًا دوال التحقق المركزية نفسها.
- **ملاحظة تصميم موثَّقة**: حالة التسليم الوسيطة "جاري التجهيز" لم تعد
  تُستخدم فعليًا من أي مسار حالي (بقيت في `DELIVERY_STATUSES` وخريطة
  الانتقال لتوافق بيانات قديمة/مستوردة فقط) — `assignDelegate` ينقل مباشرة
  من "لم يبدأ" إلى "خرج مع المندوب" بمجرد تخصيص جهاز وتعيين مندوب معًا.

**لم يُنفَّذ بعد من نطاق سلامة الحالات (يحتاج قرارًا أو عملًا لاحقًا)**:
- تحديث واجهة `deviceForm()` في `Index.html`: القائمة المنسدلة لحالة
  الجهاز لا تزال تعرض بصريًا كل الحالات الخمس (بما فيها "مع المندوب"/"تم
  التسليم") رغم أن الخادم يرفضها دائمًا الآن برسالة واضحة. تحسين شكلي
  (تعطيل/إخفاء تلك الخيارات في القائمة) مؤجَّل عمدًا — الطلب الصريح كان
  عدم البدء بتحسينات الشكل قبل إنهاء سلامة الحالات أولًا.
- تشغيل `repairStateIntegrityIssues()` على أي بيانات حية: بانتظار تشغيل
  `diagnoseStateIntegrity()` أولًا على النسخة الحية بعد تركيب هذه الملفات،
  ومراجعة تقريرها، وموافقتك الصريحة.
- المرحلة الثانية (إن وُجدت) من سلامة الحالات: هذا العمل غطى فقط ما طُلب
  صراحة (المرحلة الأولى) — أي توسعة أخرى (كإدارة "تالف" أعمق، أو سيناريوهات
  استرجاع/عكس بعد التسليم) تحتاج طلبًا صريحًا جديدًا.

## المرحلة الثانية: إدارة حسابات الجمعيات والمناديب واستعادة الوصول

**الهدف**: منح الإدارة أداة آمنة لاستعادة وصول جمعية فقدت كلمة مرورها أو
اشتُبه في اختراق حسابها، ومنح الجمعية رؤية وضبطًا كاملَين على مناديبها،
دون أي ثغرة صلاحيات أو تسريب سرّ — مع فرض كل ذلك من الخادم لا من الواجهة.

### أولًا: حساب الجمعية
- **`resetAssociationPassword(token, associationId)` (جديدة، `DevicesAssociations.gs`، ADMIN فقط)**:
  تُنشئ كلمة مرور مؤقتة قوية عشوائية (`generateStrongTempPassword_`، تجتاز
  `assertStrongPassword_` دائمًا)، تحفظ كلمة المرور القديمة وملحها في عمودين
  جديدين "سابقين" (لمنع إعادة استخدامها لاحقًا)، تضبط عمود "يجب تغيير كلمة
  المرور" إلى "نعم"، وتُبطل جلسة حساب الجمعية نفسه فورًا (لا تمسّ جلسات
  مناديبها — سرّان مستقلان). محمية بقفل (`LockService`) وتحديد معدل
  (`throttle_`، 5 محاولات/15 دقيقة لكل جمعية) لمنع تكرار أو تزامن يُبقي أكثر
  من كلمة مرور صالحة. تُعيد كلمة المرور الجديدة **فقط** في استجابة هذا
  الاستدعاء — لا تُسجَّل في `audit_` (يُسجَّل اسم الجمعية فقط)، ولا تُخزَّن
  في أي مكان آخر نصًا صريحًا.
- **إجبار تغيير كلمة المرور المؤقتة عند أول دخول**: عمود جديد "يجب تغيير
  كلمة المرور" في ورقة "المستخدمون". `requireSession_` (الآن يقبل `opts`
  ثالثًا) يرفض **كل** استدعاء خادم لحساب جمعية يحمل هذا الوسم إلا
  `changePassword`/`logout` — رفض حقيقي من الخادم، لا إخفاء في الواجهة، حتى
  لو استُدعيت أي دالة أخرى مباشرة متجاوزةً الواجهة. `loginUser_` لا يعيد
  `bootstrap` إطلاقًا لحساب بهذا الوسم — فقط `{token, user, mustChangePassword:true}`.
- **منع إعادة استخدام كلمة المرور السابقة**: `changePassword` (`ExecutionTracking.gs`)
  يقارن كلمة المرور الجديدة بالحالية (رفض إن كانت مطابقة) وبالسابقة المحفوظة
  (رفض إن كانت مطابقة) — طبقة حماية واحدة ضمن البنية الحالية (لا يُحفظ سجل
  كامل لكل كلمات المرور التاريخية، فقط آخر واحدة سبقت الحالية، محفوظة فعليًا
  في العمودين الجديدين عند كل تغيير ناجح أو إعادة تعيين).
- **الواجهة**: زر "إعادة تعيين كلمة المرور" في صفحة تفاصيل الجمعية (ADMIN
  فقط) يفتح نافذة تأكيد وتحذير واضحة (تحذير قطع الجلسات فورًا)، ثم يعرض
  كلمة المرور الجديدة مرة واحدة بزر نسخ صريح ورسالة تحذّر من عدم ظهورها
  مجددًا وتوصي بإعادة تعيين جديدة إن ضاعت بدل محاولة استرجاعها. شاشة كاملة
  إلزامية جديدة (`renderForcePasswordChange`) تظهر تلقائيًا بعد تسجيل دخول
  حساب بهذا الوسم، ولا تسمح بأي تنقّل قبل تغيير كلمة المرور فعليًا؛ بعد
  النجاح تُقطع الجلسة (بما يشمل الحالية) ويُعاد المستخدم لشاشة الدخول
  برسالة واضحة.

### ثانيًا: حساب المندوب
- **صفحة تفاصيل المندوب (`viewDelegate`, جديدة, `Index.html`)**: تعرض رقم
  الجوال، الجمعية، آخر دخول، عدد المهام الحالية، عدد المستفيدين المخدومين
  (تم تسليمهم)، وجدول كامل بالمستفيدين المكلَّف بهم حاليًا (الاسم ورقمه
  وحالة التسليم) — بيانات مُشتقة بالكامل من `state.data` المُصفّى مسبقًا من
  الخادم حسب الدور (لا استدعاء خادم إضافي، ولا كشف بيانات خارج نطاق الفاعل).
- **`regenerateDelegateCode`**: مُعزَّزة بقفل (`LockService`) وتحديد معدل
  (`throttle_`، 8 محاولات/15 دقيقة لكل مندوب) لمنع بقاء أكثر من رمز صالح
  واحد مع طلبات متكررة أو متزامنة. الرمز القديم يُبطَل فور استبدال تجزئته
  وملحه، وجلسة المندوب المفتوحة بالرمز القديم تُقطع فورًا (`revokeSessions_`).
  الواجهة: نافذة تأكيد قبل التنفيذ، وعرض الرمز الجديد مرة واحدة بزر نسخ
  (كان معروضًا بلا زر نسخ سابقًا — أُضيف الآن).
- **تفعيل/تعطيل المندوب (`setDelegateStatus`, موجودة مسبقًا)**: مؤكَّد أنها
  تقطع جلسات المندوب فورًا عند التعطيل، وأن دخوله بالرمز يُرفض فور التعطيل
  (كلاهما مختبر الآن صراحة في `tools/account-test.js`). نافذة تأكيد موجودة
  مسبقًا في الواجهة.
- لا يُسجَّل رمز الدخول (الجديد أو القديم) كنص صريح في `audit_` عند أي من
  هذه العمليات — مُتحقَّق منه اختباريًا بفحص محتوى سجل العمليات فعليًا.

### ثالثًا: الصلاحيات والأمان
- `resetAssociationPassword`: `requireSession_(token, ['ADMIN'])` — لا تصل
  إليها أي جمعية أو مندوب مهما حاول.
- عزل الجمعيات في إدارة المناديب: الفحص الموجود مسبقًا
  (`if (user.role === 'ASSOCIATION' && ...associationId !== user.associationId)`)
  في `regenerateDelegateCode`/`setDelegateStatus`/`saveDelegate` مؤكَّد الآن
  باختبار فعلي بجمعيتين ومندوبين حقيقيين: جمعية لا تستطيع إعادة إنشاء رمز
  أو تعطيل مندوب جمعية أخرى، ومحاولاتها المرفوضة لا تترك أي أثر جزئي.
  الإدارة (ADMIN) تحتفظ بصلاحيتها الإشرافية الكاملة على كل المناديب — لم
  تُقيَّد، لأن ذلك لم يكن مطلوبًا وكان سيكسر سلوكًا قائمًا مقصودًا.
  Admin لا يستقبل أي سرّ قديم عبر `resetAssociationPassword` — فقط السرّ
  الجديد المُنشَأ للتو.
- التحقق من الدور والملكية مفروض بالكامل من الخادم (`requireSession_`
  بالأدوار الصريحة + فحوص `associationId` صريحة) — لا اعتماد على إخفاء
  أزرار الواجهة في أي مسار من هذه المرحلة.
- تحديد معدل وقفل تزامن على `resetAssociationPassword` و`regenerateDelegateCode`
  كما ورد أعلاه — مختبَر بمحاكاة استدعاءات متتالية سريعة فعليًا.
- لم تُعدَّل أي بيانات حية ولم تُشغَّل أي دالة ترحيل أو إصلاح خلال هذه
  المرحلة — كل ما سبق مُنفَّذ ومختبَر داخل بيئة محاكاة Node.js فقط.

### رابعًا: تحسين دورة الدخول
- رسائل الخطأ العامة (بريد/كلمة مرور/رمز مندوب خاطئ) كانت بالفعل موحَّدة
  وغير كاشفة لوجود الحساب من قبل هذه المرحلة (`'بيانات الدخول غير صحيحة'`،
  `'رمز الدخول غير صحيح أو المندوب غير نشط'`) — رُوجعت ومؤكَّد أنها لا تزال
  كذلك، لم تُغيَّر.
- رسالة انتهاء الجلسة (`'انتهت الجلسة. يرجى تسجيل الدخول مجددًا'`) كانت
  موجودة وواضحة مسبقًا وتُعيد المستخدم لشاشة الدخول تلقائيًا (`reportError`
  → `doLogout`) — لم تُغيَّر.
- **جديد**: حساب مُلزَم بتغيير كلمة مرور مؤقتة أو حساب معطَّل لا يستطيع
  تجاوز القيود عبر استدعاء API مباشر — مُتحقَّق منه الآن بفحص اختباري صريح
  يستدعي `getBootstrapData`/`saveBeneficiary` مباشرة بتوكن حساب مُلزَم
  بالتغيير ويؤكد الرفض من الخادم لا الواجهة فقط.
- **جديد**: واجهة تغيير كلمة المرور المؤقتة عند أول دخول للجمعية
  (`renderForcePasswordChange` في `Index.html`) — شاشة كاملة إلزامية، لا
  اختيارية ولا قابلة للتجاوز بالتنقّل.

### اختبارات جديدة (`tools/account-test.js`, 53 تأكيدًا)
عشرة أقسام: نجاح إعادة تعيين كلمة مرور الجمعية وقوة الكلمة الناتجة، إبطال
الجلسة القديمة للجمعية (مع تأكيد عدم تأثّر جلسة مندوبها)، فرض تغيير كلمة
المرور عند أول دخول عبر الدوال الفعلية (بما في ذلك محاولة تجاوز مباشرة عبر
API)، عدم تسجيل أي سرّ في سجل العمليات، إعادة إنشاء رمز مندوب وإبطال
القديم فورًا (مع قطع جلسته المفتوحة)، منع دخول مندوب معطَّل ومتابعة جلسة
قديمة، عزل الجمعيات الكامل عن مناديب بعضها، مقاومة تكرار الطلب والتزامن
(كلمة مرور/رمز واحد صالح فقط رغم استدعاءات متتالية، وتحديد المعدّل يمنع
سلسلة طويلة)، عدم كشف أي تجزئة أو ملح أو سرّ قديم في أي استجابة خادم فعلية
(بمقارنة القيم الحرفية لا أسماء الحقول فقط)، وأخيرًا فحص صريح أن قواعد
سلامة الحالات من `c449d3d` لم تتأثر بهذه التعديلات.

**لم يُنفَّذ بعد من نطاق إدارة الحسابات (يحتاج قرارًا أو عملًا لاحقًا)**:
- إجبار تغيير كلمة المرور عند **إنشاء** حساب جمعية جديد (طلب انضمام مقبول
  أو إضافة يدوية من الإدارة) — طُبِّق الإلزام فقط على مسار **إعادة
  التعيين** الصريح لأن هذا ما طلبه النص حرفيًا. تمديده لمسار الإنشاء
  الابتدائي منطقي ومتّسق لكنه قرار مستقل يحتاج طلبًا صريحًا.
- منع إعادة استخدام كلمة المرور محدود بعمق واحد فقط (الكلمة السابقة
  مباشرة، لا سجل كامل لكل كلمات المرور التاريخية) — موثَّق صراحة كقيد
  "ضمن البنية الحالية" في الكود والاختبارات، لا كسهو.
- لا توجد صفحة "تفاصيل حساب الجمعية" مستقلة موسَّعة (سجل تسجيلات دخول
  مفصَّل، مثلًا) — التفاصيل الحالية ضمن النافذة المنبثقة القائمة فقط.

## المتبقي (قرارات مدروسة موثَّقة، وليست إغفالًا)

- **`Index.html` لم يُقسَّم**: تقسيمه عبر `HtmlService.createTemplateFromFile`
  + `include()` لا يقلّل حجم ما يصل للمتصفح فعليًا (الأجزاء تُدمَج نصيًا في
  الخادم قبل الإرسال، فالنتيجة مطابقة لملف واحد) — الفائدة تنظيمية بحتة.
  المخاطرة حقيقية: كل جافاسكربت الواجهة برنامج واحد مترابط عبر نطاق عام
  مشترك وتفويض أحداث (event delegation) على DOM واحد، وتقسيم ملف التأليف
  يفتح بابًا لأخطاء حدود الملفات — بالضبط فئة العطل الذي بدأ منه هذا
  المشروع (inline event handlers). القرار: الإبقاء عليه ملفًا واحدًا؛
  يُعاد النظر فقط إن أصبح حجمه (~2500 سطر) عائقًا فعليًا على التحرير.
- **اختبار حي فعلي على Apps Script/Google Sheets**: لم يحدث من داخل أي
  جلسة تطوير (حجب شبكي مؤكَّد لـ `script.google.com`/`docs.google.com` من
  بيئة التنفيذ). `RELEASE.md` يُجهِّز كل ما تحتاجه لتنفيذه بنفسك — هذا هو
  الأثر العملي الوحيد المتبقي فعليًا قبل أي نشر واسع.
- **اختبار اختراق يدوي مستقل احترافي**: `tools/security-test.js` فحص آلي
  على مستوى الكود والمنطق داخل بيئة محاكاة، وليس بديلًا عن مراجعة أمنية
  مستقلة بمعايير OWASP/PTES تغطي الشبكة والبنية التحتية والنشر الفعلي.
- **الاستجابة الكاملة عبر كل حجم شاشة**: تحسينات جزئية مطبَّقة (جداول
  قابلة للتمرير الأفقي، شريط جانبي منهار على الجوال) لكن لم يُجرَ اختبار
  فعلي في متصفح حقيقي على أجهزة متعددة — بند في قائمة الاختبار اليدوي
  بـ`RELEASE.md` (القسم 5.5) لتنفيذه أنت مباشرة.
- **إثبات تسليم فاشل بتفاصيل أوسع**: الأساس موجود (`FAILED_REASONS`،
  `updateDeliveryStatus`) لكن لم تُراجَع تجربة المندوب الكاملة لحالات
  التعذر المتكررة أو إعادة الجدولة.

## الترحيلات المكتوبة ولم تُشغَّل (بانتظار قرارك)

كل ما يلي **آمن ومتكرر التشغيل (idempotent)** ولا يحذف بيانات، لكنه **لم
يُشغَّل على أي بيانات حقيقية** بناءً على تعليماتك الصريحة بعدم تشغيل أي
ترحيل دون إذن مباشر. **`RELEASE.md` القسم 3 يوثّق الترتيب الموصى به
بالتفصيل** — الملخص هنا:

- `setupSheets()` — تُنشئ كل الأوراق والأعمدة الناقصة (بما فيها "طلبات
  انضمام الجمعيات" وعمودا الموقع الجغرافي). آمنة للتشغيل المتكرر.
- `previewPhoneNormalization()` — قراءة فقط، تقرير بكل رقم سيتغيّر تنسيقه.
- `migratePhoneNumbers()` — يطبّق التطبيع الفعلي؛ لا تُشغّلها قبل مراجعة
  تقرير `previewPhoneNormalization()`.
- `migrateReferenceData()` — يبذر ورقة "البيانات المرجعية" فتتحوّل حقول
  المنطقة/المدينة من نص حر لقوائم منسدلة مترابطة في كل النماذج.

## ملفات مهمة

| الملف | الدور |
|---|---|
| `Config.gs` … `Validation.gs` + `StateRules.gs` (13 ملفًا) | الخادم الكامل، مقسَّم موضوعيًا — انظر جدول الملفات في `README.md` |
| `Index.html` | الواجهة الكاملة (ملف واحد عمدًا) |
| `assets/` | ملفا الشعار الأصليان (مرجع؛ النسخة المستخدَمة فعليًا مضمَّنة data URI في `Index.html`) |
| `tools/gs-manifest.js` | ترتيب دمج ملفات `.gs` لأدوات الاختبار المحلية فقط |
| `tools/verify.js` | فحص ثابت — 31 تأكيدًا |
| `tools/smoke.js` | اختبار DOM تشغيلي — 117 تأكيدًا |
| `tools/server-test.js` | اختبار منطق خادم بمحاكاة Apps Script — 128 تأكيدًا |
| `tools/security-test.js` | مراجعة أمنية آلية — 79 اختبارًا |
| `tools/state-test.js` | اختبار سلامة الحالات والترابط — 53 تأكيدًا |
| `tools/account-test.js` | اختبار إدارة الحسابات: كلمة مرور الجمعية، رمز المندوب، العزل، التزامن، الأسرار — 53 تأكيدًا |
| `tools/perf-bench.js` | قياس تجريبي لعدد قراءات الشيت (اختياري) |
| `README.md` | تعليمات التثبيت والتشغيل والنشر الكاملة على Apps Script |
| `RELEASE.md` | خطة الاختبار الحي الكاملة والنسخة المرشحة للإصدار وخطة الرجوع |

## نتائج الاختبارات (آخر تشغيل قبل هذا التسليم)

```
node tools/verify.js         → 31/31
node tools/smoke.js          → 117/117
node tools/server-test.js    → 128/128
node tools/security-test.js  → 79/79
node tools/state-test.js     → 53/53
node tools/account-test.js   → 53/53
```

**المجموع: 461/461، بلا أي تراجع وظيفي عبر كل commit في هذا الفرع.**

## نقطة المتابعة الدقيقة للجلسة القادمة (إن وُجدت)

كل الأعمال البرمجية المخطَّطة مكتملة ومدفوعة. لا توجد أي وظيفة نصف
منجزة أو تعديلات غير محفوظة في هذا الفرع. الخطوة العملية الوحيدة
المتبقية هي **بشرية وليست برمجية**: تنفيذ قائمة الاختبار الحي في
`RELEASE.md` بنفسك على نسخة Google Sheets تجريبية (بعد تركيب ملفات
المرحلتين الأولى والثانية أولًا — انظر التنبيه أعلى هذا الملف)، ثم القرار
بالنشر الفعلي أو الدمج بعد رضاك عن النتائج. إن أردت جلسة عمل جديدة لاحقًا:

1. شغّل الستة (`verify.js`/`smoke.js`/`server-test.js`/`security-test.js`/
   `state-test.js`/`account-test.js`) أولًا للتأكد من عدم وجود تراجع منذ
   آخر commit.
2. راجع نتائج اختبارك الحي وفق `RELEASE.md`؛ أي بند فشل يستحق تحقيقًا
   قبل أي عمل جديد.
3. إن رغبت في إطلاق واسع النطاق: اطلب اختبار اختراق مستقل احترافي.

## روابط

- الفرع: `claude/code-index-review-kz5k4u`
- المستودع: `mroan7241-create/-`
- Pull Request #1 (مفتوح، غير مدموج): https://github.com/mroan7241-create/-/pull/1
