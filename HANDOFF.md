# تقرير تسليم — نظام متابعة توزيع الأجهزة (جمعية الزاد)

آخر تحديث: هذا الملف يعكس حالة الفرع `claude/code-index-review-kz5k4u` عند
commit **`ccf5ad9`** (أحدث commit عند كتابة هذا التقرير). مستودع GitHub هو
المصدر المعتمد الوحيد — تجاهل أي نسخة مطابقة من ملفات لصقت سابقًا في المحادثة.

## الحالة العامة

جميع البنود الاثني عشر المخطَّطة **مكتملة ومختبرة آليًا** (260 تأكيدًا عبر
ثلاث أدوات، جميعها ناجحة عند كل commit). لم يُنشر أي إصدار جديد على Apps
Script الحي، ولم تُشغَّل أي دالة ترحيل على بيانات إنتاجية، ولم يُدمَج أي
Pull Request — كل ذلك بانتظار قرارك.

## ما هو مكتمل ومختبر فعليًا

| # | البند | الحالة |
|---|---|---|
| 1–5 | بناء المشروع الأساسي: أمن (تشفير، جلسات، صلاحيات، منع Formula Injection)، هوية الزاد البصرية، إزالة كل inline event handlers (سبب العطل الأصلي)، أدوات فحص آلية | ✅ مكتمل |
| 6 | مصادر مرجعية موحّدة (مناطق/مدن/تصنيفات/حالات اجتماعية) مع سقوط آمن لنص حر قبل تشغيل الترحيل | ✅ مكتمل |
| 7 | إعادة هندسة الأداء: ذاكرة تخزين مؤقت داخل الطلب + ذاكرة عبر الطلبات لكامل `getBootstrapData` + قياس تجريبي فعلي (`tools/perf-bench.js`) | ✅ **الجزء الأساسي مكتمل.** تقسيم `Code.gs`/`Index.html` إلى ملفات متعددة **لم يُنفَّذ** (أُجِّل عمدًا — انظر "المتبقي") |
| 8 | بوابة تقديم الجمعيات العامة (بلا تسجيل دخول) + مراجعة إدارية (قبول/رفض) مع إنشاء الجمعية وحسابها تلقائيًا وكلمة مرور مؤقتة تُعرض مرة واحدة فقط | ✅ مكتمل |
| 9 | لوحة إدارة تفاعلية: كل مؤشر KPI وكل تنبيه قابل للنقر وينقل لصفحة مفلترة | ✅ مكتمل |
| 10 | إدارة الأنشطة الرئيسية والفرعية من داخل بوابة الإدارة: إضافة/تعديل بنموذج كامل، تبويبات تصفية (الكل/جارية/متأخرة/مكتملة/قادمة) | ✅ مكتمل |
| 11 | موقع المستفيد الجغرافي (إحداثيات اختيارية + زر تحديد الموقع الحالي) ومسار يوم المندوب التقريبي (خوارزمية أقرب-جار مجانية بالكامل عبر Haversine، بلا أي API مدفوع) | ✅ مكتمل |
| 12 | مراجعة اللغة الرسمية (فحص منهجي، لم يُعثر على صياغة غير رسمية) + مراجعة أمنية نهائية (رفع الملفات، عزل الجمعيات، عدم تسريب الأسرار) + سجل عمليات قابل للبحث والتصفية بالقسم | ✅ مكتمل |

نتائج القياس الفعلي للأداء (`node tools/perf-bench.js`، قبل commit `c188f74`
مقابل النسخة الحالية، بعدد قراءات `getDataRange().getValues()` الكاملة —
المقياس الحقيقي لتكلفة Sheets API):

| السيناريو | قبل | بعد | التحسّن |
|---|---|---|---|
| بناء بوابة جمعية واحدة | 10 | 6 | ‑40٪ |
| بناء بوابة إدارة واحدة | 8 | 8 | بلا تغيير (لا تكرار أصلًا) |
| دخول أول لجمعية | 11 | 6 | ‑45٪ |
| دخول ثانٍ فوري (خلال دقيقة) | 11 | 0 | ‑100٪ (كاش كامل) |

لا يقيس هذا زمن شبكة فعليًا (لا تتوفر بيئة Apps Script حية في جلسات التطوير
هذه) لكنه المتغيّر المتحكم مباشرة في ذلك الزمن.

## ما أُنجز في هذه الجولة (البنود 10–12)

- **إدارة الأنشطة (`saveActivity`)**: يضيف نشاطًا جديدًا أو يعدّل نشاطًا
  قائمًا عبر مطابقة ثلاثية (المرحلة، النشاط الرئيسي، النشاط الفرعي) —
  لا يوجد عمود معرّف تسلسلي مستقل في ورقة "إدارة الأنشطة" (تصميم قائم منذ
  البداية)، فأُضيفت `updateRowByMatch_` كأداة عامة للتحديث بمطابقة عدة
  أعمدة معًا بدل معرّف واحد. الحالة تُحسب تلقائيًا كما كانت إن تُركت فارغة.
- **موقع المستفيد الجغرافي**: عمودان إضافيان بالكامل في نهاية جدول
  "المستفيدون" ("خط العرض"، "خط الطول") — الأعمدة العشرون الأصلية لم
  تتغير. اختياريان تمامًا، يُحصَران ضمن مربع إحداثيات المملكة تقريبًا عند
  إدخالهما فقط (`optionalCoordinate_`).
- **مسار يوم المندوب**: تبويب "مسار اليوم" الجديد في بوابة المندوب يطلب
  الموقع الحالي عبر `navigator.geolocation` (مجاني، مدمج في المتصفح، بلا
  أي مفتاح API)، ثم يرتّب المستفيدين ذوي الإحداثيات بخوارزمية أقرب-جار
  تقريبية. من لا تتوفر له إحداثيات يبقى في قائمة منفصلة بالعنوان النصي.
- **سجل العمليات**: أصبح قابلًا للبحث (بالمستخدم/الإجراء/رقم السجل)
  والتصفية حسب القسم، بنفس مكوّن `toolbar` المستخدم في بقية الصفحات.
- **المراجعة اللغوية**: فحص منهجي لكل النصوص الظاهرة في `Index.html`
  بحثًا عن عبارات عامية أو تجريبية (تمام، يلا، أوكي، إلخ) — **لم يُعثر
  على أي مثال حقيقي**؛ كل المطابقات الأولية كانت أجزاءً من كلمات فصيحة
  (مثل "تخزين" التي تحتوي تسلسل "زين" حرفيًا). الصياغة القائمة رسمية منذ
  البداية ولم تحتَج تعديلًا.
- **المراجعة الأمنية النهائية**: تأكيد أن رفع الملفات (استيراد Excel،
  صور إثبات التسليم) يتحقق من نوع الملف عبر رأس `data:` نفسه لا الامتداد
  فقط، بحدود حجم صارمة (8 و6 ميجابايت)، وأن الملف المؤقت على Drive يُنقَل
  للمهملات دائمًا حتى عند الفشل. تأكيد عدم وجود ثغرات IDOR جديدة في أي من
  الدوال المضافة هذه الجلسة (كل دالة تلمس بيانات جمعية تتحقق من مطابقة
  `associationId` مع الفاعل). تأكيد أن كلمة مرور الجمعية المؤقتة الجديدة
  لا تُكتب في سجل العمليات ولا في أي مكان دائم — تُعاد فقط في استجابة API
  لمرة واحدة عند القبول.

## المتبقي (أُجِّل عمدًا، ليس عطلًا)

- **تقسيم ملفات Code.gs/Index.html**: حاليًا كل شيء في ملف خادم واحد
  (~1750 سطرًا) وملف واجهة واحد (~2450 سطرًا). Apps Script يدعم تقسيم
  `.gs` إلى عدة ملفات (تُدمج تلقائيًا بنطاق عام واحد) وتقسيم HTML عبر
  `HtmlService.createTemplateFromFile` + `include()`. أُجِّل هذا لأنه
  إعادة هيكلة عالية الخطورة (تتطلب إعادة اختبار كل مسار) بعائد أداء أقل
  من الكاش المطبَّق فعلًا؛ يُنصح بتنفيذه فقط إن أصبح حجم الملفين عائقًا
  فعليًا على التحرير، وليس كهدف بحد ذاته.
- **الشعارات الفعلية**: لم تُضمَّن أي بيانات صورة حقيقية لشعار الزاد أو
  شعار مؤسسة أبانمي — كلاهما لا يزال فارغًا في ثابت `BRAND` أعلى
  `Index.html` (النظام يعرض بديلًا نصيًا أنيقًا بدلًا منهما تلقائيًا، وهذا
  سلوك مقصود لا عطل). سبب عدم التضمين: كل الصور المرسلة سابقًا عبر
  المحادثة إما غير قابلة للوصول من هذه البيئة (حجب شبكي مؤكّد على
  `i.postimg.cc` و`script.google.com` و`docs.google.com`) أو وصلت تالفة
  عند إعادة البناء من نص المحادثة (تحقّق فعلي عبر فاحص PNG مخصص، لا
  تخمين). **للإكمال**: أرفق ملفَي الشعار كملفات حقيقية (رفع مباشر إلى
  بيئة عمل قادمة، أو مسار في نفس المستودع) لا كصور مضمّنة في نص
  المحادثة، ثم حوّلهما إلى Data URI حسب التعليمات في `README.md` والصقهما
  في `BRAND`.
- **اختبار اختراق يدوي شامل**: المراجعة الأمنية في هذه الجلسة كانت
  مركّزة (رفع الملفات، IDOR، تسريب الأسرار) وليست بديلًا عن اختبار اختراق
  احترافي مستقل قبل أي إطلاق واسع النطاق.
- **الاستجابة الكاملة عبر كل حجم شاشة**: تحسينات جزئية طُبِّقت (الجداول
  تُغلَّف بـ `table-wrap` قابل للتمرير الأفقي، الشريط الجانبي ينهار على
  الجوال). لم يُجرَ اختبار فعلي في متصفح حقيقي على أجهزة متعددة (بيئة هذه
  الجلسة بلا واجهة رسومية تفاعلية مع Apps Script الحي).
- **إثبات تسليم فاشل بتفاصيل أوسع**: الأساس موجود (`FAILED_REASONS`،
  `updateDeliveryStatus`) لكن لم تُراجَع تجربة المندوب الكاملة لحالات
  التعذر المتكررة أو إعادة الجدولة.
- **استيراد جماعي بإحداثيات**: `importBeneficiaries` و
  `inspectBeneficiaryExcel` لا يدعمان بعد أعمدة خط العرض/الطول عند
  الاستيراد بالجملة من Excel — الإحداثيات تُدخَل حاليًا فقط عبر نموذج
  الإدخال الفردي أو التعديل.

## الترحيلات المكتوبة ولم تُشغَّل (بانتظار قرارك)

كل ما يلي **آمن ومتكرر التشغيل (idempotent)** ولا يحذف بيانات، لكنه **لم
يُشغَّل على أي بيانات حقيقية** بناءً على تعليماتك الصريحة بعدم تشغيل أي
ترحيل دون إذن مباشر:

- `migrateReferenceData()` — يبذر ورقة "البيانات المرجعية" (مناطق/مدن/
  تصنيفات/حالات اجتماعية معتمدة). بعده تتحوّل حقول المنطقة/المدينة من نص
  حر إلى قوائم منسدلة مترابطة تلقائيًا في كل النماذج.
- `previewPhoneNormalization()` ثم `migratePhoneNumbers()` — يقرأ الأول
  تقريرًا بكل رقم جوال مخزَّن سيتغيّر تنسيقه، والثاني يطبّق التطبيع
  الفعلي. لا تُشغّل الثاني قبل مراجعة تقرير الأول.
- `setupSheets()` — آمنة للتشغيل المتكرر (لا تحذف أعمدة أو صفوفًا قائمة،
  تُضيف فقط الأعمدة/الأوراق الناقصة). **يجب تشغيلها مرة واحدة بعد كل نشر
  لهذا الفرع** لإنشاء ورقة "طلبات انضمام الجمعيات" الجديدة (البند 8)
  وعمودَي الموقع الجغرافي في ورقة "المستفيدون" (البند 11)، وإلا ستفشل
  بوابة تقديم الجمعيات برسالة عربية واضحة بدل أي خطأ غامض.

## ملفات مهمة

| الملف | الدور |
|---|---|
| `Code.gs` | الخادم الكامل |
| `Index.html` | الواجهة الكاملة |
| `tools/verify.js` | فحص ثابت — 31 تأكيدًا |
| `tools/smoke.js` | اختبار DOM تشغيلي — 108 تأكيدًا |
| `tools/server-test.js` | اختبار منطق خادم بمحاكاة Apps Script — 121 تأكيدًا |
| `tools/perf-bench.js` | قياس تجريبي لعدد قراءات الشيت (اختياري، ليس جزءًا من بوابة النجاح) |
| `README.md` | تعليمات التثبيت والتشغيل والنشر الكاملة على Apps Script |

## نتائج الاختبارات (آخر تشغيل قبل هذا التسليم)

```
node tools/verify.js       → 31/31
node tools/smoke.js        → 108/108
node tools/server-test.js  → 121/121
```

**المجموع: 260/260، بلا أي تراجع وظيفي عبر كل commit في هذا الفرع.**

## خطوات مقترحة لأي جلسة عمل قادمة

كل البنود المخطَّطة الاثنا عشر مكتملة. الخطوات التالية اختيارية، حسب
الأولوية الفعلية القادمة:

1. شغّل الفحوص الثلاثة أولًا للتأكد من عدم وجود تراجع منذ آخر commit.
2. عند توفر ملفَي الشعار الفعليَّين كملفات حقيقية: حوّلهما وأدرجهما في `BRAND`.
3. إن أردت دعم الإحداثيات في الاستيراد الجماعي من Excel: أضف عمودين
   اختياريين لخط العرض/الطول في `inspectBeneficiaryExcel`/`importBeneficiaries`.
4. إن رغبت في إطلاق واسع النطاق: اطلب اختبار اختراق مستقل احترافي، لا
   تعتمد فقط على المراجعة الداخلية في هذه الجلسة.
5. لا تنسَ تشغيل `setupSheets()` مرة واحدة بعد أي نشر لهذا الفرع على بيئة
   حية، لإنشاء الورقة والأعمدة الجديدة (طلبات الانضمام، إحداثيات المستفيد).

## روابط

- الفرع: `claude/code-index-review-kz5k4u`
- آخر commit عند كتابة هذا التقرير: `ccf5ad9`
- المستودع: `mroan7241-create/-`
- Pull Request #1 (مفتوح، غير مدموج): https://github.com/mroan7241-create/-/pull/1
