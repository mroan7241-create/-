# خطة النسخة المرشحة للإصدار (Release Candidate) — الاختبار الحي والنشر

هذا الملف مرجع تنفيذي لاختبار هذا الفرع على بيئة Google Sheets/Apps Script
حية **قبل** أي نشر واسع أو دمج. لا شيء هنا نُفِّذ فعليًا من داخل هذه الجلسة —
لا مهاجرة (migration) شُغِّلت، ولا `setupSheets()` نُفِّذت، ولا تطبيق نُشر.
كل ما يلي **خطوات مُعدَّة لك لتنفِّذها بنفسك**، مرتَّبة وواضحة.

## 1. لماذا لم تُختبَر هذه النسخة حيًّا من داخل الجلسة

بيئة تنفيذ هذه الجلسة محجوبة شبكيًا عن `script.google.com` و`docs.google.com`
(تحقّق فعلي متكرر عبر `curl`، `403 policy denial`) — لا يوجد أي مسار لتشغيل
Apps Script حي أو الوصول إلى Google Sheets من هنا. كل الاختبارات المذكورة في
`HANDOFF.md` (verify.js/smoke.js/server-test.js/security-test.js) هي محاكاة
دقيقة داخل Node.js `vm`، وليست بديلًا عن تشغيل حقيقي على حساب Google فعلي.
هذا الملف يُجهِّز لك بالضبط ما تحتاجه لإجراء ذلك الاختبار الحي بنفسك بأقل جهد
وأقل مخاطرة.

## 2. إنشاء نسخة تجريبية (Staging) معزولة تمامًا

**لا تختبر على أي Google Sheet يحتوي بيانات حقيقية.** أنشئ نسخة تجريبية
مستقلة بالكامل:

1. أنشئ Google Sheet جديدًا فارغًا، مثلًا باسم "نظام الزاد — تجريبي" (حتى لا
   يُخلَط مع أي نسخة إنتاجية لاحقًا).
2. اربط مشروع Apps Script به عبر الإضافات ← Apps Script.
3. الصق ملفات `.gs` الأربعة عشر و`Index.html` من هذا الفرع كما هو موضَّح في
   قسم "التثبيت والتشغيل" في `README.md`.
4. **لا تربط هذه النسخة التجريبية بأي حساب بريد أو رقم جوال حقيقي** عند
   إدخال بيانات الاختبار — استخدم فقط البيانات الوهمية في القسم 4 أدناه.
5. بعد انتهاء الاختبار الحي: يمكنك إما الإبقاء على هذا الـ Sheet كبيئة اختبار
   دائمة لأي تحديث مستقبلي، أو حذفه بالكامل إن لم تعد بحاجته — لا علاقة له
   بأي بيانات إنتاجية إطلاقًا لأنه لم يُنشأ من نسخة حقيقية.

## 3. ترتيب تشغيل دوال الإعداد والمعاينة والترحيل (بدون تنفيذها الآن)

نفّذ هذا الترتيب **على النسخة التجريبية فقط** أولًا، وراقب كل خطوة قبل
الانتقال للتالية. كل دالة هنا آمنة ومتكررة التشغيل (idempotent) ولا تحذف
بيانات — لكن الترتيب مهم لتفادي حالات وسيطة مربكة:

| # | الدالة | ماذا تفعل | ماذا تراقب بعدها |
|---|---|---|---|
| 1 | `setupSheets()` | تُنشئ كل الأوراق والأعمدة الإحدى عشرة (بما فيها "طلبات انضمام الجمعيات" وعمودا الموقع الجغرافي)، تبذر الإعدادات، تُنشئ أول حساب مدير إن لم يوجد | افتح **سجل التنفيذ** (Executions) وابحث عن سطر "بيانات المدير المؤقتة" — انسخ البريد وكلمة المرور فورًا |
| 2 | تسجيل الدخول بحساب المدير المؤقت | — | غيّر كلمة المرور فورًا من صفحة الإعدادات داخل النظام نفسه |
| 3 | `previewPhoneNormalization()` | **قراءة فقط** — تقرير بكل رقم جوال سيتغيّر تنسيقه دون كتابة أي شيء | راجع التقرير في سجل التنفيذ (Logger.log) وفي القيمة المُعادة؛ إن كان فارغًا (لا بيانات مستفيدين بعد) فهذا متوقَّع في نسخة تجريبية جديدة |
| 4 | `migratePhoneNumbers()` | يطبّق التطبيع الفعلي بناءً على نفس تقرير المعاينة | **لا تُشغّلها إلا بعد مراجعة تقرير الخطوة 3**؛ في نسخة تجريبية جديدة بلا بيانات، تشغيلها آمن وبلا أثر |
| 5 | `migrateReferenceData()` | يبذر ورقة "البيانات المرجعية" (مناطق/مدن/تصنيفات/حالات اجتماعية/أنواع أجهزة معتمدة) | بعدها تتحوّل حقول المنطقة/المدينة/التصنيف/الحالة الاجتماعية/نوع الجهاز في كل النماذج من نص حر إلى قوائم منسدلة مترابطة تلقائيًا، ويبدأ التحقق الخادمي الصارم — تحقّق من ذلك بفتح نموذج "إضافة مستفيد" |
| 6 | `diagnoseReferenceDataIssues()` | **قراءة فقط** — تقرير بكل قيمة منطقة/مدينة/حالة اجتماعية/تصنيف/نوع جهاز قديمة لا تطابق القائمة المعتمدة الجديدة، مع موقعها الدقيق (الورقة والمعرّف) | راجع `issueCount`/`bySeverity`/`byType`/`issues` في القيمة المُعادة؛ في نسخة تجريبية جديدة بلا بيانات قديمة يُتوقَّع `issueCount: 0` |
| 7 | `migrateLegacyReferenceValues(true)` | **معاينة فقط (dry-run)** — تبني قائمة تصحيحات مقترَحة للمنطقة/المدينة القديمة دون كتابة أي شيء؛ القيم الغامضة (حالة اجتماعية/تصنيف/نوع جهاز غير معروف، أو منطقة/مدينة بلا مرادف مؤكَّد) تُدرَج في `ambiguous` دون أي اقتراح تلقائي لها | راجع `proposals` (قديم←جديد لكل سجل) و`ambiguous` (يحتاج مراجعة يدوية سجلًا سجلًا) بعناية كاملة قبل أي تفكير بالخطوة التالية |
| 8 | `migrateLegacyReferenceValues(false)` | **كتابة فعلية** — تطبّق فقط التعديلات المؤكَّدة من معاينة الخطوة 7 (لا الغامضة) | شغّلها فقط بعد رضاك التام عن قائمة `proposals` في الخطوة 7؛ آمنة لإعادة التشغيل (تشغيل ثانٍ على نفس البيانات لا يقترح شيئًا جديدًا) |

**لا تُشغّل الخطوات 3–8 على أي نسخة تحتوي بيانات إنتاجية حقيقية إلا بعد
تكرار هذا الاختبار بالكامل على النسخة التجريبية أولًا والتأكد من رضاك عن
النتائج. الخطوة 8 خصوصًا تكتب على البيانات — لا تُشغَّل إلا بعد مراجعة
يدوية كاملة لمخرجات الخطوة 7.**

## 4. بيانات اختبار وهمية (وليست حقيقية — آمنة للنشر والمشاركة)

استخدم هذه البيانات فقط عند الاختبار على النسخة التجريبية. لا تحتوي على أي
معلومة حقيقية عن أي شخص أو جهة فعلية:

**جمعية تجريبية** (من صفحة "الجمعيات" في بوابة الإدارة):
- الاسم: `جمعية الاختبار التجريبية`
- التصنيف: `جمعية خيرية`
- المنطقة/المدينة: `الرياض` / `الرياض`
- رقم التواصل: `0500000001`
- البريد الإلكتروني: `test-association@example.org`
- كلمة مرور الحساب: `TestPass1234` (غيّرها فور أول دخول فعلي)

**مستفيد تجريبي** (من بوابة الجمعية بعد تسجيل الدخول بحسابها):
- الاسم: `مستفيد تجريبي`
- رقم الجوال: `0500000002`
- المنطقة/المدينة: `الرياض` / `الرياض`
- العنوان: `حي تجريبي، شارع وهمي`
- عدد الأفراد: `3`
- الحالة الاجتماعية: `أرملة`
- الاحتياج: `ثلاجة`
- الإحداثيات (اختياري، لاختبار "مسار اليوم"): خط العرض `24.7136` / خط الطول `46.6753` (وسط الرياض تقريبًا)

**مندوب تجريبي** (من بوابة الجمعية):
- الاسم: `مندوب تجريبي`
- رقم الجوال: `0500000003`
- بعد الإنشاء: انسخ رمز الدخول المعروض (مرة واحدة فقط) لاستخدامه في اختبار بوابة المندوب.

**طلب انضمام جمعية عام** (من شاشة الدخول، بلا تسجيل دخول):
- الاسم: `جمعية طلب تجريبي`
- المسؤول: `مسؤول تجريبي`
- البريد: `test-application@example.org`
- الجوال: `0500000004`

## 5. قائمة اختبار يدوية شاملة للبوابات الثلاث

نفّذ كل بند بالترتيب، وسجّل ✅/❌ أمام كل واحد. "النتيجة المتوقعة" هي ما
يجب أن يحدث بناءً على منطق الكود الفعلي في هذا الفرع — أي انحراف عنها
يستحق تحقيقًا قبل أي نشر واسع.

### 5.1 شاشة الدخول العامة (بلا تسجيل دخول)

| # | الخطوة | النتيجة المتوقعة |
|---|---|---|
| 1 | افتح رابط تطبيق الويب لأول مرة | تظهر شاشة دخول بهوية الزاد (عنابي/ذهبي)، شعار الزاد وشعار مؤسسة أبانمي ظاهران فعليًا (لا بديل نصي) |
| 2 | بدّل التبويب إلى "دخول المندوب" | يظهر حقل رمز الدخول (MND-XXXXXX) بدل البريد/كلمة المرور؛ رابط "تقديم طلب انضمام" يختفي في هذا الوضع |
| 3 | عد لتبويب "دخول الإدارة والجمعيات"، اضغط "تقديم طلب انضمام جمعية جديدة" | تظهر شاشة نموذج طلب الانضمام العامة بحقولها كاملة (بيانات القسم 4) |
| 4 | عبّئ النموذج بالبيانات التجريبية وأرسل | رسالة نجاح "تم استلام طلب الانضمام"، والعودة لشاشة الدخول |
| 5 | حاول إرسال نفس الطلب مرة أخرى بنفس البريد | رسالة خطأ واضحة: يوجد طلب سابق قيد المراجعة بنفس البريد |
| 6 | جرّب إدخال بريد إلكتروني غير صحيح الصيغة | التحقق من المتصفح (HTML5) يمنع الإرسال قبل وصوله للخادم |

### 5.2 بوابة الإدارة (ADMIN)

سجّل الدخول بحساب المدير (من الخطوة 1–2 في القسم 3).

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | لوحة البيانات | مؤشرات KPI تظهر بقيم حقيقية (أصفار في البداية)؛ كل مؤشر عنصر `<button>` قابل للنقر |
| 2 | انقر مؤشر "المستفيدون" | ينتقل لصفحة المستفيدين مباشرة |
| 3 | انقر مؤشر "تم تسليمها" (أجهزة) | ينتقل لصفحة الأجهزة **مع فلتر "تم التسليم" مطبَّقًا تلقائيًا** |
| 4 | صفحة "طلبات الانضمام" | يظهر طلب الاختبار من القسم 5.1 بحالة "قيد المراجعة"، مع زرَي قبول/رفض |
| 5 | اضغط "قبول" على الطلب التجريبي، أكّد النافذة | تظهر نافذة "تم إنشاء الجمعية" بكلمة مرور مؤقتة — **انسخها فورًا، لن تظهر مرة أخرى** |
| 6 | تحقّق من صفحة "الجمعيات" | الجمعية الجديدة من الطلب المقبول ظاهرة بحالة "نشطة" |
| 7 | أرسل طلبًا آخر مشابهًا، ثم اضغط "رفض" وأدخل سببًا | الطلب يتحول لحالة "مرفوض" مع عرض السبب، ولا يُنشأ أي حساب |
| 8 | صفحة "الجمعيات" ← "＋ إضافة جمعية" | نموذج كامل يظهر؛ احفظ ببيانات القسم 4 |
| 9 | صفحة "الأجهزة" ← "＋ إضافة جهاز" | نموذج يظهر مع قوائم الجمعيات/المستفيدين؛ احفظ جهازًا واحدًا (مثال: ثلاجة) للجمعية التجريبية |
| 10 | صفحة "متابعة المشروع" ← "＋ نشاط" | نموذج إضافة نشاط كامل (مرحلة/نشاط رئيسي/فرعي/تواريخ/نسبة إنجاز)؛ احفظ نشاطًا تجريبيًا |
| 11 | جرّب تبويبات تصفية الأنشطة (جارية/متأخرة/مكتملة/قادمة) | القائمة تتغيّر فعليًا حسب حالة كل نشاط |
| 12 | صفحة "سجل العمليات" | كل الإجراءات أعلاه ظاهرة كسجلات؛ جرّب البحث باسم أحد الفاعلين، وجرّب فلتر القسم |
| 13 | صفحة "الإعدادات" ← تغيير كلمة المرور | يطلب كلمة المرور الحالية؛ كلمة جديدة قصيرة أو بلا رقم تُرفض برسالة واضحة |
| 14 | سجّل الخروج ثم أعد الدخول بكلمة المرور الجديدة | الدخول ينجح؛ الجلسة القديمة (إن كانت مفتوحة في متصفح آخر) تُقطع فورًا |

### 5.3 بوابة الجمعية (ASSOCIATION)

سجّل الدخول بحساب الجمعية التجريبية (من القسم 4 أو من القبول في 5.2).

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | لوحة البيانات | لا تظهر بيانات أي جمعية أخرى؛ فقط بيانات هذه الجمعية |
| 2 | صفحة "المستفيدون" ← "＋ مستفيد" | نموذج كامل؛ جرّب تعبئة الإحداثيات يدويًا، ثم جرّب زر "📍 استخدم الموقع الحالي" (يطلب إذن الموقع من المتصفح) |
| 3 | احفظ مستفيدًا ببيانات القسم 4 | يظهر في القائمة فورًا؛ حالته "جديد" |
| 4 | صفحة "المستفيدون" ← "استيراد بالجملة" | جرّب رفع ملف CSV بعمودي خط العرض/الطول في النهاية، وملف آخر بدونهما — كلاهما يجب أن ينجح |
| 5 | صفحة "المناديب" ← "＋ مندوب" | احفظ مندوبًا؛ يظهر رمز دخول لمرة واحدة — انسخه لاختبار 5.4 |
| 6 | من صفحة المستفيدين، اضغط "تعيين المندوب" على المستفيد التجريبي | يعيَّن المندوب؛ حالة المستفيد تتحول لـ"جاري التسليم" |
| 7 | صفحة "الإعدادات" ← تعديل بيانات التواصل | يحفظ بريدًا/جوالًا جديدَين إن كانا صحيحَين وغير مكرَّرين |
| 8 | من "الإعدادات" أيضًا، جرّب تغيير كلمة المرور | نفس سلوك 5.2 البند 13 |

### 5.4 بوابة المندوب (DELEGATE)

سجّل الخروج، ثم من شاشة الدخول اختر "دخول المندوب" وأدخل الرمز من 5.3.

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | تبويب "قائمة المهام" | يظهر المستفيد المعيَّن من 5.3؛ أزرار واتساب/اتصال/الموقع كلها بروابط صحيحة |
| 2 | اضغط زر "اتصال" | يحاول فتح تطبيق الاتصال؛ إن حُجب (شائع داخل iframe) يُنسخ الرقم تلقائيًا مع تنبيه |
| 3 | اضغط زر "الموقع" | يفتح خرائط جوجل — بالإحداثيات الدقيقة إن أُدخلت في 5.3، أو ببحث نصي بالعنوان إن لم تُدخَل |
| 4 | تبويب "مسار اليوم" ← "📍 ترتيب المسار من موقعي الحالي" | يطلب إذن الموقع؛ بعد الموافقة يظهر المستفيد مرتَّبًا مع المسافة التقريبية بالكيلومتر |
| 5 | ارجع لتبويب "قائمة المهام"، اضغط "تعذّر" على المستفيد | نافذة سبب التعذّر تظهر؛ احفظ سببًا — حالة التسليم تتحول لـ"تعذر التسليم" |
| 6 | عيّن المندوب لمستفيد آخر (من بوابة الجمعية)، ثم من بوابة المندوب اضغط "تأكيد التسليم" | يطلب صورة إثبات؛ بلا جهاز مخصَّص للمستفيد يكون الزر معطَّلًا كما في 5.2 توثيقًا لسلوك مصمَّم |
| 7 | ارفع صورة إثبات (JPG/PNG) وأكّد | التسليم يُسجَّل، الصورة تُرفع لمجلد Drive مخصَّص، الحالة تتحول لـ"تم التسليم" |
| 8 | تبويب "السجل" | يظهر التسليم المؤكَّد للتو بتاريخه ووقته |

### 5.5 تجربة الجوال (لكل بوابة)

افتح رابط التطبيق على متصفح جوال حقيقي (أو أدوات المطوّر بوضع محاكاة جوال
390px عرضًا) وكرّر البنود الأساسية أعلاه، مع التركيز على:

| # | ماذا تفحص | النتيجة المتوقعة |
|---|---|---|
| 1 | الشريط الجانبي في بوابتَي الإدارة/الجمعية | ينهار لقائمة قابلة للفتح بزر، لا يُغطي المحتوى |
| 2 | الجداول (المستفيدون/الأجهزة/سجل العمليات) | قابلة للتمرير الأفقي داخل حاويتها، لا تكسر تخطيط الصفحة |
| 3 | النوافذ المنبثقة (نماذج الإضافة/التعديل) | تظهر كاملة ضمن حدود الشاشة، حقولها قابلة للّمس بسهولة |
| 4 | أزرار بوابة المندوب (اتصال/واتساب/الموقع/تسليم) | أهداف لمس مريحة، لا تراكب بينها |

### 5.6 سلوك الأداء الجديد (المرحلة الثالثة)

| # | الخطوة | النتيجة المتوقعة |
|---|---|---|
| 1 | افتح صفحة "سجل العمليات" (إدارة أو جمعية) | تظهر حالة تحميل موجزة ثم السجل مُرقَّمًا بأزرار "السابق/التالي" إن تجاوز صفحة واحدة |
| 2 | ابحث في سجل العمليات باسم مستخدم أو إجراء | النتائج تُحدَّث من الخادم (لا فورًا محليًا) — انتظر جزءًا من الثانية |
| 3 | افتح صفحة "متابعة المشروع" (الإدارة) | الأنشطة والمراحل تُجلب عند الفتح، لا فور تسجيل الدخول |
| 4 | افتح صفحة "طلبات الانضمام" (الإدارة) | الطلبات تُجلب مُرقَّمة عند الفتح |
| 5 | احفظ مستفيدًا أو جهازًا أو مندوبًا | لا "تجميد" للواجهة؛ فقط زر الحفظ نفسه يُعطَّل مؤقتًا، بقية الأزرار (تسجيل الخروج، التنقّل) تبقى تعمل |
| 6 | افصل الإنترنت لحظيًا أثناء عملية حفظ (أدوات المطوّر ← Network ← Offline) ثم أعده | بعد نحو 25 ثانية تظهر رسالة "لا نعرف نتيجتها بعد" بدل تجمّد صامت؛ الزر يُفكّ تعطيله ويمكن إعادة المحاولة |
| 7 | أنشئ جمعية جديدة، راقب أن كلمة المرور المؤقتة تظهر مرة واحدة فقط | كما في القسم 5.2 سابقًا — سلوك لم يتغيّر رغم إعادة الهيكلة الداخلية |

## 6. النتائج المتوقعة إجمالًا

إن اجتزت كل بنود القسم 5 دون أي انحراف عن "النتيجة المتوقعة"، فالنسخة
جاهزة تقنيًا للنشر الفعلي حسب تقديرك. أي بند يفشل: وثّقه (لقطة شاشة + خطوات
إعادة الإنتاج) قبل المتابعة — لا تُصلح على النسخة الحية مباشرة، أعد
الاختبار على الفرع بعد الإصلاح ثم كرّر القسم 5 من جديد.

## 7. خطة الرجوع (Rollback)

إن ظهرت مشكلة **بعد** نشر هذا الفرع على نسخة حية:

1. **إن كانت المشكلة في الكود فقط (لا بيانات)**: من محرر Apps Script، اذهب
   إلى **Manage deployments** ← اختر النشرة الحالية ← **Edit** ← بدّل
   الإصدار (Version) إلى الإصدار السابق المنشور قبل هذا التحديث. هذا يُعيد
   كود التطبيق فورًا دون أي تأثير على البيانات المخزَّنة في Sheets (الكود
   والبيانات منفصلان تمامًا في Apps Script).
2. **إن شُغِّلت `setupSheets()` بالخطأ على نسخة حية قبل التأكد**: آمنة —
   لا تحذف أعمدة أو صفوفًا قائمة، فقط تضيف الناقص. لا حاجة لأي رجوع.
3. **إن شُغِّلت `migratePhoneNumbers()` وظهرت نتيجة غير متوقعة**: الدالة
   تُطبِّع فقط صيغة عرض الرقم (لا تحذف بيانات)؛ للتراجع، صحّح الأرقام
   المتأثرة يدويًا من داخل النظام (تعديل بيانات المستفيد/الجمعية/المندوب)
   بالاعتماد على تقرير `previewPhoneNormalization()` المحفوظ.
4. **إن ظهرت مشكلة في بيانات حقيقية أُدخلت أثناء الاختبار الحي بالخطأ على
   نسخة إنتاجية بدل التجريبية**: لا تحذف الصفوف يدويًا بسرعة — استخدم
   نسخة "سجل الإصدارات" في Google Sheets (الملف ← سجل الإصدارات) لمعاينة
   واستعادة حالة الورقة قبل الاختبار، بدل حذف انتقائي قد يُفوِّت أثرًا جانبيًا.
5. **في كل الحالات**: لا تدمج أي Pull Request ولا تُبقِ نشرًا حيًّا يعمل
   بمشكلة معروفة أكثر من اللازم — التراجع للإصدار السابق (الخطوة 1) خيار
   فوري ورخيص دائمًا في Apps Script.

## 8. تذكير أخير قبل أي نشر واسع

- شغّل الثمانية: `node tools/verify.js && node tools/smoke.js && node tools/server-test.js && node tools/security-test.js && node tools/state-test.js && node tools/account-test.js && node tools/perf-test.js && node tools/reference-test.js`.
- راجع `HANDOFF.md` لقائمة ما هو مكتمل فعليًا وما تبقى.
- لا تنشر ولا تدمج أي Pull Request إلا بعد موافقتك الصريحة المنفصلة —
  هذا الملف تحضير فقط، وليس تنفيذًا للنشر.

## 9. الأداء — القياس الفعلي ونتائجه (المرحلة الثالثة)

**تنبيه صادق مهم**: كل ما يلي قياسٌ **محلي داخل Node.js `vm`** عبر
`tools/perf-scale-test.js` — بيانات محاكاة في الذاكرة، بلا أي اتصال بـ
Google Sheets أو Apps Script حيَّين (نفس حجب الشبكة الموصوف في القسم 1
أعلاه). الأرقام بالمللي ثانية أدناه هي **زمن تنفيذ الشيفرة محليًا فقط**،
وهي أقل بكثير من زمن الشبكة الحقيقي على Apps Script (كل استدعاء `google.script.run`
فعلي يضيف عبء شبكة ومصادقة لا يُحاكيه Node.js). القيمة الحقيقية لهذا
القياس هي **اتجاه النمو مع حجم البيانات** و**عدد قراءات/كتابات الجداول
لكل عملية** — ذاك الرقم الثاني هو ما يتحكم فعليًا بزمن الشبكة الحي، وهو
مستقر تقريبًا بصرف النظر عن حجم البيانات (مُتحقَّق منه أدناه).

نتائج تشغيل فعلي لـ `node tools/perf-scale-test.js` عند إعداد هذا القسم:

| الحجم | دخول | Bootstrap | صفحة مستفيدين | بحث | حفظ | تعيين مندوب | تأكيد تسليم |
|---|---|---|---|---|---|---|---|
| 100 مستفيد | ~6ms | ~3ms | ~2ms | ~2ms | ~1ms | ~2ms | ~2ms |
| 1000 مستفيد | ~28ms | ~28ms | ~22ms | ~25ms | ~5ms | ~12ms | ~11ms |
| 5000 مستفيد | ~145ms | ~154ms | ~94ms | ~95ms | ~36ms | ~55ms | ~74ms |

عدد قراءات/كتابات الجداول الفعلية لكل عملية — **ثابت تمامًا عبر الأحجام
الثلاثة** (100/1000/5000)، وهذا هو الدليل الكمّي أن الترقيم/البحث لا
يزيدان عدد استدعاءات Sheets API مع نمو البيانات:

| العملية | قراءات | كتابات |
|---|---|---|
| `login` | 7 | 2 |
| `getBootstrapData` (ASSOCIATION) | 9 (≈4.5/طلب لوجود طلبين) | 0 |
| `listBeneficiaries` (صفحة/بحث) | 1 | 0 |
| `saveBeneficiary` | 4 | 2 |
| `assignDelegate` | 5 | 3 |
| `confirmDelivery` | 3 | 4 |

**الخلاصة الصادقة**: عدد قراءات/كتابات الجداول لكل عملية لا يتغيّر مع حجم
البيانات (المقياس الحقيقي لتكلفة Sheets API) — لكن **زمن التنفيذ نفسه
ينمو تقريبًا خطيًا** مع عدد الصفوف (ملموس بوضوح عند 5000)، لأن `readTable_`
لا يزال يقرأ الورقة **كاملة** دفعة واحدة ثم يُصفّي/يُرتّب/يُرقّم في ذاكرة
Apps Script — Google Sheets لا يوفّر استعلامًا جزئيًا حقيقيًا (`OFFSET`/`LIMIT`
على مستوى الـ API) دون فهرس أو قاعدة بيانات خارجية. هذا يعني:

- **حتى 1000–2000 مستفيد تقريبًا**: الأداء المتوقَّع جيد (أهداف القسم
  المطلوب: تنقّل محلي < ثانية، بحث < ثانيتين، حفظ < 3 ثوانٍ) — الزمن
  المحلي هنا صغير جدًا، وزمن الشبكة الحقيقي على Apps Script سيهيمن على
  التجربة (عادة 200–800ms لكل استدعاء حسب حِمل جوجل في تلك اللحظة، خارج
  سيطرة هذا الكود).
- **عند 5000 مستفيد فأكثر**: زمن معالجة البيانات نفسه (لا زمن الشبكة)
  يصبح ملموسًا (~150ms محليًا)، وسيتراكم فوق زمن الشبكة الحقيقي. هذا
  **قيد معماري حقيقي في بنية Sheets الحالية**، لا خطأ في هذه المرحلة —
  الحل الجذري (فهرسة/قاعدة بيانات مستقلة) خارج نطاق "تحسين الأداء ضمن
  البنية الحالية" الذي طُلب في هذه المرحلة تحديدًا، ويحتاج قرارًا معماريًا
  منفصلًا إن نما عدد المستفيدين إلى عشرات الآلاف مستقبلًا.
- لتكرار هذا القياس بنفسك أو بأحجام مختلفة: `node tools/perf-scale-test.js`
  (يقبل تعديل مصفوفة `sizes` أعلى الملف لأي حجم آخر).

**ما لم يُقَس حيًّا (ولن يُدَّعى قياسه)**: زمن الشبكة الفعلي بين المتصفح
وApps Script، سلوك حدود التنفيذ (6 دقائق لكل استدعاء) عند بيانات ضخمة
جدًا، وسلوك `CacheService`/`LockService` الحقيقيين تحت تزامن فعلي من عدة
مستخدمين — كل هذا يتطلّب اختبارًا حيًّا لا تتيحه بيئة هذه الجلسة.

## 10. البيانات المرجعية والترحيل الآمن (المرحلة الرابعة)

**تنبيه صادق**: كل ما يلي مؤكَّد داخل محاكاة Node.js `vm`
(`tools/reference-test.js`، 60 تأكيدًا) فقط — لا اختبار حي وقع من داخل هذه
الجلسة، لنفس سبب حجب الشبكة الموصوف في القسم 1.

- **العطلان الحيّان المُبلَّغان مُصلَحان عند الجذر**: فقدان الصفر الأول في
  الهاتف (`safeCell_` في `DataUtils.gs`) وتاريخ JavaScript خام في طلبات
  انضمام الجمعيات (`normalizeApplication_` في `Applications.gs`) — راجع
  `HANDOFF.md` قسم "المرحلة الرابعة" للتفصيل الكامل لكل سبب وإصلاح.
- **ظهور المنطقة/المدينة/التصنيف كحقول نص حر في نسختك الحية ليس عطلًا في
  هذا الكود** — القوائم المنسدلة المترابطة موجودة ومفعَّلة بالفعل في كل
  النماذج، لكنها تسقط تلقائيًا لحقل نص حر حتى تُشغِّل `migrateReferenceData()`
  (الخطوة 5 في القسم 3 أعلاه). هذا سلوك توافق مقصود، لا خطأ.
- **الفجوة الفعلية الوحيدة التي أُصلحت في الواجهة**: نوع الجهاز في نموذج
  إضافة/تعديل جهاز كان دائمًا حقل نص حر (`deviceTypeField()` جديدة الآن،
  بنفس نمط بقية الحقول المرجعية).
- **قبل أي ترحيل على نسختك الحية**: شغّل `diagnoseReferenceDataIssues()`
  (الخطوة 6 في القسم 3) أولًا لمعرفة الحجم الفعلي للقيم القديمة/غير
  المطابقة عندك تحديدًا — العدد الحقيقي غير معروف من هذه الجلسة لأنه يعتمد
  على بياناتك الفعلية.
- **الترحيل لا يخمّن أبدًا**: `migrateLegacyReferenceValues` تقترح تصحيحًا
  فقط لحالات مؤكَّدة (تطابق حرفي بعد تنظيف المسافات، أربعة مرادفات معروفة
  لاختصارات مناطق شائعة، أو مدينة تتبع منطقة واحدة فقط بلا غموض) — أي شيء
  آخر (خصوصًا الحالة الاجتماعية/التصنيف/نوع الجهاز، إذ لا مرادفات معروفة
  لها إطلاقًا) يذهب إلى قائمة مراجعة يدوية منفصلة (`ambiguous`) دون أي لمس
  تلقائي.
- **قابلة لإعادة التشغيل بأمان**: تشغيل `migrateLegacyReferenceValues(false)`
  مرتين متتاليتين على نفس البيانات لا يكرر أي تعديل ولا يفسد أي سجل غامض —
  مُتحقَّق منه اختباريًا صراحة.
- لتفصيل كل دالة ومسار اختبار: راجع `tools/reference-test.js` مباشرة
  (10 أقسام مرقَّمة تطابق كل بند طُلب صراحة لهذه المرحلة) و`HANDOFF.md`.
