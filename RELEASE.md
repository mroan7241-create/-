# خطة النسخة المرشحة للإصدار (Release Candidate) — الاختبار الحي والنشر

هذا الملف مرجع تنفيذي لاختبار هذا الفرع على بيئة Google Sheets/Apps Script
حية **قبل** أي نشر واسع أو دمج. لا شيء هنا نُفِّذ فعليًا من داخل هذه الجلسة —
لا مهاجرة (migration) شُغِّلت، ولا `setupSheets()` نُفِّذت، ولا تطبيق نُشر.
كل ما يلي **خطوات مُعدَّة لك لتنفِّذها بنفسك**، مرتَّبة وواضحة.

## 1. لماذا لم تُختبَر هذه النسخة حيًّا من داخل الجلسة

بيئة تنفيذ هذه الجلسة محجوبة شبكيًا عن `script.google.com` و`docs.google.com`
(تحقّق فعلي متكرر عبر `curl`، `403 policy denial`) — لا يوجد أي مسار لتشغيل
Apps Script حي أو الوصول إلى Google Sheets من هنا. كل الاختبارات المذكورة في
`HANDOFF.md` (verify.js/smoke.js/server-test.js/security-test.js) هي محاكاة
دقيقة داخل Node.js `vm`، وليست بديلًا عن تشغيل حقيقي على حساب Google فعلي.
هذا الملف يُجهِّز لك بالضبط ما تحتاجه لإجراء ذلك الاختبار الحي بنفسك بأقل جهد
وأقل مخاطرة.

## 2. إنشاء نسخة تجريبية (Staging) معزولة تمامًا

**لا تختبر على أي Google Sheet يحتوي بيانات حقيقية.** أنشئ نسخة تجريبية
مستقلة بالكامل:

1. أنشئ Google Sheet جديدًا فارغًا، مثلًا باسم "نظام الزاد — تجريبي" (حتى لا
   يُخلَط مع أي نسخة إنتاجية لاحقًا).
2. اربط مشروع Apps Script به عبر الإضافات ← Apps Script.
3. الصق ملفات `.gs` الستة عشر (بما فيها `ExcelTemplate.gs` و`ReleaseOps.gs` الجديدين في
   المرحلة السابعة) و`Index.html` من هذا الفرع كما هو موضَّح في قسم
   "التثبيت والتشغيل" في `README.md`.
4. **لا تربط هذه النسخة التجريبية بأي حساب بريد أو رقم جوال حقيقي** عند
   إدخال بيانات الاختبار — استخدم فقط البيانات الوهمية في القسم 4 أدناه.
5. بعد انتهاء الاختبار الحي: يمكنك إما الإبقاء على هذا الـ Sheet كبيئة اختبار
   دائمة لأي تحديث مستقبلي، أو حذفه بالكامل إن لم تعد بحاجته — لا علاقة له
   بأي بيانات إنتاجية إطلاقًا لأنه لم يُنشأ من نسخة حقيقية.

## 3. ترتيب تشغيل دوال الإعداد والمعاينة والترحيل (بدون تنفيذها الآن)

**تحديث أمني (المرحلة الثامنة، تصحيح لاحق)**: كل دوال هذا الجدول أصبحت
**خاصة** (تنتهي بشرطة سفلية `_`) **ومقفلة افتراضيًا** — لا تعمل أي منها
بلا رمز وصول صيانة مؤقت تولّده أنت بنفسك أولًا عبر `grantMaintenanceAccess_()`
من محرر Apps Script. **راجع `DEPLOYMENT.md` الأقسام 5–8 للترتيب التنفيذي
الدقيق والحديث** — الجدول أدناه ملخّص فقط، ولم يعد "شغّل الدالة مباشرة من
القائمة المنسدلة" كافيًا لأي منها.

نفّذ هذا الترتيب **على النسخة التجريبية فقط** أولًا، وراقب كل خطوة قبل
الانتقال للتالية. كل دالة هنا آمنة ومتكررة التشغيل (idempotent) ولا تحذف
بيانات — لكن الترتيب مهم لتفادي حالات وسيطة مربكة:

| # | الدالة | ماذا تفعل | ماذا تراقب بعدها |
|---|---|---|---|
| 0 | `grantMaintenanceAccess_()` | يمنح رمز وصول صيانة مؤقتًا (20 دقيقة افتراضيًا) لكل الدوال أدناه | انسخ الرمز من سجل التنفيذ فورًا — لن يظهر مرة أخرى |
| 1 | `setupSheets_(token)` | تُنشئ كل الأوراق والأعمدة (بما فيها "طلبات انضمام الجمعيات" وعمودا الموقع الجغرافي)، تبذر الإعدادات، تُنشئ أول حساب مدير إن لم يوجد | افتح **سجل التنفيذ** (Executions) وابحث عن سطر "بيانات المدير المؤقتة" — انسخ البريد وكلمة المرور فورًا |
| 2 | تسجيل الدخول بحساب المدير المؤقت | — | غيّر كلمة المرور فورًا من صفحة الإعدادات داخل النظام نفسه |
| 3 | `previewPhoneNormalization_(token)` | **قراءة فقط** — تقرير بكل رقم جوال سيتغيّر تنسيقه دون كتابة أي شيء | راجع التقرير في سجل التنفيذ (Logger.log) وفي القيمة المُعادة؛ إن كان فارغًا (لا بيانات مستفيدين بعد) فهذا متوقَّع في نسخة تجريبية جديدة |
| 4 | `migratePhoneNumbers_(token)` | يطبّق التطبيع الفعلي بناءً على نفس تقرير المعاينة | **لا تُشغّلها إلا بعد مراجعة تقرير الخطوة 3**؛ في نسخة تجريبية جديدة بلا بيانات، تشغيلها آمن وبلا أثر |
| 5 | `migrateReferenceData_(token)` | يبذر ورقة "البيانات المرجعية" (مناطق/مدن/تصنيفات/حالات اجتماعية/أنواع أجهزة معتمدة) | بعدها تتحوّل حقول المنطقة/المدينة/التصنيف/الحالة الاجتماعية/نوع الجهاز في كل النماذج من نص حر إلى قوائم منسدلة مترابطة تلقائيًا، ويبدأ التحقق الخادمي الصارم — تحقّق من ذلك بفتح نموذج "إضافة مستفيد" |
| 6 | `diagnoseReferenceDataIssues_(token)` | **قراءة فقط** — تقرير بكل قيمة منطقة/مدينة/حالة اجتماعية/تصنيف/نوع جهاز قديمة لا تطابق القائمة المعتمدة الجديدة، مع موقعها الدقيق (الورقة والمعرّف) | راجع `issueCount`/`bySeverity`/`byType`/`issues` في القيمة المُعادة؛ في نسخة تجريبية جديدة بلا بيانات قديمة يُتوقَّع `issueCount: 0` |
| 7 | `migrateLegacyReferenceValues_(token, true)` | **معاينة فقط (dry-run)** — تبني قائمة تصحيحات مقترَحة للمنطقة/المدينة القديمة دون كتابة أي شيء؛ القيم الغامضة (حالة اجتماعية/تصنيف/نوع جهاز غير معروف، أو منطقة/مدينة بلا مرادف مؤكَّد) تُدرَج في `ambiguous` دون أي اقتراح تلقائي لها | راجع `proposals` (قديم←جديد لكل سجل) و`ambiguous` (يحتاج مراجعة يدوية سجلًا سجلًا) بعناية كاملة قبل أي تفكير بالخطوة التالية |
| 8 | `migrateLegacyReferenceValues_(token, false)` | **كتابة فعلية** — تطبّق فقط التعديلات المؤكَّدة من معاينة الخطوة 7 (لا الغامضة) | شغّلها فقط بعد رضاك التام عن قائمة `proposals` في الخطوة 7؛ آمنة لإعادة التشغيل (تشغيل ثانٍ على نفس البيانات لا يقترح شيئًا جديدًا) |

**لا تُشغّل الخطوات 3–8 على أي نسخة تحتوي بيانات إنتاجية حقيقية إلا بعد
تكرار هذا الاختبار بالكامل على النسخة التجريبية أولًا والتأكد من رضاك عن
النتائج. الخطوة 8 خصوصًا تكتب على البيانات — لا تُشغَّل إلا بعد مراجعة
يدوية كاملة لمخرجات الخطوة 7. لكل دالة تتطلب `token`، راجع `DEPLOYMENT.md`
القسم 5 لطريقة تمريره فعليًا من محرر Apps Script (زر ▶ Run لا يقبل
معاملات مباشرة).**

## 4. بيانات اختبار وهمية (وليست حقيقية — آمنة للنشر والمشاركة)

استخدم هذه البيانات فقط عند الاختبار على النسخة التجريبية. لا تحتوي على أي
معلومة حقيقية عن أي شخص أو جهة فعلية:

**جمعية تجريبية** (من صفحة "الجمعيات" في بوابة الإدارة):
- الاسم: `جمعية الاختبار التجريبية`
- التصنيف: `جمعية خيرية`
- المنطقة/المدينة: `الرياض` / `الرياض`
- رقم التواصل: `0500000001`
- البريد الإلكتروني: `test-association@example.org`
- كلمة مرور الحساب: `TestPass1234` (غيّرها فور أول دخول فعلي)

**مستفيد تجريبي** (من بوابة الجمعية بعد تسجيل الدخول بحسابها):
- الاسم: `مستفيد تجريبي`
- رقم الجوال: `0500000002`
- المنطقة/المدينة: `الرياض` / `الرياض`
- العنوان: `حي تجريبي، شارع وهمي`
- عدد الأفراد: `3`
- الحالة الاجتماعية: `أرملة`
- الاحتياج: `ثلاجة`
- الإحداثيات (اختياري، لاختبار "مسار اليوم"): خط العرض `24.7136` / خط الطول `46.6753` (وسط الرياض تقريبًا)

**مندوب تجريبي** (من بوابة الجمعية):
- الاسم: `مندوب تجريبي`
- رقم الجوال: `0500000003`
- بعد الإنشاء: انسخ رمز الدخول المعروض (مرة واحدة فقط) لاستخدامه في اختبار بوابة المندوب.

**طلب انضمام جمعية عام** (من شاشة الدخول، بلا تسجيل دخول):
- الاسم: `جمعية طلب تجريبي`
- المسؤول: `مسؤول تجريبي`
- البريد: `test-application@example.org`
- الجوال: `0500000004`

## 5. قائمة اختبار يدوية شاملة للبوابات الثلاث

نفّذ كل بند بالترتيب، وسجّل ✅/❌ أمام كل واحد. "النتيجة المتوقعة" هي ما
يجب أن يحدث بناءً على منطق الكود الفعلي في هذا الفرع — أي انحراف عنها
يستحق تحقيقًا قبل أي نشر واسع.

### 5.1 شاشة الدخول العامة (بلا تسجيل دخول)

| # | الخطوة | النتيجة المتوقعة |
|---|---|---|
| 1 | افتح رابط تطبيق الويب لأول مرة | تظهر شاشة دخول بهوية الزاد (عنابي/ذهبي)، شعار الزاد وشعار مؤسسة أبانمي ظاهران فعليًا (لا بديل نصي) |
| 2 | بدّل التبويب إلى "دخول المندوب" | يظهر حقل رمز الدخول (MND-XXXXXX) بدل البريد/كلمة المرور؛ رابط "تقديم طلب انضمام" يختفي في هذا الوضع |
| 3 | عد لتبويب "دخول الإدارة والجمعيات"، اضغط "تقديم طلب انضمام جمعية جديدة" | تظهر شاشة نموذج طلب الانضمام العامة بحقولها كاملة (بيانات القسم 4) |
| 4 | عبّئ النموذج بالبيانات التجريبية وأرسل | رسالة نجاح "تم استلام طلب الانضمام"، والعودة لشاشة الدخول |
| 5 | حاول إرسال نفس الطلب مرة أخرى بنفس البريد | رسالة خطأ واضحة: يوجد طلب سابق قيد المراجعة بنفس البريد |
| 6 | جرّب إدخال بريد إلكتروني غير صحيح الصيغة | التحقق من المتصفح (HTML5) يمنع الإرسال قبل وصوله للخادم |

### 5.2 بوابة الإدارة (ADMIN)

سجّل الدخول بحساب المدير (من الخطوة 1–2 في القسم 3).

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | لوحة البيانات | مؤشرات KPI تظهر بقيم حقيقية (أصفار في البداية)؛ كل مؤشر عنصر `<button>` قابل للنقر |
| 2 | انقر مؤشر "المستفيدون" | ينتقل لصفحة المستفيدين مباشرة |
| 3 | انقر مؤشر "تم تسليمها" (أجهزة) | ينتقل لصفحة الأجهزة **مع فلتر "تم التسليم" مطبَّقًا تلقائيًا** |
| 4 | صفحة "طلبات الانضمام" | يظهر طلب الاختبار من القسم 5.1 بحالة "قيد المراجعة"، مع زرَي قبول/رفض |
| 5 | اضغط "قبول" على الطلب التجريبي، أكّد النافذة | تظهر نافذة "تم إنشاء الجمعية" بكلمة مرور مؤقتة — **انسخها فورًا، لن تظهر مرة أخرى** |
| 6 | تحقّق من صفحة "الجمعيات" | الجمعية الجديدة من الطلب المقبول ظاهرة بحالة "نشطة" |
| 7 | أرسل طلبًا آخر مشابهًا، ثم اضغط "رفض" وأدخل سببًا | الطلب يتحول لحالة "مرفوض" مع عرض السبب، ولا يُنشأ أي حساب |
| 8 | صفحة "الجمعيات" ← "＋ إضافة جمعية" | نموذج كامل يظهر؛ احفظ ببيانات القسم 4 |
| 9 | صفحة "الأجهزة" ← "＋ إضافة جهاز" | نموذج يظهر مع قوائم الجمعيات/المستفيدين؛ احفظ جهازًا واحدًا (مثال: ثلاجة) للجمعية التجريبية |
| 10 | صفحة "متابعة المشروع" ← "＋ نشاط" | نموذج إضافة نشاط كامل (مرحلة/نشاط رئيسي/فرعي/تواريخ/نسبة إنجاز)؛ احفظ نشاطًا تجريبيًا |
| 11 | جرّب تبويبات تصفية الأنشطة (جارية/متأخرة/مكتملة/قادمة) | القائمة تتغيّر فعليًا حسب حالة كل نشاط |
| 12 | صفحة "سجل العمليات" | كل الإجراءات أعلاه ظاهرة كسجلات؛ جرّب البحث باسم أحد الفاعلين، وجرّب فلتر القسم |
| 13 | صفحة "الإعدادات" ← تغيير كلمة المرور | يطلب كلمة المرور الحالية؛ كلمة جديدة قصيرة أو بلا رقم تُرفض برسالة واضحة |
| 14 | سجّل الخروج ثم أعد الدخول بكلمة المرور الجديدة | الدخول ينجح؛ الجلسة القديمة (إن كانت مفتوحة في متصفح آخر) تُقطع فورًا |

### 5.3 بوابة الجمعية (ASSOCIATION)

سجّل الدخول بحساب الجمعية التجريبية (من القسم 4 أو من القبول في 5.2).

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | لوحة البيانات | لا تظهر بيانات أي جمعية أخرى؛ فقط بيانات هذه الجمعية |
| 2 | صفحة "المستفيدون" ← "＋ مستفيد" | نموذج كامل؛ **تحقّق أولًا أن الخريطة تُحمَّل فعليًا** (Leaflet/OpenStreetMap) وتظهر داخل حاوية "موقع المستفيد على الخريطة" — انقر عليها لتحديد نقطة، اسحب العلامة لتعديلها، جرّب زر "📍 استخدم موقعي الحالي" (يطلب إذن الموقع)، وزر "✕ مسح الموقع"، وزر "↗ فتح في خرائط جوجل". ثم جرّب الإدخال اليدوي (`<details>` "إدخال يدوي للإحداثيات") مباشرة بلا خريطة |
| 2ب | عطّل الشبكة مؤقتًا (Network ← Offline) وافتح نموذج مستفيد جديد | الخريطة تفشل بصمت مع رسالة عربية واضحة تحت الحاوية؛ **حقول العنوان والإدخال اليدوي وزرا الموقع الحالي/فتح الخرائط تبقى كلها تعمل**؛ الحفظ ينجح بلا خريطة |
| 3 | احفظ مستفيدًا ببيانات القسم 4، مع علامة مميزة اختيارية (مثال: "بجانب المسجد") | يظهر في القائمة فورًا؛ حالته "جديد"؛ العلامة المميزة تظهر لاحقًا في بطاقة المندوب |
| 4 | صفحة "المستفيدون" ← "استيراد بالجملة" | اقرأ النص التوضيحي الجديد (يذكر "Latitude"/"Longitude" والعلامة المميزة). جرّب رفع ملف Excel يحتوي صفًا صحيحًا وصفًا بإحداثية واحدة فقط (بلا الأخرى) | يظهر جدول معاينة برقم كل صف وحالته (✓/✗) وسبب الخطأ والإحداثيات المقروءة؛ زر "استيراد المستفيدين" يستورد الصف الصحيح فقط، لا الخاطئ |
| 4ب | جرّب أيضًا ملف CSV بعمودي خط العرض/الطول في النهاية، وملف آخر بدونهما | كلاهما ينجح (ملف قديم بلا الأعمدة الاختيارية يبقى مقبولًا كاملًا) |
| 5 | صفحة "المناديب" ← "＋ مندوب" | احفظ مندوبًا؛ يظهر رمز دخول لمرة واحدة — انسخه لاختبار 5.4 |
| 6 | من صفحة المستفيدين، اضغط "تعيين المندوب" على المستفيد التجريبي | يعيَّن المندوب؛ حالة المستفيد تتحول لـ"جاري التسليم" |
| 7 | صفحة "الإعدادات" ← تعديل بيانات التواصل | يحفظ بريدًا/جوالًا جديدَين إن كانا صحيحَين وغير مكرَّرين |
| 8 | من "الإعدادات" أيضًا، جرّب تغيير كلمة المرور | نفس سلوك 5.2 البند 13 |

### 5.4 بوابة المندوب (DELEGATE)

سجّل الخروج، ثم من شاشة الدخول اختر "دخول المندوب" وأدخل الرمز من 5.3.

| # | الصفحة/الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | تبويب "قائمة المهام" | يظهر المستفيد المعيَّن من 5.3؛ أزرار واتساب/اتصال/**نسخ الرقم**/الموقع كلها ظاهرة بروابط صحيحة؛ العلامة المميزة (إن أُدخلت) تظهر ضمن العنوان |
| 2 | اضغط زر "اتصال" | يحاول فتح تطبيق الاتصال؛ إن حُجب (شائع داخل iframe) يُنسخ الرقم تلقائيًا مع تنبيه |
| 2ب | اضغط زر "نسخ الرقم" مباشرة | ينسخ الرقم فورًا مع رسالة تأكيد — بديل صريح لا ينتظر فشل الاتصال أولًا |
| 3 | اضغط زر "الموقع" | يفتح خرائط جوجل — بالإحداثيات الدقيقة إن أُدخلت في 5.3، أو ببحث نصي بالعنوان إن لم تُدخَل |
| 3ب | جرّب مستفيدًا بلا إحداثيات وبلا عنوان (إن أمكن إنشاء واحد لاختبار الحالة الحدّية) | زر "الموقع" يعرض تنبيهًا عربيًا واضحًا بدل فتح رابط خرائط فارغ |
| 4 | تبويب "مسار اليوم" ← "📍 ترتيب المسار من موقعي الحالي" | يطلب إذن الموقع؛ بعد الموافقة يظهر المستفيد مرتَّبًا مع نص صريح "مسافة تقريبية بخط مستقيم"، وبطاقته تعرض الأجهزة وكل أزرار التواصل أيضًا (لا فقط رابط الموقع كما كان) |
| 5 | ارجع لتبويب "قائمة المهام"، اضغط "تعذّر" على المستفيد | نافذة سبب التعذّر تظهر؛ احفظ سببًا — حالة التسليم تتحول لـ"تعذر التسليم" |
| 5ب | عد لتبويب "مسار اليوم" دون تحديث الصفحة يدويًا | بطاقة نفس المستفيد في المسار تعكس حالة "تعذر التسليم" الجديدة فورًا |
| 6 | عيّن المندوب لمستفيد آخر (من بوابة الجمعية)، ثم من بوابة المندوب اضغط "تأكيد التسليم" | يطلب صورة إثبات؛ بلا جهاز مخصَّص للمستفيد يكون الزر معطَّلًا كما في 5.2 توثيقًا لسلوك مصمَّم |
| 7 | ارفع صورة إثبات (JPG/PNG) وأكّد | التسليم يُسجَّل، الصورة تُرفع لمجلد Drive مخصَّص، الحالة تتحول لـ"تم التسليم" |
| 7ب | عد لتبويب "مسار اليوم" دون تحديث الصفحة | بطاقة المستفيد المُسلَّم تُصبح باهتة بصريًا (`dg-card-done`) وزرا "تأكيد التسليم"/"تعذّر" معطَّلان — دون إعادة حساب المسار أو تحميل الصفحة كاملة |
| 8 | تبويب "السجل" | يظهر التسليم المؤكَّد للتو بتاريخه ووقته |

### 5.5 تجربة الجوال (لكل بوابة)

افتح رابط التطبيق على متصفح جوال حقيقي (أو أدوات المطوّر بوضع محاكاة جوال
390px عرضًا) وكرّر البنود الأساسية أعلاه، مع التركيز على:

| # | ماذا تفحص | النتيجة المتوقعة |
|---|---|---|
| 1 | الشريط الجانبي في بوابتَي الإدارة/الجمعية | ينهار لقائمة قابلة للفتح بزر، لا يُغطي المحتوى |
| 2 | الجداول (المستفيدون/الأجهزة/سجل العمليات) | قابلة للتمرير الأفقي داخل حاويتها، لا تكسر تخطيط الصفحة |
| 3 | النوافذ المنبثقة (نماذج الإضافة/التعديل) | تظهر كاملة ضمن حدود الشاشة، حقولها قابلة للّمس بسهولة |
| 4 | أزرار بوابة المندوب (اتصال/واتساب/الموقع/تسليم) | أهداف لمس مريحة، لا تراكب بينها |

### 5.6 سلوك الأداء الجديد (المرحلة الثالثة)

| # | الخطوة | النتيجة المتوقعة |
|---|---|---|
| 1 | افتح صفحة "سجل العمليات" (إدارة أو جمعية) | تظهر حالة تحميل موجزة ثم السجل مُرقَّمًا بأزرار "السابق/التالي" إن تجاوز صفحة واحدة |
| 2 | ابحث في سجل العمليات باسم مستخدم أو إجراء | النتائج تُحدَّث من الخادم (لا فورًا محليًا) — انتظر جزءًا من الثانية |
| 3 | افتح صفحة "متابعة المشروع" (الإدارة) | الأنشطة والمراحل تُجلب عند الفتح، لا فور تسجيل الدخول |
| 4 | افتح صفحة "طلبات الانضمام" (الإدارة) | الطلبات تُجلب مُرقَّمة عند الفتح |
| 5 | احفظ مستفيدًا أو جهازًا أو مندوبًا | لا "تجميد" للواجهة؛ فقط زر الحفظ نفسه يُعطَّل مؤقتًا، بقية الأزرار (تسجيل الخروج، التنقّل) تبقى تعمل |
| 6 | افصل الإنترنت لحظيًا أثناء عملية حفظ (أدوات المطوّر ← Network ← Offline) ثم أعده | بعد نحو 25 ثانية تظهر رسالة "لا نعرف نتيجتها بعد" بدل تجمّد صامت؛ الزر يُفكّ تعطيله ويمكن إعادة المحاولة |
| 7 | أنشئ جمعية جديدة، راقب أن كلمة المرور المؤقتة تظهر مرة واحدة فقط | كما في القسم 5.2 سابقًا — سلوك لم يتغيّر رغم إعادة الهيكلة الداخلية |

## 6. النتائج المتوقعة إجمالًا

إن اجتزت كل بنود القسم 5 دون أي انحراف عن "النتيجة المتوقعة"، فالنسخة
جاهزة تقنيًا للنشر الفعلي حسب تقديرك. أي بند يفشل: وثّقه (لقطة شاشة + خطوات
إعادة الإنتاج) قبل المتابعة — لا تُصلح على النسخة الحية مباشرة، أعد
الاختبار على الفرع بعد الإصلاح ثم كرّر القسم 5 من جديد.

## 7. خطة الرجوع (Rollback)

إن ظهرت مشكلة **بعد** نشر هذا الفرع على نسخة حية:

1. **إن كانت المشكلة في الكود فقط (لا بيانات)**: من محرر Apps Script، اذهب
   إلى **Manage deployments** ← اختر النشرة الحالية ← **Edit** ← بدّل
   الإصدار (Version) إلى الإصدار السابق المنشور قبل هذا التحديث. هذا يُعيد
   كود التطبيق فورًا دون أي تأثير على البيانات المخزَّنة في Sheets (الكود
   والبيانات منفصلان تمامًا في Apps Script).
2. **إن شُغِّلت `setupSheets_(token)` بالخطأ على نسخة حية قبل التأكد**: آمنة —
   لا تحذف أعمدة أو صفوفًا قائمة، فقط تضيف الناقص. لا حاجة لأي رجوع.
3. **إن شُغِّلت `migratePhoneNumbers_(token)` وظهرت نتيجة غير متوقعة**: الدالة
   تُطبِّع فقط صيغة عرض الرقم (لا تحذف بيانات)؛ للتراجع، صحّح الأرقام
   المتأثرة يدويًا من داخل النظام (تعديل بيانات المستفيد/الجمعية/المندوب)
   بالاعتماد على تقرير `previewPhoneNormalization_(token)` المحفوظ.
4. **إن ظهرت مشكلة في بيانات حقيقية أُدخلت أثناء الاختبار الحي بالخطأ على
   نسخة إنتاجية بدل التجريبية**: لا تحذف الصفوف يدويًا بسرعة — استخدم
   نسخة "سجل الإصدارات" في Google Sheets (الملف ← سجل الإصدارات) لمعاينة
   واستعادة حالة الورقة قبل الاختبار، بدل حذف انتقائي قد يُفوِّت أثرًا جانبيًا.
5. **في كل الحالات**: لا تدمج أي Pull Request ولا تُبقِ نشرًا حيًّا يعمل
   بمشكلة معروفة أكثر من اللازم — التراجع للإصدار السابق (الخطوة 1) خيار
   فوري ورخيص دائمًا في Apps Script.

## 8. تذكير أخير قبل أي نشر واسع

- شغّل التسعة: `node tools/verify.js && node tools/smoke.js && node tools/server-test.js && node tools/security-test.js && node tools/state-test.js && node tools/account-test.js && node tools/perf-test.js && node tools/reference-test.js && node tools/integration-test.js` (680 تأكيدًا إجمالًا عند آخر commit).
- راجع `HANDOFF.md` لقائمة ما هو مكتمل فعليًا وما تبقى.
- لا تنشر ولا تدمج أي Pull Request إلا بعد موافقتك الصريحة المنفصلة —
  هذا الملف تحضير فقط، وليس تنفيذًا للنشر.

## 9. الأداء — القياس الفعلي ونتائجه (المرحلة الثالثة)

**تنبيه صادق مهم**: كل ما يلي قياسٌ **محلي داخل Node.js `vm`** عبر
`tools/perf-scale-test.js` — بيانات محاكاة في الذاكرة، بلا أي اتصال بـ
Google Sheets أو Apps Script حيَّين (نفس حجب الشبكة الموصوف في القسم 1
أعلاه). الأرقام بالمللي ثانية أدناه هي **زمن تنفيذ الشيفرة محليًا فقط**،
وهي أقل بكثير من زمن الشبكة الحقيقي على Apps Script (كل استدعاء `google.script.run`
فعلي يضيف عبء شبكة ومصادقة لا يُحاكيه Node.js). القيمة الحقيقية لهذا
القياس هي **اتجاه النمو مع حجم البيانات** و**عدد قراءات/كتابات الجداول
لكل عملية** — ذاك الرقم الثاني هو ما يتحكم فعليًا بزمن الشبكة الحي، وهو
مستقر تقريبًا بصرف النظر عن حجم البيانات (مُتحقَّق منه أدناه).

نتائج تشغيل فعلي لـ `node tools/perf-scale-test.js` عند إعداد هذا القسم:

| الحجم | دخول | Bootstrap | صفحة مستفيدين | بحث | حفظ | تعيين مندوب | تأكيد تسليم |
|---|---|---|---|---|---|---|---|
| 100 مستفيد | ~6ms | ~3ms | ~2ms | ~2ms | ~1ms | ~2ms | ~2ms |
| 1000 مستفيد | ~28ms | ~28ms | ~22ms | ~25ms | ~5ms | ~12ms | ~11ms |
| 5000 مستفيد | ~145ms | ~154ms | ~94ms | ~95ms | ~36ms | ~55ms | ~74ms |

عدد قراءات/كتابات الجداول الفعلية لكل عملية — **ثابت تمامًا عبر الأحجام
الثلاثة** (100/1000/5000)، وهذا هو الدليل الكمّي أن الترقيم/البحث لا
يزيدان عدد استدعاءات Sheets API مع نمو البيانات:

| العملية | قراءات | كتابات |
|---|---|---|
| `login` | 7 | 2 |
| `getBootstrapData` (ASSOCIATION) | 9 (≈4.5/طلب لوجود طلبين) | 0 |
| `listBeneficiaries` (صفحة/بحث) | 1 | 0 |
| `saveBeneficiary` | 4 | 2 |
| `assignDelegate` | 5 | 3 |
| `confirmDelivery` | 3 | 4 |

**الخلاصة الصادقة**: عدد قراءات/كتابات الجداول لكل عملية لا يتغيّر مع حجم
البيانات (المقياس الحقيقي لتكلفة Sheets API) — لكن **زمن التنفيذ نفسه
ينمو تقريبًا خطيًا** مع عدد الصفوف (ملموس بوضوح عند 5000)، لأن `readTable_`
لا يزال يقرأ الورقة **كاملة** دفعة واحدة ثم يُصفّي/يُرتّب/يُرقّم في ذاكرة
Apps Script — Google Sheets لا يوفّر استعلامًا جزئيًا حقيقيًا (`OFFSET`/`LIMIT`
على مستوى الـ API) دون فهرس أو قاعدة بيانات خارجية. هذا يعني:

- **حتى 1000–2000 مستفيد تقريبًا**: الأداء المتوقَّع جيد (أهداف القسم
  المطلوب: تنقّل محلي < ثانية، بحث < ثانيتين، حفظ < 3 ثوانٍ) — الزمن
  المحلي هنا صغير جدًا، وزمن الشبكة الحقيقي على Apps Script سيهيمن على
  التجربة (عادة 200–800ms لكل استدعاء حسب حِمل جوجل في تلك اللحظة، خارج
  سيطرة هذا الكود).
- **عند 5000 مستفيد فأكثر**: زمن معالجة البيانات نفسه (لا زمن الشبكة)
  يصبح ملموسًا (~150ms محليًا)، وسيتراكم فوق زمن الشبكة الحقيقي. هذا
  **قيد معماري حقيقي في بنية Sheets الحالية**، لا خطأ في هذه المرحلة —
  الحل الجذري (فهرسة/قاعدة بيانات مستقلة) خارج نطاق "تحسين الأداء ضمن
  البنية الحالية" الذي طُلب في هذه المرحلة تحديدًا، ويحتاج قرارًا معماريًا
  منفصلًا إن نما عدد المستفيدين إلى عشرات الآلاف مستقبلًا.
- لتكرار هذا القياس بنفسك أو بأحجام مختلفة: `node tools/perf-scale-test.js`
  (يقبل تعديل مصفوفة `sizes` أعلى الملف لأي حجم آخر).

**ما لم يُقَس حيًّا (ولن يُدَّعى قياسه)**: زمن الشبكة الفعلي بين المتصفح
وApps Script، سلوك حدود التنفيذ (6 دقائق لكل استدعاء) عند بيانات ضخمة
جدًا، وسلوك `CacheService`/`LockService` الحقيقيين تحت تزامن فعلي من عدة
مستخدمين — كل هذا يتطلّب اختبارًا حيًّا لا تتيحه بيئة هذه الجلسة.

## 10. البيانات المرجعية والترحيل الآمن (المرحلة الرابعة)

**تنبيه صادق**: كل ما يلي مؤكَّد داخل محاكاة Node.js `vm`
(`tools/reference-test.js`، 60 تأكيدًا) فقط — لا اختبار حي وقع من داخل هذه
الجلسة، لنفس سبب حجب الشبكة الموصوف في القسم 1.

- **العطلان الحيّان المُبلَّغان مُصلَحان عند الجذر**: فقدان الصفر الأول في
  الهاتف (`safeCell_` في `DataUtils.gs`) وتاريخ JavaScript خام في طلبات
  انضمام الجمعيات (`normalizeApplication_` في `Applications.gs`) — راجع
  `HANDOFF.md` قسم "المرحلة الرابعة" للتفصيل الكامل لكل سبب وإصلاح.
- **ظهور المنطقة/المدينة/التصنيف كحقول نص حر في نسختك الحية ليس عطلًا في
  هذا الكود** — القوائم المنسدلة المترابطة موجودة ومفعَّلة بالفعل في كل
  النماذج، لكنها تسقط تلقائيًا لحقل نص حر حتى تُشغِّل `migrateReferenceData_(token)`
  (الخطوة 5 في القسم 3 أعلاه). هذا سلوك توافق مقصود، لا خطأ.
- **الفجوة الفعلية الوحيدة التي أُصلحت في الواجهة**: نوع الجهاز في نموذج
  إضافة/تعديل جهاز كان دائمًا حقل نص حر (`deviceTypeField()` جديدة الآن،
  بنفس نمط بقية الحقول المرجعية).
- **قبل أي ترحيل على نسختك الحية**: شغّل `diagnoseReferenceDataIssues_(token)`
  (الخطوة 6 في القسم 3) أولًا لمعرفة الحجم الفعلي للقيم القديمة/غير
  المطابقة عندك تحديدًا — العدد الحقيقي غير معروف من هذه الجلسة لأنه يعتمد
  على بياناتك الفعلية.
- **الترحيل لا يخمّن أبدًا**: `migrateLegacyReferenceValues_` تقترح تصحيحًا
  فقط لحالات مؤكَّدة (تطابق حرفي بعد تنظيف المسافات، أربعة مرادفات معروفة
  لاختصارات مناطق شائعة، أو مدينة تتبع منطقة واحدة فقط بلا غموض) — أي شيء
  آخر (خصوصًا الحالة الاجتماعية/التصنيف/نوع الجهاز، إذ لا مرادفات معروفة
  لها إطلاقًا) يذهب إلى قائمة مراجعة يدوية منفصلة (`ambiguous`) دون أي لمس
  تلقائي.
- **قابلة لإعادة التشغيل بأمان**: تشغيل `migrateLegacyReferenceValues_(token, false)`
  مرتين متتاليتين على نفس البيانات لا يكرر أي تعديل ولا يفسد أي سجل غامض —
  مُتحقَّق منه اختباريًا صراحة.
- لتفصيل كل دالة ومسار اختبار: راجع `tools/reference-test.js` مباشرة
  (10 أقسام مرقَّمة تطابق كل بند طُلب صراحة لهذه المرحلة) و`HANDOFF.md`.

## 11. الموقع الجغرافي، الاستيراد بالإحداثيات، وأدوات المندوب (المرحلة الخامسة)

**تنبيه صادق**: كل ما يلي مؤكَّد داخل محاكاة Node.js (`tools/smoke.js` بمحرّك
DOM مبسّط بلا شبكة حقيقية، و`tools/server-test.js`/`tools/security-test.js`
بمحاكاة Apps Script) — **لم تُفتَح الخريطة (Leaflet/OpenStreetMap) فعليًا في
أي متصفح حقيقي من هذه الجلسة**، ولم يُختبَر `tel:`/واتساب على جهاز جوال
حقيقي. هذا نفس القيد الموثَّق في كل قسم سابق من هذا الملف (لا وصول لشبكة
خارجية من بيئة التنفيذ).

- **لا مفتاح API ولا خدمة مدفوعة أُضيفت**: Leaflet.js وبلاطات OpenStreetMap
  مجانيان بالكامل بلا تسجيل. تحقّق يدويًا عند الاختبار الحي أن الخريطة
  تُحمَّل فعليًا (قد تحتاج شبكة الاختبار السماح بالوصول لـ`unpkg.com`
  و`tile.openstreetmap.org` تحديدًا إن كانت خلف جدار حماية شركة صارم) —
  **وتأكّد أيضًا أن غياب الوصول لهذين النطاقين لا يمنع حفظ مستفيد جديد**
  (القسم 5.3 البند 2ب أعلاه يغطي هذا تحديدًا).
- **قبل تشغيل أي شيء على بيانات حقيقية**: الأعمدة الثلاثة الجديدة في
  "المستفيدون" ("علامة مميزة"، "مصدر الموقع"، "تاريخ تحديث الموقع") تُنشَأ
  تلقائيًا فقط عند تشغيل `setupSheets()` (الخطوة 1 في القسم 3 أعلاه) —
  لم تُشغَّل هذه المرحلة أي دالة جديدة، فقط أضافت أعمدة إلى بنية موجودة.
- **تغيير سلوك مقصود يستحق انتباهك خصيصًا**: نطاق الإحداثيات المقبول تحوّل
  من "مربع تقريبي لحدود السعودية" (خط عرض 15–33، خط طول 33–56) إلى **نطاق
  عالمي قياسي** (-90..90 / -180..180) بناءً على طلب صريح بهذه المرحلة. إن
  كنت تفضّل الإبقاء على القيد الجغرافي الأضيق الخاص بالمملكة كطبقة حماية
  إضافية ضد أخطاء إدخال واضحة (كعكس خط العرض والطول)، هذا قرار مستقل يحتاج
  طلبًا صريحًا لإعادته.
- **اختبار يدوي إضافي موصى به بعد التركيب**: افتح نموذج مستفيد على جوال
  حقيقي (لا محاكاة متصفح فقط) وتأكّد أن النقر على الخريطة يعمل باللمس،
  وأن السحب لا يتعارض مع تمرير الصفحة (Leaflet يعالج هذا افتراضيًا لكنه
  لم يُختبَر حيًّا من هذه الجلسة).
- لتفصيل كل دالة ومسار اختبار: راجع `tools/server-test.js` (قسمَا 16
  و17)، `tools/smoke.js` (قسم 3ب وقسم 6 المُوسَّع)، `tools/security-test.js`
  (قسم 10)، و`HANDOFF.md` قسم "المرحلة الخامسة".

## 12. ترابط دورة العمل الكاملة واستكمال البوابات الثلاث (المرحلة السادسة)

**تنبيه صادق**: كل ما يلي مؤكَّد داخل محاكاة Node.js فقط
(`tools/integration-test.js`، رحلة متصلة واحدة بـ43 تأكيدًا) — لم تُختبَر
هذه الرحلة في متصفح حقيقي ولا على Apps Script فعلي من هذه الجلسة.

- **عطل حقيقي أُصلح**: إضافة مستفيد من حساب الإدارة كانت تفشل دائمًا
  (لا حقل لاختيار الجمعية في النموذج). إن كنت جرَّبت هذا سابقًا على نسختك
  الحية ورأيت رسالة "اختر جمعية صحيحة" — هذا هو السبب، وهو مُصلَح الآن في
  هذا الفرع (لم يُصلَح على أي نسخة حية).
- **عطل حقيقي أُصلح**: لوحات بيانات الجمعيات والمناديب كانت تعرض أرقامًا
  قديمة حتى دقيقة كاملة بعد أي حفظ (ذاكرة Bootstrap المؤقتة لم تكن تُبطَل
  إلا لحساب الإدارة). إن لاحظت أرقامًا لا تتحدّث فورًا على نسختك الحية —
  هذا هو السبب على الأرجح.
- **تغيير سلوك مقصود**: أي حساب جمعية جديد (من قبول طلب أو إضافة إدارية
  مباشرة) يُفرَض عليه الآن تغيير كلمة المرور عند أول دخول — لم يكن كذلك
  سابقًا لمسار الإضافة المباشرة. إن كانت لديك جمعيات أُنشئت بهذه الطريقة
  على نسخة تجريبية سابقة، ستبقى كما هي (هذا لا يُطبَّق بأثر رجعي، فقط على
  حسابات جديدة تُنشَأ بعد هذا التحديث).
- **جديد يستحق اختبارًا يدويًا خاصًا**: صفحات المستفيدين/الجمعيات/الأجهزة/
  المناديب أصبحت الآن تُرقَّم وتُبحَث وتُفلتَر فعليًا من الخادم (لم تكن كذلك
  رغم توثيق سابق كان يزعم عكس ذلك) — تحقّق يدويًا أن التنقّل بين الصفحات
  وحقل البحث وفلتر الحالة وفلتر الجمعية (يظهر للإدارة فقط) كلها تعمل بسلاسة
  على بيانات حقيقية بأعداد كبيرة نسبيًا.
- لتفصيل كامل: راجع `tools/integration-test.js` (14 قسمًا مرقَّمة تطابق
  كل خطوة من الرحلة المطلوبة صراحة) و`HANDOFF.md` قسم "المرحلة السادسة".

## 13. المرحلة السابعة: إغلاق النواقص الوظيفية، الهوية البصرية، الجوال، والوصول

هذه المرحلة نُفِّذت على دفعتين متتاليتين فوق commit `2225364` (نهاية المرحلة
السادسة)، على الفرع نفسه، دون إعادة أي عمل سابق:

### 13.1 الدفعة الأولى — النواقص الوظيفية (commits `60da6a7`، `320065a`، `05407b2`)

- **منع التكرار في المستفيدين** (`Beneficiaries.gs`): مطابق مؤكَّد (نفس رقم
  الجوال، الأساسي أو الإضافي، ضمن الجمعية نفسها فقط) يُرفض دائمًا في الإضافة
  الفردية والاستيراد الجماعي وبين صفوف الملف نفسه — لا يُعتمد على الاسم وحده
  أبدًا. مطابق محتمل (اسم+مدينة) استشاري فقط، لا يمنع الحفظ. `saveBeneficiary`
  و`importBeneficiaries` يعيدان فحص التكرار المؤكَّد داخل `LockService`
  مباشرة قبل الكتابة الفعلية لمنع سباق تزامن بين طلبين متزامنين. رسالة
  الرفض لا تكشف أبدًا عن وجود/عدم وجود الرقم لدى جمعية أخرى (الفحص مقيَّد
  بجمعية الطالب فقط).
- **قالب Excel حقيقي** (`ExcelTemplate.gs` جديد): حزمة OOXML فعلية (ليست
  CSV بامتداد مغاير) تُبنى بالكامل عبر `Utilities.zip` المدمجة في Apps
  Script — بلا مكتبة خارجية وبلا خدمة مدفوعة، ولا شيء يُحمَّل عند فتح شاشة
  الدخول (يُستدعى فقط عند ضغط زر التنزيل). تتضمن الأعمدة الرسمية، صفّ مثال،
  ورقة "تعليمات" منفصلة، تنسيقًا نصيًا إجباريًا لعمود الجوال (لا يفقد الصفر
  الأول)، وقوائم Excel منسدلة أصلية (data validation) للمنطقة/الحالة
  الاجتماعية/الضمان الاجتماعي. اختبار بنيوي حقيقي (`server-test.js` قسم ١٩)
  يفتح الملف الناتج عبر `zipfile`/`ElementTree` القياسيتين في Python —
  أرشيف ZIP سليم فعليًا، كل أجزاء OOXML المطلوبة موجودة وسليمة البنية.
  محاولة تحقّق إضافية عبر LibreOffice headless في بيئة التطوير باءت بالفشل
  لسبب بيئي غير متعلق بالملف (فشل حتى تحويل ملف `.txt` عادي)، فلم تُستخدَم
  كدليل. **فتح الملف فعليًا داخل Microsoft Excel حقيقي يبقى بندًا يحتاج
  اختبارًا يدويًا حيًا.**
- **تفاصيل الجهاز** (`DevicesAssociations.gs`: `getDeviceDetail`): صفحة/نافذة
  تفاصيل كاملة (الحالة، الجمعية، المستفيد، المندوب المرتبط، كل التواريخ،
  سجل عمليات خاص بالجهاز)، مرتبطة من جدول الأجهزة وصفحة تفاصيل المستفيد.
  تاريخا "التخصيص" و"الخروج مع المندوب" يُشتقّان من سجل العمليات الموجود
  أصلًا (وليس عمودين جديدين في الجدول — `assignDelegate` أصبح يسجّل حدثًا
  إضافيًا لكل جهاز يخرج فعليًا مع مندوب) — **لا حاجة لأي ترحيل مخطط بيانات
  لهذا البند تحديدًا**.
- **سجل عمليات المندوب** (`Normalize.gs`: `listDelegateAuditLog`،
  و`listAuditLog` اكتسبت فلتر `options.recordId` جديدًا): الإدارة ترى
  السجل الكامل لأي مندوب، الجمعية ترى مندوبيها فقط، والمندوب يرى سجله
  التشغيلي فقط (دخول، تعيين، تعذر/إعادة محاولة، تسليم، تفعيل/تعطيل، إعادة
  إنشاء رمز) دون أي حدث إداري حسّاس — ولا رمز دخول ولا كلمة مرور ولا موقع حي
  مسجَّل أصلًا في أي مكان بالنظام.
- ١٤ اختبار خادم جديد (تكرار مؤكَّد/محتمل، تزامن، IDOR بين جمعيتين وبين
  مندوبين، صحة xlsx بنيويًا) — راجع `tools/server-test.js` الأقسام ١٨–٢٠.

### 13.2 الدفعة الثانية — الهوية البصرية، الجوال، والوصول (هذه الجولة)

**خلل مؤكَّد اكتُشف وأُصلح** (وليس مجرد تحسين): قاعدة CSS لإخفاء الشريط
الجانبي على الجوال (`≤820px`) كانت تحمل استثناءً لاتجاه RTL يُزيحه بالاتجاه
الخاطئ (`translateX(110%)` بدل `translateX(-110%)`) — النتيجة: نحو ٨٩ بكسل
من الشريط الجانبي كانت تبقى ظاهرة فوق المحتوى بدل الاختفاء الكامل عند إغلاقه.
تحقّق فعلي عبر Chromium حقيقي (Playwright، أداة تطوير محلية بحتة — ليست جزءًا
من مشروع Apps Script ولا من `tools/` المثبَّتة، ولم تُستخدم في مسار النشر):
قياس `getBoundingClientRect()`/`getComputedStyle().transform` فعليًا قبل
وبعد الإصلاح، ولقطات شاشة عند 360/390/768/1024/1440px لصفحات الإدارة/الجمعية/
المندوب وعدة نوافذ منبثقة، على بيانات وهمية بالكامل من `tools/preview.js`
(لا بيانات حقيقية إطلاقًا). تم توسيع شِم `tools/preview.js` بدوال قوائم
وهمية (`listBeneficiaries`/`listDevices`/...) لجعل صفحات الجدول القابلة
للمعاينة فعليًا لأول مرة.

إصلاحات وتحسينات إضافية مؤكَّدة بنفس الطريقة:
- شاشة الدخول على الجوال كانت تتمركز رأسيًا داخل `100vh` كاملة رغم اختفاء
  لوحة الفن الجانبية، ما يترك مساحة فارغة كبيرة أعلى النموذج — أصبح المحتوى
  يبدأ قريبًا من الأعلى بحشوة معقولة.
- `env(safe-area-inset-bottom)` أُضيف لتذييل النافذة المنبثقة على الجوال
  (نمط "ورقة سفلية") ولحاوية التنبيهات — يتجنّب تراكب أزرار الإجراء الأساسي
  مع شريط النظام على الأجهزة ذات الحافة السفلية المنحنية/الشق.
- **حصر تركيز فعلي (focus trap)** داخل النوافذ المنبثقة: الضغط على Tab من
  آخر عنصر يعيد التركيز لأول عنصر (زر الإغلاق ×) بدل الهروب لعناصر خلف
  النافذة — لم يكن موجودًا سابقًا رغم وجود نقل التركيز عند الفتح وإعادته
  عند الإغلاق مسبقًا. تحقّق فعليًا عبر Playwright (`Tab` من آخر عنصر ← أول
  عنصر بالضبط).
- **تحذير قبل إغلاق نافذة فيها تغييرات غير محفوظة**: أي إدخال/تغيير داخل
  نافذة مفتوحة يُعلِّمها "متغيّرة"؛ الإغلاق العرضي فقط (زر Escape أو النقر
  خارج النافذة) يعرض تأكيدًا أصليًا من المتصفح قبل الإغلاق الفعلي — أزرار
  الإلغاء/الإغلاق الصريحة في التذييل، وكل مسارات الإغلاق البرمجي بعد حفظ
  ناجح، تبقى فورية تمامًا كما كانت (لا تغيير في سلوكها). تحقّق فعليًا عبر
  Playwright: تعبئة حقل ثم Escape يعرض `window.confirm` فعليًا ويُبقي
  النافذة مفتوحة عند الرفض.
- روابط خارجية (`target="_blank"`) جميعها مؤكَّدة تحمل `rel="noopener noreferrer"`
  (كانت كذلك مسبقًا من مراحل سابقة — أُضيف اختبار انحدار صريح له الآن).
- `aria-live`/`role` على حاوية التنبيهات مضبوطان ديناميكيًا حسب نوع الرسالة
  (نجاح/تحذير/خطأ) — نوع "تحذير" (`warn`) جديد أُضيف لتنبيه التكرار المحتمل
  من الدفعة الأولى.
- `prefers-reduced-motion`، `role="dialog"`/`aria-modal`، وسمات `autocomplete`
  المناسبة لحقول الدخول (`username`/`current-password`/`one-time-code`) —
  كلها كانت موجودة مسبقًا من مراحل سابقة، أُضيفت لها اختبارات انحدار صريحة
  الآن بدل الاعتماد على المراجعة اليدوية فقط.

**لم يُعثر على تمرير أفقي غير مقصود في أي صفحة رُوجعت** (تحقّق عبر
`document.documentElement.scrollWidth - clientWidth === 0` فعليًا، ليس
افتراضًا) عند 360/390/768/1024/1440px للوحة البيانات، قائمة المستفيدين
(جدول يتحول لبطاقات على الجوال)، نافذة تفاصيل المستفيد، نافذة تفاصيل
الجهاز، بوابة الجمعية، وبوابة المندوب (قائمة المهام/مسار اليوم/السجل).

**هوية الشعارين**: كانا مُضمَّنين مسبقًا كـ`data:image/png;base64,...` داخل
`Index.html` (`BRAND.zadLogo`/`BRAND.partnerLogo`، من مرحلة سابقة موثَّقة)
— هذا هو بالضبط الحل الآمن لـ HtmlService الذي كان مطلوبًا (لا مسار GitHub
محلي هشّ)، ولم يتغيّر في هذه المرحلة. راجع `tools/smoke.js` للتحقق الآلي من
أن كلا الشعارين مُضمَّنان فعليًا كبيانات صحيحة لا نص فارغ.

**اختبارات جديدة**: ١٤ اختبار انحدار في `tools/smoke.js` (قسم ١١) تغطي إصلاح
الشريط الجانبي تحديدًا (يمنع عودة القاعدة الخاطئة بالاسم الحرفي)،
`safe-area-inset`، حصر التركيز، تحذير الإغلاق غير المحفوظ، الروابط
الخارجية، `aria-live`. **جميع مجموعات الاختبار الحالية شُغِّلت وتنجح**:
`verify.js` 31/31، `smoke.js` 186/186، `server-test.js` 183/183،
`security-test.js` 87/87، `state-test.js` 53/53، `account-test.js` 53/53،
`perf-test.js` 32/32، `reference-test.js` 60/60، `integration-test.js`
43/43 — **إجمالي 728 تأكيدًا ناجحًا**، بالإضافة إلى `perf-scale-test.js`
و`perf-bench.js` (تقارير أداء لا نجاح/فشل).

**حجم/بنية `Index.html`**: 374687 بايت (~366 كيلوبايت) بعد هذه المرحلة،
٣٧٠٢ سطرًا — الزيادة عن المرحلة السادسة طفيفة (إضافات CSS/JS مستهدفة فقط).
أغلب الحجم فعليًا هو بيانات الشعارين المُرمَّزة (base64)، لا الترميز/الأنماط/
البرمجة. **بقي ملفًا واحدًا موحَّدًا عمدًا** — نفس المسوّغ الموثَّق في مراحل
سابقة (`HtmlService.include()` يدمج المحتوى خادميًا قبل الإرسال فلا يقلّل
التقسيم من حجم الحمولة الفعلي المُرسَل للمتصفح، بينما يزيد من خطر أخطاء حدود
الملفات — وهو تحديدًا صنف الخلل الذي بدأ منه هذا المشروع أصلًا) يبقى صحيحًا؛
التعديلات هذه المرحلة (إصلاح CSS مركّز + دالتا JS صغيرتان) لا تُغيّر هذا
التقييم. لم تُعدَّل `tools/gs-manifest.js`/`tools/verify.js` لأن بنية الملفات
لم تتغيّر (لا ملفات جديدة أُضيفت في هذه الدفعة الثانية تحديدًا).

**بنود تحتاج اختبارًا حيًا لم تُختبَر هنا**: فتح قالب `.xlsx` فعليًا داخل
Microsoft Excel حقيقي (وليس فقط python zipfile/ElementTree)؛ تحقّق التباين
اللوني وفق WCAG AA بأداة قياس تباين فعلية (رُوجع التصميم بصريًا فقط عبر
لقطات شاشة، لا بأداة قياس تباين آلية)؛ سلوك لوحة مفاتيح الجوال الفعلية عند
التركيز على الحقول (لا يمكن محاكاتها في متصفح سطح مكتب headless)؛ التصغير/
التكبير الفعلي للنص إلى 200% في متصفح حقيقي؛ وقارئ شاشة فعلي (مثل NVDA/
VoiceOver) لم يُستخدم — الاعتماد كان على العناصر الدلالية وسمات ARIA
الصحيحة فقط، لا تشغيلًا فعليًا لقارئ شاشة.

## 14. تنظيم Index.html — تقييم عملي لتقسيمه (المرحلة الثامنة)

الحجم الحالي: **374,687 بايت (~366 كيلوبايت)، 3,702 سطرًا**. **القرار:
البقاء ملفًا واحدًا موحَّدًا** — لكن هذه المرة بتقييم عملي كامل لخيار
التقسيم المقترح تحديدًا، لا تكرارًا للتبرير السابق فقط.

### الخيار المُقيَّم: `Styles.html` / `AppShell.html` / `ClientCore.html` / `AdminPortal.html` / `AssociationPortal.html` / `DelegatePortal.html`

**الآلية التقنية المتاحة فعليًا**: نمط `<?!= include('Filename') ?>` القياسي
في HtmlService (دالة `include(name)` تُعيد `HtmlService.createHtmlOutputFromFile(name).getContent()`،
تُستدعى من `Index.html` الرئيسي عبر scriptlets قبل الإرسال للمتصفح). هذا
ممكن تقنيًا ومُختبَر في مشاريع Apps Script أخرى — **ليس عائقًا تقنيًا**.

**لماذا تقرَّر عدم تنفيذه رغم ذلك:**

1. **لا فائدة حمولة/سرعة فعلية إطلاقًا**: `HtmlService.include()` تنفيذ
   خادمي بحت — يدمج كل الملفات إلى مستند HTML واحد **قبل** إرساله للمتصفح.
   المتصفح يستقبل نفس عدد البايتات بالضبط سواء كان المصدر ملفًا واحدًا أو
   سبعة. لا تقسيم كسول (lazy loading) حقيقي ممكن هنا أصلًا، لأن الدخول
   الحالي صفحة واحدة (SPA) تُقرِّر البوابة (إدارة/جمعية/مندوب) **بعد**
   تسجيل الدخول على العميل، لا قبل رسم الصفحة على الخادم — فصل
   `AdminPortal.html` عن `DelegatePortal.html` لا يعني تحميل أحدهما فقط
   دون الآخر؛ كلاهما يُرسَلان معًا دائمًا كما هو الحال الآن مع الملف
   الموحَّد. تحقيق تحميل انتقائي حقيقي يتطلب إعادة هندسة تدفق الدخول
   نفسه (مثل `doGet(e)` يعرض بوابة مختلفة حسب معامل رابط أو جلسة سابقة) —
   تغيير معماري أكبر بكثير من مجرد "تقسيم ملف"، وخارج نطاق هذه المرحلة
   الأمنية/التنظيمية.
2. **يزيد خطر أخطاء التركيب اليدوي فعليًا**: التركيب موثَّق بالكامل في
   `DEPLOYMENT.md` كنسخ-ولصق يدوي (لا أداة نشر آلية متصلة بحساب Google —
   بالتصميم، لتفادي تخزين أي مفتاح/توكن). تحويل "الصق ملفًا واحدًا باسم
   Index" إلى "أنشئ 6 ملفات HTML بأسماء دقيقة، وتأكد أن كل scriptlet
   `include()` يشير للاسم الصحيح حرفيًا" يرفع سطح الخطأ البشري بشكل مباشر
   وواضح — بالضبط الحالة التي حذَّرت التعليمات من تنفيذها ("لا تقسّمه إذا
   سيزيد أخطاء التركيب اليدوي").
3. **يكسر أدوات الاختبار المحلية دون فائدة توازيها**: `tools/smoke.js`
   يستخرج كتلة JavaScript الفعلية من `Index.html` عبر افتراض "آخر وسم
   `<script>`" — تقسيم الأنماط/الهيكل/العميل عبر ملفات HtmlService منفصلة
   يتطلّب إعادة كتابة منطق الاستخراج هذا (ومحاكاة `include()` نفسها) في كل
   أداة اختبار محلية تتعامل مع `Index.html` (`smoke.js`/`verify.js`/
   `preview.js`) — عمل حقيقي وخطر انحدار حقيقي، بلا أي مقابل في السرعة أو
   الحمولة الفعلية (نقطة 1).
4. **الحجم الفعلي ليس مشكلة أداء ملموسة**: أغلب الـ366 كيلوبايت هو بيانات
   الشعارين المُرمَّزة (base64) — **إزالتها من الحساب تترك أقل من نصف الحجم
   الفعلي كترميز/تصميم/برمجة حقيقية**، وهو حجم معقول جدًا لتطبيق SPA كامل
   بأربع بوابات. لا مؤشر أداء فعلي (لا محليًا ولا حيًا) يُظهر أن حجم الملف
   الحالي يُبطئ أي شيء ملموس.

**متى يستحق التقسيم إعادة تقييم لاحقًا**: إن نما المشروع لإضافة بوابات
جديدة كاملة (لا تعديل بوابات قائمة)، أو إن تغيّر تدفق الدخول ليصبح
خادمي القرار (كل بوابة على رابط/معامل مختلف بدل SPA واحدة) — عندها يصبح
تحميل انتقائي حقيقي ممكنًا ومفيدًا فعليًا، ويستحق التقسيم حينها.

**لم يتغيّر ترتيب تنفيذ ملفات `.gs`**: `tools/gs-manifest.js` مُحدَّث
بالملف الجديد `ReleaseOps.gs` فقط (إضافة ترتيب، لا تغيير ترتيب قائم) —
Apps Script نفسه لا يعتمد على أي ترتيب فعليًا (نطاق عام واحد، موثَّق في
تعليق الأداة نفسها).

## 15. المرحلة الثامنة: المراجعة الأمنية النهائية، preflight/dry-run، وRelease Candidate

راجع `SECURITY_REVIEW.md` (جرد سطح النظام الكامل، مصفوفة الصلاحيات،
الاكتشاف المعماري المؤكَّد حول دوال الإعداد بلا تحقق جلسة، ومعالجة صور
إثبات التسليم بالتحقق من التوقيع الحقيقي للبايتات) و`DEPLOYMENT.md`
(الترتيب التنفيذي الكامل: preflight ← dry-run ← تطبيق المخطط ← نشر تجريبي
← اختبار البوابات الأربع ← معايير القبول ← الرجوع) للتفصيل الكامل.

ملخص تنفيذي:
- **`ReleaseOps.gs` جديد**: `preflightRelease()` (قراءة فقط بالكامل،
  مختبَرة أنها لا تكتب شيئًا) و`applyReleaseSchema(options)` (إضافية بحتة،
  ترفض العمل بلا `options.approvalCode` مطابق حرفيًا، آمنة لإعادة
  التشغيل، لا تُنشئ حسابات ولا تستبدل كلمات مرور) — بديل آمن موصى به بدل
  استخدام `setupSheets()` مباشرة كخطوة نشر عامة متكررة.
- **معالجة أمنية فعلية**: `verifyImageMagicBytes_()` جديدة في `Delegates.gs`
  تتحقق من التوقيع الحقيقي لبايتات صورة إثبات التسليم بعد فك base64 (لا
  الاكتفاء بتصديق بادئة `data:image/...` المُعلَنة من العميل) — صور مموَّهة
  (نص/HTML ببادئة صورة مزيَّفة) تُرفض الآن، ومختبَرة صراحة.
- **اكتشاف موثَّق دون معالجة تلقائية** (يحتاج قرارك): دوال الإعداد/الترحيل
  القديمة (`setupSheets`/`migrateReferenceData`/`migrateLegacyReferenceValues`/
  `migratePhoneNumbers`/`repairStateIntegrityIssues`) بلا أي تحقق جلسة —
  خاصية معمارية أصيلة في Apps Script (كل دالة top-level عامة تلقائيًا)، لا
  خلل محلي. تفصيل الأثر والتخفيف المقترَح في `SECURITY_REVIEW.md` القسم 2.
- **تنظيم Index.html**: قُيِّم عمليًا تقسيمه (القسم 14 أعلاه) وتقرَّر
  البقاء موحَّدًا — لا فائدة حمولة فعلية مع HtmlService، ويزيد خطر التركيب
  اليدوي فعليًا.
- **10 اختبارات جديدة في `server-test.js`** (قسم 21: preflight/applyReleaseSchema)،
  **4 اختبارات جديدة في `security-test.js`** (قسم 6: التحقق من التوقيع
  الحقيقي)، وتحديث اختبار وحيد في `reference-test.js` (توقّع استدعاء
  `diagnoseReferenceDataIssues` من `preflightRelease` الجديدة، بدل توقّع
  عدم استدعائها إطلاقًا). **إجمالي الاختبارات الآن: 745** عبر 9 أدوات —
  راجع القسم 16 أدناه للأرقام الكاملة.
- **لم يُشغَّل** `setupSheets`/`preflightRelease`/`applyReleaseSchema`/أي
  دالة ترحيل أو إصلاح على أي بيانات حية من هذه الجلسة. لم يُنشر أي إصدار،
  لم يُدمَج أي Pull Request، لم تُعدَّل أي بيانات Google Sheets حية.

## 16. نتائج الاختبارات (آخر تشغيل، بعد التصحيح الأمني اللاحق)

```
node tools/verify.js            → 31/31
node tools/smoke.js             → 186/186
node tools/server-test.js       → 198/198
node tools/security-test.js     → 141/141
node tools/state-test.js        → 56/56
node tools/account-test.js      → 53/53
node tools/perf-test.js         → 32/32
node tools/reference-test.js    → 61/61
node tools/integration-test.js  → 43/43
```

**المجموع: 801/801**، بالإضافة إلى `tools/perf-scale-test.js` و
`tools/perf-bench.js` (تقريرا أداء لا يعتمدان على نجاح/فشل) — كلاهما
شُغِّلا ونجحا بلا أخطاء. أداة المعاينة البصرية `tools/preview.js` لم
تُشغَّل هذه الدفعة لأن `Index.html` لم يتغيّر (تعديلات `.gs` وملفات
التوثيق فقط).

## 17. تصحيح أمني لاحق: قفل كل دوال الصيانة عن google.script.run

راجع `SECURITY_REVIEW.md` القسم 2 (مُحدَّث بالكامل) و`HANDOFF.md` قسم
"تصحيح أمني لاحق للمرحلة الثامنة" للتفصيل الكامل. ملخّص: الفجوة المعمارية
الموثَّقة في القسم 15 أعلاه (`SECURITY_REVIEW.md` عبر `RELEASE.md`، دوال
إعداد/ترحيل بلا تحقق جلسة) **عولجت
فعليًا** — عشر دوال صيانة أُعيدت تسميتها لتنتهي بشرطة سفلية وأصبحت تتطلب
رمز وصول صيانة مؤقت (`grantMaintenanceAccess_`، مُولَّد عشوائيًا، مُخزَّن
مُجزَّأً فقط، مؤقت، مقفَل تلقائيًا بعد محاولات فاشلة، غير مُسجَّل في أي
مكان). لا رمز موافقة ثابت في الكود بعد الآن. `DEPLOYMENT.md` أُعيد ترقيمه
وتحديثه بالكامل بالترتيب التنفيذي الجديد. لم يُشغَّل أي شيء على بيانات
حية.
