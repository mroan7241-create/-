# دليل التركيب والنشر — Release Candidate (المرحلة الثامنة)

هذا الملف إجرائي وتنفيذي فقط: **ماذا تفعل بالضبط، بأي ترتيب**، قبل أي
تركيب حي. للتفصيل الكامل (لماذا، والقياسات، وسجل كل مرحلة) راجع
`RELEASE.md` و`HANDOFF.md` و`SECURITY_REVIEW.md`. لم يُنفَّذ أي بند من هذا
الملف فعليًا من داخل هذه الجلسة — كل شيء هنا **مُعدّ لك لتنفّذه بنفسك**.

آخر commit مبني عليه هذا الدليل: راجع `git log -1` على الفرع
`claude/code-index-review-kz5k4u`.

---

## 1. المتطلبات

- حساب Google (Workspace أو شخصي) بصلاحية إنشاء Google Sheets وApps Script.
- **لا تحتاج أي مفتاح API ولا اشتراك مدفوع** — المشروع بأكمله مجاني
  بالكامل (Leaflet/OpenStreetMap لخرائط المستفيدين، `tel:`/واتساب/خرائط
  Google كروابط عادية بلا مفتاح).
- متصفح حديث لاختبار الواجهة (Chrome/Edge/Firefox/Safari محدَّثة).
- صلاحية الوصول لـ **Executions** (سجل التنفيذ) في محرر Apps Script لقراءة
  بيانات الدخول المؤقتة عند أول إعداد.

## 2. النسخ الاحتياطي (قبل أي شيء آخر، حتى على نسخة تجريبية جديدة)

إن كانت هذه أول نسخة تجريبية فارغة: لا حاجة لنسخ احتياطي (لا بيانات
لفقدانها). إن كنت تُحدِّث نسخة **قائمة بالفعل** (تجريبية سبق تركيبها، أو
—الأخطر— نسخة حية):

1. **نسخة الجداول**: من Google Sheets ← ملف ← إنشاء نسخة (Make a copy) —
   احتفظ بالنسخة باسم واضح يتضمن التاريخ، مثل "نظام الزاد — نسخة احتياطية
   قبل تحديث المرحلة الثامنة — 2026-07-31". **لا تعتمد على "سجل الإصدارات"
   وحده كنسخة احتياطية** — أنشئ نسخة كاملة منفصلة أيضًا.
2. **نسخة الكود**: من محرر Apps Script ← انسخ محتوى كل ملف `.gs`/`Index.html`
   الحالي (**قبل** استبداله) إلى مجلد محلي أو Google Drive منفصل بتاريخ
   واضح. (هذا المستودع نفسه هو نسخة الكود الجديدة — لا تحتاج نسخه، فقط
   احتفظ بالقديم قبل الاستبدال.)
3. **رابط النشر الحالي**: من محرر Apps Script ← Deploy ← Manage deployments
   — انسخ رابط النشرة الحالية ورقم إصدارها (Version) واحتفظ بهما.
4. **سجّل** اسم الملف/الرابط لكل نسخة احتياطية في مكان تتذكره — ستحتاجها
   في خطة الرجوع (القسم 11).

## 3. قائمة ملفات Apps Script (15 ملف `.gs` + `Index.html`)

بالترتيب الأبجدي كما في المستودع (لا يوجد ترتيب إلزامي فعليًا — Apps
Script يدمج كل ملفات `.gs` في نطاق عام واحد):

```
Applications.gs
Auth.gs
Beneficiaries.gs
Bootstrap.gs
Config.gs
DataUtils.gs
Delegates.gs
DevicesAssociations.gs
ExcelTemplate.gs        ← جديد (المرحلة السابعة)
ExecutionTracking.gs
Normalize.gs
Pagination.gs
ReferenceData.gs
ReleaseOps.gs            ← جديد (هذه المرحلة الثامنة)
StateRules.gs
Validation.gs
Index.html
```

راجع `README.md` لجدول دور كل ملف بالتفصيل.

## 4. طريقة نقل الملفات

**لا توجد أداة بناء/نشر آلية في هذا المستودع تتصل بـ Apps Script مباشرة**
(عمدًا — لا مفتاح/توكن Google مخزَّن في أي مكان). النقل يدوي:

1. افتح Google Sheet الفارغة (أو التجريبية) ← الإضافات ← Apps Script.
2. لكل ملف `.gs` أعلاه: أنشئ ملف script جديد بنفس الاسم بالضبط (بلا
   الامتداد `.gs` عند التسمية داخل المحرر)، والصق المحتوى كاملًا.
3. لملف `Index.html`: أنشئ ملف HTML باسم `Index` بالضبط (حساس لحالة
   الأحرف، ويُستخدَم في `doGet()`)، والصق المحتوى كاملًا.
4. احفظ المشروع (Ctrl+S / ⌘+S).

## 5. ترتيب preflight (قراءة فقط — آمن دائمًا، أي عدد مرات)

**قبل أي تشغيل لأي دالة كتابة**، من محرر Apps Script، اختر الدالة
`preflightRelease` من القائمة المنسدلة أعلى المحرر واضغط ▶ Run:

- **أول مرة على مشروع فارغ تمامًا**: ستفشل الدالة (لا توجد أوراق بعد) —
  هذا متوقَّع، انتقل مباشرة للقسم 6.
- **على مشروع مُهيَّأ جزئيًا أو بالكامل**: تعيد تقريرًا كاملًا JSON في نافذة
  التنفيذ يشمل: كل ورقة (موجودة/ناقصة)، الأعمدة الناقصة إن وُجدت، ترتيب
  الأعمدة، تعارضات الحالات (`stateIntegrity`)، القيم المرجعية غير المطابقة
  (`referenceData`)، عدد السجلات المتأثرة، حالة مجلد الإثباتات، وإصدار
  المخطط الحالي/المطلوب (`schemaVersion`). **راجعه بعناية كاملة قبل أي
  خطوة تالية.**

## 6. ترتيب dry-run

- إن أظهر `preflightRelease()` أن `referenceData.ready === false` أو
  وجود قيم مرجعية غير مطابقة: شغّل `diagnoseReferenceDataIssues()` (قراءة
  فقط) لتفصيل كامل، ثم `migrateLegacyReferenceValues(true)` (معاينة فقط،
  `dryRun=true`) لمراجعة كل تصحيح مقترَح قبل أي كتابة.
- إن أظهر `stateIntegrity.issueCount > 0`: شغّل `diagnoseStateIntegrity()`
  مباشرة (نفس التقرير المُضمَّن في preflight، لكن بتفصيل أوسع) وراجع كل
  تعارض قبل التفكير بـ `repairStateIntegrityIssues()`.
- لا يوجد "dry-run" منفصل لـ `applyReleaseSchema()` نفسها لأنها إضافية
  بحتة أصلًا (لا تحذف ولا تستبدل) — لكن `preflightRelease()` قبلها يعرض
  بدقة ماذا ستضيف بالضبط (`missingSheets`/`sheetsWithMissingColumns`).

## 7. تطبيق المخطط بعد الموافقة

**فقط بعد رضاك التام عن تقرير القسم 5**، من محرر Apps Script، لا يمكن
تمرير معامل عبر زر ▶ Run مباشرة — اكتب دالة صغيرة مؤقتة في أي ملف `.gs`
(يمكن حذفها بعد التشغيل)، ثم شغّلها هي بدل `applyReleaseSchema` مباشرة:

```js
function _runApplyReleaseSchemaOnce() {
  const result = applyReleaseSchema({approvalCode: 'أوافق-على-تطبيق-مخطط-الإصدار'});
  Logger.log(JSON.stringify(result, null, 2));
}
```

راجع `createdSheets`/`addedColumns` في السجل — إن كانت فارغتين، المخطط
كان مكتملًا أصلًا. **آمنة لإعادة التشغيل** إن احتجت تكرارها.

إن كان هذا أول إعداد لمشروع فارغ تمامًا (لا أول أدمن بعد): شغّل
`setupSheets()` بدل ذلك (تُنشئ أول حساب مدير أيضًا، بخلاف `applyReleaseSchema`
التي لا تُنشئ حسابات إطلاقًا) — **انسخ بيانات المدير المؤقتة من سجل
التنفيذ فورًا، قبل أي خطوة أخرى، وقبل أي نشر عام لرابط التطبيق** (راجع
`SECURITY_REVIEW.md` القسم 2 لسبب أهمية هذا الترتيب تحديدًا).

## 8. نشر نسخة تجريبية منفصلة

1. Deploy ← New deployment ← Web app.
2. Execute as: **Me** (مالك المشروع).
3. Who has access: **حصريًا وفق قرارك** — للاختبار الأولي، يُفضَّل
   "Only myself" أو "Anyone within [نطاقك]" بدل "Anyone" علنًا، لتضييق
   نافذة الخطر الموثَّقة في `SECURITY_REVIEW.md` القسم 2 إلى أدنى حد ممكن
   أثناء الاختبار.
4. انسخ رابط النشرة الناتج — هذا رابط الاختبار الحي، منفصل تمامًا عن أي
   نشرة سابقة.

## 9. اختبار البوابات الأربع

راجع `RELEASE.md` القسم 5 لقائمة الاختبار اليدوي الكاملة المفصَّلة
(البوابة العامة، الإدارة، الجمعية، المندوب) — لا تُكرَّر هنا بالكامل. أضيفت
هذه المرحلة بنود جديدة يجب اختبارها يدويًا تحديدًا:

- **قالب Excel**: نزّل قالب `.xlsx` من نافذة الاستيراد الجماعي (بوابة
  الجمعية) وافتحه فعليًا في Microsoft Excel حقيقي (أو Google Sheets عبر
  "فتح باستخدام") — تحقق: الأعمدة الرسمية، ورقة "تعليمات" ثانية، القوائم
  المنسدلة تعمل فعليًا عند النقر على خلايا المنطقة/الحالة الاجتماعية/الضمان،
  ورقم الجوال في صف المثال يحافظ على الصفر الأول ولا يتحول لصيغة علمية.
- **تفاصيل الجهاز**: من جدول الأجهزة وصفحة تفاصيل المستفيد، افتح "تفاصيل"
  لجهاز مخصَّص فعليًا لمستفيد له مندوب — تحقق ظهور كل التواريخ والمندوب
  المرتبط وسجل العمليات.
- **سجل المندوب**: من صفحة تفاصيل المندوب (إدارة/جمعية) اضغط "سجل
  العمليات" — وسجّل دخول كمندوب فعليًا وافتح "سجل عملياتي الكامل" من تبويب
  السجل — تحقق أن المندوب لا يرى أحداثًا إدارية (مثل "إضافة مندوب").
- **منع التكرار**: أضف مستفيدًا بجوال موجود مسبقًا لنفس الجمعية — تحقق
  الرفض الفوري بلا كشف بيانات جمعية أخرى؛ أضف مستفيدًا بنفس الاسم والمدينة
  لكن جوال مختلف — تحقق ظهور تنبيه "مطابق محتمل" غير مانع.
- **الجوال**: افتح كل بوابة على جهاز جوال حقيقي (لا محاكي فقط) بعرض قريب
  من 390px — تحقق القائمة الجانبية تُفتح وتُغلق بلمسة، لا تمرير أفقي، لوحة
  المفاتيح لا تحجب زر الحفظ الأساسي في النماذج الطويلة.
- **رفع إثبات التسليم**: من بوابة المندوب، أكّد تسليمًا فعليًا بصورة
  حقيقية ملتقطة من كاميرا الجوال — تحقق نجاح الرفع وظهور رابط الإثبات في
  سجل التسليمات؛ جرّب رفع ملف غير صورة (مثل PDF مُعاد تسميته لامتداد صورة)
  وتحقق الرفض.

## 10. معايير القبول

النسخة تُعتبر جاهزة للانتقال من "اختبار" إلى "استخدام فعلي محدود" فقط عند
تحقق **كل** ما يلي معًا:
- كل بند في قائمة الاختبار اليدوي (القسم 9 هنا + `RELEASE.md` القسم 5)
  ✅ منفَّذ فعليًا على المتصفح/الجوال الحقيقيين، لا افتراضًا.
- `preflightRelease()` يعيد `missingSheets: []` و`sheetsWithMissingColumns: []`
  و`stateIntegrity.issueCount === 0`.
- كل مجموعات الاختبار المحلية (القسم 12 في `RELEASE.md`، و`tools/*.js`)
  خضراء بالكامل عند آخر commit قبل النشر.
- بيانات المدير المؤقتة الأولى وأي كلمة مرور مؤقتة أخرى **غُيِّرت فعليًا**
  من كلماتها الافتراضية.
- رابط النشرة "Who has access" مضبوط عمدًا وفق قرارك، لا تركه على الإعداد
  الافتراضي دون مراجعة.

**لا تُعلَن هذه النسخة "Production Ready" حتى الآن** — هذا خارج نطاق
هذه الجلسة عمدًا، القرار قرارك بعد اكتمال القسم 9 فعليًا.

## 11. خطة الرجوع (Rollback)

راجع `RELEASE.md` القسم 7 للتفصيل الكامل (5 خطوات، تشمل التراجع عن نشرة
كود عبر Manage deployments، وتصحيح بيانات هاتف يدويًا، واستعادة سجل
إصدارات Google Sheets). **إضافة هذه المرحلة**: إن طُبِّق `applyReleaseSchema()`
وظهرت مشكلة، لا حاجة لأي "تراجع" خاص — الدالة **لا تحذف ولا تستبدل أي شيء
قائم إطلاقًا**، فقط تضيف أعمدة/أوراق فارغة؛ يمكن حذف أي ورقة/عمود أُضيف
يدويًا من واجهة Google Sheets نفسها إن رغبت، بلا أي أثر على بيانات موجودة
مسبقًا.

**نقطة توقف واضحة**: إن فشلت أي خطوة أعلاه (خطأ غير متوقَّع، تقرير
`preflightRelease` يظهر شيئًا لا تفهمه)، **توقف فورًا ولا تكمل للخطوة
التالية** — راجع الخطأ كاملًا، وإن لزم الأمر ارجع لنسخة النشرة السابقة
(القسم 2/الخطوة 3) بدل المحاولة على النسخة نفسها.

---

## بنود لا تزال تحتاج اختبارًا حيًا (لم تُختبَر من هذه الجلسة)

- فتح ملف `.xlsx` الجديد داخل Microsoft Excel حقيقي (اختُبر بنيويًا فقط
  عبر Python `zipfile`/`ElementTree`، راجع `RELEASE.md` القسم 13).
- كل بند في القسم 9 أعلاه (البوابات الأربع، الجوال الحقيقي، رفع إثبات
  تسليم حقيقي).
- تباين الألوان وفق WCAG AA بأداة قياس فعلية؛ قارئ شاشة فعلي (NVDA/
  VoiceOver)؛ تكبير النص إلى 200% في متصفح حقيقي.
- أداء Apps Script/Sheets الفعلي على بيانات كبيرة حية (قِيس محليًا فقط،
  راجع `RELEASE.md` القسم 9).
- اختراق مستقل احترافي — هذه الجلسة قدَّمت مراجعة أمنية برمجية ومحاكاة
  فقط (`SECURITY_REVIEW.md`)، وليست بديلًا عنه.
