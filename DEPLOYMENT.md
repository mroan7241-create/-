# دليل التركيب والنشر — Release Candidate (حتى المرحلة العاشرة)

هذا الملف إجرائي وتنفيذي فقط: **ماذا تفعل بالضبط، بأي ترتيب**، قبل أي
تركيب حي. للتفصيل الكامل (لماذا، والقياسات، وسجل كل مرحلة) راجع
`RELEASE.md` و`HANDOFF.md` و`SECURITY_REVIEW.md`. لم يُنفَّذ أي بند من هذا
الملف فعليًا من داخل هذه الجلسة — كل شيء هنا **مُعدّ لك لتنفّذه بنفسك**.

آخر commit مبني عليه هذا الدليل: راجع `git log -1` على الفرع
`claude/code-index-review-kz5k4u`.

---

## 1. المتطلبات

- حساب Google (Workspace أو شخصي) بصلاحية إنشاء Google Sheets وApps Script.
- **لا تحتاج أي مفتاح API ولا اشتراك مدفوع** — المشروع بأكمله مجاني
  بالكامل (Leaflet/OpenStreetMap لخرائط المستفيدين، `tel:`/واتساب/خرائط
  Google كروابط عادية بلا مفتاح).
- متصفح حديث لاختبار الواجهة (Chrome/Edge/Firefox/Safari محدَّثة).
- صلاحية الوصول لـ **Executions** (سجل التنفيذ) في محرر Apps Script لقراءة
  بيانات الدخول المؤقتة عند أول إعداد.

## 2. النسخ الاحتياطي (قبل أي شيء آخر، حتى على نسخة تجريبية جديدة)

إن كانت هذه أول نسخة تجريبية فارغة: لا حاجة لنسخ احتياطي (لا بيانات
لفقدانها). إن كنت تُحدِّث نسخة **قائمة بالفعل** (تجريبية سبق تركيبها، أو
—الأخطر— نسخة حية):

1. **نسخة الجداول**: من Google Sheets ← ملف ← إنشاء نسخة (Make a copy) —
   احتفظ بالنسخة باسم واضح يتضمن التاريخ، مثل "نظام الزاد — نسخة احتياطية
   قبل تحديث المرحلة الثامنة — 2026-07-31". **لا تعتمد على "سجل الإصدارات"
   وحده كنسخة احتياطية** — أنشئ نسخة كاملة منفصلة أيضًا.
2. **نسخة الكود**: من محرر Apps Script ← انسخ محتوى كل ملف `.gs`/`Index.html`
   الحالي (**قبل** استبداله) إلى مجلد محلي أو Google Drive منفصل بتاريخ
   واضح. (هذا المستودع نفسه هو نسخة الكود الجديدة — لا تحتاج نسخه، فقط
   احتفظ بالقديم قبل الاستبدال.)
3. **رابط النشر الحالي**: من محرر Apps Script ← Deploy ← Manage deployments
   — انسخ رابط النشرة الحالية ورقم إصدارها (Version) واحتفظ بهما.
4. **سجّل** اسم الملف/الرابط لكل نسخة احتياطية في مكان تتذكره — ستحتاجها
   في خطة الرجوع (القسم 12).

## 3. قائمة ملفات Apps Script (16 ملف `.gs` + `Index.html`)

بالترتيب الأبجدي كما في المستودع (لا يوجد ترتيب إلزامي فعليًا — Apps
Script يدمج كل ملفات `.gs` في نطاق عام واحد):

```
Applications.gs
Auth.gs
Beneficiaries.gs
Bootstrap.gs
Config.gs
DataUtils.gs
Delegates.gs
DevicesAssociations.gs
ExcelTemplate.gs        ← جديد (المرحلة السابعة)
ExecutionTracking.gs
Normalize.gs
Pagination.gs
ReferenceData.gs
ReleaseOps.gs            ← جديد (المرحلة الثامنة؛ حُدِّثت آلية الوصول في تصحيح لاحق — راجع القسم 5)
StateRules.gs
Validation.gs
Index.html
```

راجع `README.md` لجدول دور كل ملف بالتفصيل.

**المرحلة التاسعة (2026/08/01):** لم يُضَف ولم يُحذف أي ملف تشغيل — البنية
تبقى **16 ملف `.gs` + `Index.html`** بنفس الأسماء تمامًا. التعديلات
جوهرية داخل الملفات الحالية، وأثقلها في:
`DataUtils.gs` (نطاق الطلب الواحد، قياس الأداء، رفض المعرّف المكرَّر)،
`Bootstrap.gs` (`getPortalBundle`)، `Delegates.gs` (`retryDelivery`
وسجل المحاولات)، `ReferenceData.gs` (المصدر المضمَّن + grandfathering)،
`Applications.gs` (قرار ذرّي)، `Auth.gs` (بدء الطلب)، و`Index.html`
(اللوحة التنفيذية للأنشطة، بوابة التسليم، حالة التشغيل، حالات المسار).
لذلك **يجب استبدال كل الملفات الـ17 بالكامل** عند التركيب اليدوي، لا
بعضها.

## 4. طريقة نقل الملفات

**لا توجد أداة بناء/نشر آلية في هذا المستودع تتصل بـ Apps Script مباشرة**
(عمدًا — لا مفتاح/توكن Google مخزَّن في أي مكان). النقل يدوي:

1. افتح Google Sheet الفارغة (أو التجريبية) ← الإضافات ← Apps Script.
2. لكل ملف `.gs` أعلاه: أنشئ ملف script جديد بنفس الاسم بالضبط (بلا
   الامتداد `.gs` عند التسمية داخل المحرر)، والصق المحتوى كاملًا.
3. لملف `Index.html`: أنشئ ملف HTML باسم `Index` بالضبط (حساس لحالة
   الأحرف، ويُستخدَم في `doGet()`)، والصق المحتوى كاملًا.
4. احفظ المشروع (Ctrl+S / ⌘+S).

## 5. منح وصول صيانة مؤقت (خطوة إلزامية أولى، قبل preflight وقبل أي شيء آخر)

**كل دوال الصيانة في المشروع الآن خاصة (تنتهي بشرطة سفلية `_`) ومقفلة
افتراضيًا** — لا تعمل أي منها (`preflightRelease_`/`applyReleaseSchema_`/
`setupSheets_`/`migrateReferenceData_`/`migrateLegacyReferenceValues_`/
`previewPhoneNormalization_`/`migratePhoneNumbers_`/`diagnoseReferenceDataIssues_`/
`diagnoseStateIntegrity_`/`repairStateIntegrityIssues_`) دون رمز وصول
صيانة صالح، **حتى لو استُدعيت من محرر Apps Script نفسه**. هذا مقصود:
دفاع مستقل عن كونها خاصة بالاسم، لا يعتمد على جلسة ويب ولا على أي رمز
مكتوب في الكود.

**الخطوة الأولى دائمًا**: من محرر Apps Script، اختر `grantMaintenanceAccess_`
من القائمة المنسدلة أعلى المحرر واضغط ▶ Run:

1. تولّد رمزًا عشوائيًا بالكامل (لا رمز ثابت في الكود أو GitHub إطلاقًا).
2. تخزّن بصمته المُجزَّأة فقط في خصائص السكربت (Script Properties) — لا
   الرمز الخام نفسه أبدًا.
3. تطبع الرمز الخام **مرة واحدة فقط** في سجل التنفيذ (Executions ← اضغط
   على التشغيل الأخير ← Logs). **انسخه فورًا من هناك** — لن يظهر مرة
   أخرى ولا يُعاد في أي نافذة أو قيمة أخرى.
4. الرمز صالح لاستخدام متكرر (ليس أحادي الاستخدام) خلال 20 دقيقة
   افتراضيًا (يمكن تمرير عدد دقائق آخر حتى 60 كحد أقصى، مثل
   `grantMaintenanceAccess_(45)`) — يكفي لجلسة صيانة واحدة متصلة
   (preflight ← dry-run ← تطبيق). يُقفَل تلقائيًا بعد 5 محاولات فاشلة
   بصرف النظر عن الوقت المتبقي.
5. عند انتهاء جلسة الصيانة، شغّل `revokeMaintenanceAccess_` لإبطال الرمز
   فورًا بدل انتظار انتهاء الصلاحية الطبيعية.

كل دالة صيانة أدناه تتطلب هذا الرمز **كأول معامل لها**. بما أن زر ▶ Run
في المحرر يستدعي الدالة المختارة بلا أي معاملات دائمًا، لا يمكن تشغيل
دالة صيانة تتطلب الرمز مباشرة من القائمة المنسدلة — اكتب دالة صغيرة مؤقتة
في أي ملف `.gs` (احذفها بعد الاستخدام) تستدعيها بالرمز الذي نسخته، ثم
شغّل **تلك الدالة المؤقتة** من القائمة المنسدلة بدلًا من ذلك:

```js
function _runMaintenanceOnce() {
  const token = 'الصق-الرمز-الذي-نسخته-من-سجل-التنفيذ-هنا';
  const report = preflightRelease_(token);
  Logger.log(JSON.stringify(report, null, 2));
}
```

**لا تترك هذه الدالة المؤقتة في الكود بعد الانتهاء، ولا تكتب الرمز نفسه
في أي commit أو ملف يُدفع إلى GitHub** — الصقه، شغّله، ثم احذف السطر أو
الدالة كاملة قبل أي حفظ/دفع لاحق.

## 6. ترتيب preflight (قراءة فقط — آمن دائمًا، أي عدد مرات، برمز صالح)

بعد منح الوصول (القسم 5)، استدعِ `preflightRelease_(token)` عبر دالة
مؤقتة كما في المثال أعلاه:

- **أول مرة على مشروع فارغ تمامًا**: ستفشل الدالة (لا توجد أوراق بعد) —
  هذا متوقَّع، انتقل مباشرة للقسم 7.
- **على مشروع مُهيَّأ جزئيًا أو بالكامل**: تعيد تقريرًا كاملًا JSON في نافذة
  التنفيذ يشمل: كل ورقة (موجودة/ناقصة)، الأعمدة الناقصة إن وُجدت، ترتيب
  الأعمدة، تعارضات الحالات (`stateIntegrity`)، القيم المرجعية غير المطابقة
  (`referenceData`)، عدد السجلات المتأثرة، حالة مجلد الإثباتات، وإصدار
  المخطط الحالي/المطلوب (`schemaVersion`). **راجعه بعناية كاملة قبل أي
  خطوة تالية.** بفضل رمز الوصول، هذا التقرير لا يُسرَّب لأي زائر عام —
  لا يعمل بلا الرمز الذي تملكه أنت فقط من سجل تنفيذ المحرر.

  **منذ المرحلة العاشرة**: `schemaVersion` صار 3 (كان 2) — 18 عمودًا
  إضافيًا فقط في نهاية «طلبات انضمام الجمعيات» (رقم الترخيص، تاريخه،
  الأسئلة الثمانية، بيانات ملف الترخيص، الإقرار…). حتى تُطبَّق
  `applyReleaseSchema_` (القسم 8) لن ينجح تقديم أي طلب انضمام جديد من
  النموذج العام — القراءة والعرض للطلبات القديمة يعملان دائمًا بلا أي
  شرط. مجلد Drive جديد "مرفقات تراخيص طلبات الانضمام - جمعية الزاد"
  يُنشأ تلقائيًا (خاصًا، لا رابط عام) عند أول رفع ملف ترخيص فعلي — بنفس
  آلية مجلد إثباتات التسليم تمامًا، لا خطوة يدوية مطلوبة.

## 7. ترتيب dry-run (كل عملية بدالتها الخاصة — لا دالة واحدة تُشغِّل كل شيء تلقائيًا)

كل دالة أدناه منفصلة عمدًا (فحص المخطط ≠ إضافة المخطط ≠ ترحيل المرجعيات ≠
تشخيص الحالات ≠ إصلاح الحالات) — لا واحدة منها تستدعي الأخريات تلقائيًا
لتطبيق تغييرات كتابة، فقط `preflightRelease_` تقرأ من دالتَي التشخيص
القرائيتين لتجميع تقريرها. كل استدعاء أدناه يتطلب نفس الرمز من القسم 5:

- إن أظهر `preflightRelease_(token)` أن `referenceData.ready === false` أو
  وجود قيم مرجعية غير مطابقة: شغّل `diagnoseReferenceDataIssues_(token)`
  لتفصيل كامل، ثم `migrateLegacyReferenceValues_(token, true)` (معاينة
  فقط، `dryRun=true`) لمراجعة كل تصحيح مقترَح قبل أي كتابة.
- إن أظهر `stateIntegrity.issueCount > 0`: شغّل `diagnoseStateIntegrity_(token)`
  مباشرة (نفس التقرير المُضمَّن في preflight، لكن بتفصيل أوسع) وراجع كل
  تعارض قبل التفكير بـ `repairStateIntegrityIssues_(token)`.
- إن أظهر `preflightRelease_` رقم جوال يحتاج تطبيعًا: شغّل
  `previewPhoneNormalization_(token)` (معاينة قراءة فقط) قبل أي تفكير
  بـ `migratePhoneNumbers_(token)` (كتابة فعلية).
- لا يوجد "dry-run" منفصل لـ `applyReleaseSchema_` نفسها لأنها إضافية
  بحتة أصلًا (لا تحذف ولا تستبدل) — لكن `preflightRelease_` قبلها يعرض
  بدقة ماذا ستضيف بالضبط (`missingSheets`/`sheetsWithMissingColumns`).

## 8. تطبيق المخطط بعد الموافقة

**فقط بعد رضاك التام عن تقرير القسم 6**، بنفس أسلوب الدالة المؤقتة:

```js
function _runApplyReleaseSchemaOnce() {
  const token = 'نفس-الرمز-من-القسم-5';
  const result = applyReleaseSchema_(token, {});
  Logger.log(JSON.stringify(result, null, 2));
}
```

راجع `createdSheets`/`addedColumns` في السجل — إن كانت فارغتين، المخطط
كان مكتملًا أصلًا. **آمنة لإعادة التشغيل** بنفس الرمز إن احتجت تكرارها
خلال نافذة صلاحيته.

**لا تُستخدَم `setupSheets_()` كخطوة نشر عامة متكررة على الإطلاق.**
تُستخدَم مرة واحدة فقط عند إعداد مشروع فارغ تمامًا لا يحتوي أي حساب مدير
بعد (`applyReleaseSchema_` لا تُنشئ حسابات إطلاقًا، بخلاف `setupSheets_`
التي تُنشئ أول حساب مدير عند أول تشغيل فقط). لأي تحديث مخطط لاحق على
نظام يعمل فعليًا، استخدم `preflightRelease_`/`applyReleaseSchema_` حصرًا:

```js
function _runSetupOnce() {
  const token = 'نفس-الرمز-من-القسم-5';
  const result = setupSheets_(token);
  Logger.log(JSON.stringify(result, null, 2));
}
```

**انسخ بيانات المدير المؤقتة من سجل التنفيذ فورًا بعد هذا التشغيل، قبل
أي خطوة أخرى، وقبل أي نشر عام لرابط التطبيق** (راجع `SECURITY_REVIEW.md`
القسم 2 لتفصيل هذا الترتيب).

## 9. نشر نسخة تجريبية منفصلة

1. Deploy ← New deployment ← Web app.
2. Execute as: **Me** (مالك المشروع).
3. Who has access: **حصريًا وفق قرارك** — للاختبار الأولي، يُفضَّل
   "Only myself" أو "Anyone within [نطاقك]" بدل "Anyone" علنًا، لتضييق
   نافذة الخطر الموثَّقة في `SECURITY_REVIEW.md` القسم 2 إلى أدنى حد ممكن
   أثناء الاختبار.
4. انسخ رابط النشرة الناتج — هذا رابط الاختبار الحي، منفصل تمامًا عن أي
   نشرة سابقة.

## 10. اختبار البوابات الأربع

راجع `RELEASE.md` القسم 5 لقائمة الاختبار اليدوي الكاملة المفصَّلة
(البوابة العامة، الإدارة، الجمعية، المندوب) — لا تُكرَّر هنا بالكامل. أضيفت
هذه المرحلة بنود جديدة يجب اختبارها يدويًا تحديدًا:

- **قالب Excel**: نزّل قالب `.xlsx` من نافذة الاستيراد الجماعي (بوابة
  الجمعية) وافتحه فعليًا في Microsoft Excel حقيقي (أو Google Sheets عبر
  "فتح باستخدام") — تحقق: الأعمدة الرسمية، ورقة "تعليمات" ثانية، القوائم
  المنسدلة تعمل فعليًا عند النقر على خلايا المنطقة/الحالة الاجتماعية/الضمان،
  ورقم الجوال في صف المثال يحافظ على الصفر الأول ولا يتحول لصيغة علمية.
- **تفاصيل الجهاز**: من جدول الأجهزة وصفحة تفاصيل المستفيد، افتح "تفاصيل"
  لجهاز مخصَّص فعليًا لمستفيد له مندوب — تحقق ظهور كل التواريخ والمندوب
  المرتبط وسجل العمليات.
- **سجل المندوب**: من صفحة تفاصيل المندوب (إدارة/جمعية) اضغط "سجل
  العمليات" — وسجّل دخول كمندوب فعليًا وافتح "سجل عملياتي الكامل" من تبويب
  السجل — تحقق أن المندوب لا يرى أحداثًا إدارية (مثل "إضافة مندوب").
- **منع التكرار**: أضف مستفيدًا بجوال موجود مسبقًا لنفس الجمعية — تحقق
  الرفض الفوري بلا كشف بيانات جمعية أخرى؛ أضف مستفيدًا بنفس الاسم والمدينة
  لكن جوال مختلف — تحقق ظهور تنبيه "مطابق محتمل" غير مانع.
- **الجوال**: افتح كل بوابة على جهاز جوال حقيقي (لا محاكي فقط) بعرض قريب
  من 390px — تحقق القائمة الجانبية تُفتح وتُغلق بلمسة، لا تمرير أفقي، لوحة
  المفاتيح لا تحجب زر الحفظ الأساسي في النماذج الطويلة.
- **رفع إثبات التسليم**: من بوابة المندوب، أكّد تسليمًا فعليًا بصورة
  حقيقية ملتقطة من كاميرا الجوال — تحقق نجاح الرفع وظهور رابط الإثبات في
  سجل التسليمات؛ جرّب رفع ملف غير صورة (مثل PDF مُعاد تسميته لامتداد صورة)
  وتحقق الرفض.

## 11. معايير القبول

النسخة تُعتبر جاهزة للانتقال من "اختبار" إلى "استخدام فعلي محدود" فقط عند
تحقق **كل** ما يلي معًا:
- كل بند في قائمة الاختبار اليدوي (القسم 9 هنا + `RELEASE.md` القسم 5)
  ✅ منفَّذ فعليًا على المتصفح/الجوال الحقيقيين، لا افتراضًا.
- `preflightRelease_(token)` يعيد `missingSheets: []` و`sheetsWithMissingColumns: []`
  و`stateIntegrity.issueCount === 0`.
- كل مجموعات الاختبار المحلية (القسم 12 في `RELEASE.md`، و`tools/*.js`)
  خضراء بالكامل عند آخر commit قبل النشر.
- بيانات المدير المؤقتة الأولى وأي كلمة مرور مؤقتة أخرى **غُيِّرت فعليًا**
  من كلماتها الافتراضية.
- رابط النشرة "Who has access" مضبوط عمدًا وفق قرارك، لا تركه على الإعداد
  الافتراضي دون مراجعة.

**لا تُعلَن هذه النسخة "Production Ready" حتى الآن** — هذا خارج نطاق
هذه الجلسة عمدًا، القرار قرارك بعد اكتمال القسم 9 فعليًا.

## 12. خطة الرجوع (Rollback)

راجع `RELEASE.md` القسم 7 للتفصيل الكامل (5 خطوات، تشمل التراجع عن نشرة
كود عبر Manage deployments، وتصحيح بيانات هاتف يدويًا، واستعادة سجل
إصدارات Google Sheets). **إضافة هذه المرحلة**: إن طُبِّق `applyReleaseSchema_(token, {})`
وظهرت مشكلة، لا حاجة لأي "تراجع" خاص — الدالة **لا تحذف ولا تستبدل أي شيء
قائم إطلاقًا**، فقط تضيف أعمدة/أوراق فارغة؛ يمكن حذف أي ورقة/عمود أُضيف
يدويًا من واجهة Google Sheets نفسها إن رغبت، بلا أي أثر على بيانات موجودة
مسبقًا.

**نقطة توقف واضحة**: إن فشلت أي خطوة أعلاه (خطأ غير متوقَّع، تقرير
`preflightRelease` يظهر شيئًا لا تفهمه)، **توقف فورًا ولا تكمل للخطوة
التالية** — راجع الخطأ كاملًا، وإن لزم الأمر ارجع لنسخة النشرة السابقة
(القسم 2/الخطوة 3) بدل المحاولة على النسخة نفسها.

---

## بنود لا تزال تحتاج اختبارًا حيًا (لم تُختبَر من هذه الجلسة)

- **(المرحلة العاشرة) إرسال نموذج تقديم جمعية فعلي مع قطع اتصال متعمَّد
  بعد الضغط على "إرسال"، ثم إعادة المحاولة من نفس المتصفح** — يختبر
  `clientRequestId`/idempotency في ظروف شبكة حقيقية لا تحاكيها بيئة
  Node. كذلك رفع صورة ترخيص حقيقية والتحقق من خصوصية مجلد Drive الناتج.
- **(المرحلة الحادية عشرة) لوحة تحكم الإدارة بالوحدات الست** نُفِّذت
  ومُختبَرة آليًا بالكامل (976/976) — يبقى فقط **معاينتها بصريًا على
  متصفح حي** (تخطيط الشبكة على شاشات فعلية مختلفة الأحجام، ألوان
  التنبيهات، سلاسة النقر من كل رقم إلى قائمته المفلترة)؛ راجع
  `RELEASE.md` القسم 20 و`HANDOFF.md` للتفصيل الكامل.
- فتح ملف `.xlsx` الجديد داخل Microsoft Excel حقيقي (اختُبر بنيويًا فقط
  عبر Python `zipfile`/`ElementTree`، راجع `RELEASE.md` القسم 13).
- كل بند في القسم 9 أعلاه (البوابات الأربع، الجوال الحقيقي، رفع إثبات
  تسليم حقيقي).
- تباين الألوان وفق WCAG AA بأداة قياس فعلية؛ قارئ شاشة فعلي (NVDA/
  VoiceOver)؛ تكبير النص إلى 200% في متصفح حقيقي.
- أداء Apps Script/Sheets الفعلي على بيانات كبيرة حية (قِيس محليًا فقط،
  راجع `RELEASE.md` القسم 9).
- اختراق مستقل احترافي — هذه الجلسة قدَّمت مراجعة أمنية برمجية ومحاكاة
  فقط (`SECURITY_REVIEW.md`)، وليست بديلًا عنه.
